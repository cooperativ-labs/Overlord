{
  "name": "@overlord/remote-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Overlord remote helper daemon — runs on the target machine and exposes filesystem + git operations over HTTP to the Electron and mobile clients.",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "overlord-remote-agent": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
