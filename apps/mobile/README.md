# Mobile Workspace

This workspace is reserved for the future React Native / Expo mobile surface.

It is intentionally scaffold-only for now so the repository can adopt a stable monorepo layout
before the mobile app implementation lands.
