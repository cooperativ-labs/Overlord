{
  "tool": "create_ticket_draft",
  "userMessage": "Create a ticket draft but simulate a malformed save response.",
  "toolInput": {
    "conversationContext": "Set up the Sunpeak resource, then verify the UI handles a save response that omits ticket metadata."
  },
  "toolResult": {
    "structuredContent": {
      "draft": {
        "title": "New ticket draft",
        "description": "Set up the Sunpeak resource, then verify the UI handles a save response that omits ticket metadata.",
        "priority": "medium",
        "projectId": null,
        "projectName": "Default project",
        "sourceSummary": "Set up the Sunpeak resource, then verify the UI handles a save response that omits ticket metadata."
      },
      "ticketCard": {
        "saveToolName": "save_ticket_draft",
        "resourceUri": "ui://overlord/ticket-card"
      }
    }
  },
  "serverTools": {
    "save_ticket_draft": {
      "content": [
        {
          "type": "text",
          "text": "Saved, but no ticket details were returned."
        }
      ],
      "structuredContent": {}
    }
  }
}
