import type { ServerConfig } from 'sunpeak/mcp';

export const server: ServerConfig = {
  name: 'Overlord Ticket Card',
  version: '0.1.0',
  description:
    'Local Sunpeak scaffold for the Overlord ticket-card MCP app while the production MCP server remains on Supabase.'
};
