# Overlord Component Interaction Contract

The current contract version is stated once under [Contract Version](#contract-version).
Do not restate the numeric version elsewhere in this document.

## Purpose

This is the normative specification for how Overlord's components interact. It defines:

- The **component registry** — what each component owns and is responsible for
- **Interaction surfaces** — the only sanctioned communication paths between components
- **Stable interfaces** — what cannot change without a contract version bump
- **Extension points** — the only sanctioned ways to extend Overlord
- **Conformance requirements** — what a shipped component must satisfy before integration
- **Maintenance rules** — when and how to update this document

**Agents and developers MUST read this document before implementing any change that crosses module boundaries, and MUST update it before implementing any change that extends or conflicts with the current contract.**

See `.claude/skills/component-contract/SKILL.md` for the enforced agent workflow.

## Editions

Overlord ships in two editions that share one contract:

- **Overlord Local** (default): the Electron desktop shell supervises a loopback
  backend over SQLite on the user's machine. No account or network dependency.
- **Overlord Cloud** (additive): the backend and system-of-record database run as
  a hosted control plane; the desktop app, web app, CLI, and execution targets
  connect over HTTPS. Same REST, protocol, and realtime contract — only
  `backend_url` and auth differ.

Both editions are first-class. A contract change for Cloud must never regress Local;
where a surface differs by edition this document calls it out explicitly.

## Contract Version

Current version: `116`

This `Current version` line is the **sole authoritative** statement of the contract
version in this document. Automated checks and agents MUST read it (and
`contract/components.yaml`) — never a header duplicate. The contract version is
incremented when any stable interface changes. All conformance manifests must
declare the contract version they were validated against.

### Version 103 Change Summary

Phase 3 exposes the authoritative project Run Queue to agents without creating a
second queue implementation. Additive Protocol `queue-objective`,
`dequeue-objective`, and `run-queue` resolve normal display-id and project
references, preserve existing RBAC, and use the Run Queue service for every
membership or ordering mutation. Queue placement accepts a queue, predecessor,
front, or one-based insertion position; entries remain execution-target neutral.

Hosted MCP and connector shims add `overlord_queue_objective`, which forwards
queue/add/remove intent through Protocol. `autoAdvance` is now a deprecated,
derived compatibility projection: Protocol create/prompt/add/update flags map
to Run Queue membership and preserve mission-local queue order. The legacy
storage write remains during this release, retires in the next release, and
column removal requires a later contract-version bump.

### Version 107 Change Summary

`pending_delivery` is execution, not a fault. It is the state an objective
returns to when its agent re-attaches after finishing a turn, and every
presentation surface now treats it that way. `MissionDto.hasExecutingObjective`
and the Live Activity running/completion projections count an objective as
executing while it is `executing` **or** `pending_delivery`, so the mission card
keeps its executing shimmer instead of going quiet between the last update and
delivery. `ActivityFeedRunItemDto.state` additively accepts `pending_delivery`
and the activity feed's run query includes those objectives, so a re-attached
objective stays on the Inbox feed as running work rather than dropping out of
it. The read shapes, authorization, and truncation semantics are otherwise
unchanged.

### Version 108 Change Summary

Latch protocol major 2 retires the legacy Overlord conversation-observation
integration. Overlord continues to use Latch as a persistent-terminal provider
through capability discovery plus create, inspect, open, attach, and stop, but
no longer invokes the removed `latch events`, `latch send`, or session-level
interaction-capability commands. The runner and mission REST harness-event
ingest routes and the mission resolve-observation route are removed;
`MissionDetailDto.terminalSessions` no longer exposes the legacy `observation`
fold (event cursor, turn count, pending input, or unattached state).

Overlord does not adapt or emulate the deleted v1 conversation surface. A future
conversation UI must be a native client of Latch's v2 Conversation Hub and its
server-first, revisioned WebSocket contract. Until then, terminal lifecycle is
the complete Overlord/Latch boundary; protocol attach/update/deliver remains
independent of Latch.

### Version 109 Change Summary

`GET /api/objectives/:id/effective-launch-config` additively exposes the
authoritative, read-only launch-config projection for one authorized objective,
agent, and selected execution target. It returns the normalized resolved
pre-command/flags, the precedence source (`objective`, `resource_source`,
`user_target`, `workspace`, or `none`), and the resolved target id. The REST
service calls the shared `resolveLaunchConfig` path used by direct launch and
Run Queue dispatch; clients must not reconstruct resource-source precedence.
An explicit empty objective override remains a real override and therefore
returns `source = objective` with empty mechanics. Deleting that override is
the only way to resume inherited defaults.

### Version 110 Change Summary

The existing authorized project Run Queue read projection additively exposes
safe execution-request diagnostics for each entry: request status, bounded
last error, claimant runner and execution target identifiers, and request
creation, claim, launch, update, and terminal timestamps. It also returns a
bounded `diagnosticCode` when the queue/request pair is impossible, including
`terminal_request_dispatched` and `running_without_live_request`. The
projection never exposes launch commands, environment, credentials, terminal
output, or provider-session metadata. Dispatch failures use the same shared
redaction and bounding policy before they become a queue hold reason, service
log detail, or response projection.

### Version 111 Change Summary

Protocol `add-objectives` additively accepts per-item `agent` and `model` in
its objectives JSON/file payload, and hosted MCP `overlord_add_objectives`
exposes the same fields. An explicit `agent` is persisted as the appended
objective's `assignedAgent`; an accompanying `model` is persisted with it.
A model without a non-empty explicit agent is rejected, because it cannot
identify a launch selection. Existing items that omit both fields retain the
project launch-preference defaulting behavior. The existing `resourceKey` and
Run Queue `autoAdvance` semantics, authorization, and response shapes are
unchanged.

### Version 112 Change Summary

Phase 0 of hook-first file-change attribution establishes the connector contract
without enabling the new runtime path. Every shipped connector descriptor now
declares fixture-backed mutation-window evidence with one honest classification:
`paired`, `post-only`, or `unsupported`. A `paired` claim proves matching native
session, call, workspace-root, tool, outcome, and direct-path fields in recorded
pre/post payloads. `post-only` proves only completion-side evidence; `unsupported`
records why no usable pair is evidenced. The descriptor generator executes these
fixtures and rejects a classification that lacks the required proof.

The Connector → Protocol hook surface is prepared for the later local-only
pre/post attribution command: adapters must resolve scope before logging or
spawning, use bounded best-effort execution, never make network/database calls,
and never persist or transmit file content, diffs, raw commands, transcripts,
environment values, or fingerprints. This release ships no mutation ledger,
sync command, delivery behavior, database, or REST change; those remain later
phases. The existing PostToolUse touched-file compatibility path is unchanged.

### Version 113 Change Summary

Phase 1 of hook-first file-change attribution makes delivery non-blocking with
respect to change tracking and advisory evidence. A delivery requires only its
summary: missing change rationales, rationale skips, and unavailable change
tracking are review metadata and cannot reject the lifecycle transition.
Malformed `deliveryReport.agentReport` items are salvaged independently; valid
siblings persist and bounded warnings are retained with the delivery report.

The mission File Changes REST projection is changed-file-first. Every observed
`changed_files` record appears once, with an optional rationale, so mechanical
evidence is never hidden behind author-supplied prose. Active changed-file
identity is `(objective_id, file_path)`, rather than session-scoped, allowing
one objective to retain one durable record per path across sessions.

### Version 114 Change Summary

Phase 2 adds the additive Protocol `sync-changes` operation. An attached
execution target submits bounded, metadata-only objective-ledger evidence using
its existing session key; every item independently reports `accepted`,
`ignored`, or `warning`, so one malformed item cannot discard valid siblings or
block delivery. The service upserts the objective/path `changed_files` identity
and stores only source, quality, overlap, window, and hook-health metadata —
never content, diffs, commands, transcripts, environment, or fingerprints.

The CLI owns a versioned local ledger keyed by canonical working directory,
objective, and protocol session. CWD alone must never select an objective:
unbound or ambiguous hook calls record local health and create no attribution.
The legacy VCS delta remains shadow-only during this phase. Local and Cloud
execution targets use the same Protocol payload; no backend reads an execution
target filesystem.

### Version 115 Change Summary

Phase 4 cuts the normal product path over to the objective/session-keyed local
ledger. CLI update, change preflight, and delivery drain metadata-only ledger
evidence through Protocol before their primary operation. Paired connector
evidence is authoritative; post-only and hookless compatibility observations
are explicitly `fallback_delta` review metadata and never exact attribution.

The normal `deliver` request requires only its summary. Legacy changed-file,
observed-dirty-path, rationale-skip, and rationale fields remain accepted for
older clients and useful optional annotations, but the CLI no longer synthesizes
them from a worktree delta, arbitrates peer claims, invents skip reasons, or
preflights rationale coverage. Ledger synchronization, hook health, path
source, overlap, and optional rationale evidence can produce bounded review
warnings but cannot reject delivery. Both Local and Cloud execution targets use
the same metadata-only Protocol sync payload; neither backend reads the target
filesystem.

### Version 116 Change Summary

Phase 5 is an immediate, breaking cutover to objective-ledger change tracking.
Protocol `update` no longer accepts changed-file payloads. Protocol `deliver`
accepts only its required summary plus current optional annotations
(`changeRationales`, artifacts, and `deliveryReport`); changed-file,
observed-dirty-path, no-file-change, and rationale-skip inputs are removed from
CLI, REST, MCP, service, help, and connector instruction surfaces. `record-work`
retains explicit `changedFiles` because it records already-completed work and has
no attached execution target capable of producing a ledger.

File-backed Protocol inputs use the per-flag `fileInputs` envelope exclusively.
The retired shared `stdin` payload is removed; a literal `--*-file -` is resolved
by the CLI into that flag's own `fileInputs` entry before transport.

The retired mission-scoped VCS baseline, touched-file log, transcript-derived
rationale notes, peer-claim arbitration, and `fallback_delta` evidence path are
removed. A connector PostToolUse callback may append `declared_edit` evidence
only when the connector-owned codec normalizes the event as `file.edited` and
produces a bounded path inside the explicitly addressed objective workspace.
Known read-only callbacks are silent; mutation-capable, shell, unmapped, and
no-path callbacks append no path claim and instead record bounded unavailable
hook health. Generic native payload path parsing is forbidden, and CWD never
selects an objective. No shipped connector currently
claims paired-window support; `window_observed` remains reserved for a future
connector whose strict fixtures and runtime both prove a matching pre/post
window.

The local ledger is owner-only, atomically updated under a cross-process lock,
and records normalized repo-relative paths only. Every synchronized item carries
source, quality (`direct` or `window`), overlap, and bounded hook-health metadata.
The backend rejects malformed or absolute paths item-by-item, preserves the
strongest observation for an objective/path, and updates last-observer session
provenance. Objective/path uniqueness migrations deduplicate existing rows before
installing the new index. Historical compatibility payloads and stored metadata
are not accepted as active write inputs.

Optional rationale annotations use one exact shape: `filePath`, `label`,
`summary`, `why`, `impact`, and optional `hunks` containing only `header`.
Unknown or retired keys discard only that advisory item with a bounded warning;
duplicate canonical paths resolve deterministically to the last valid item.
`update` persists non-final annotations and `deliver` / `record-work` persist
final annotations through one shared writer. Accepted changed-file and rationale
writes append scoped `entity_changes` in the same transaction, and the File
Changes projection joins the latest annotation regardless of finality.

Objective-ledger and `record-work` observations set `current_diff_state` to
`unknown`; a captured path is evidence of an edit, not evidence that a current
VCS diff was inspected. Historical `present` and `resolved` rows remain readable.

### Version 106 Change Summary

Objectives additively record `launched_at` and `started_at` alongside the
existing `completed_at`, and `ObjectiveDto` exposes all three as
`launchedAt`/`startedAt`/`completedAt`. Both new stamps are first-wins, so a
re-launch or a post-delivery re-attach never rewrites them. A mission's
objective list is now ordered by those moments instead of queue position:
complete oldest-completion first, then executing/pending_delivery by start,
then launching by launch, then draft/submitted and future in position order.
Rows with no recorded moment fall back to position, so untimestamped missions
render unchanged. The position queue itself is unchanged and remains what
`planEnsureDraftSlot`, auto-advance, and run-queue dispatch read; display order
is presentation only.

Run Queues become mission-scoped and are created lazily. `run_queues` gains a
nullable `mission_id` (unique among live rows) and `RunQueueDto` additively
exposes `missionId`/`missionDisplayId`. A queue is created only when a user
creates one by hand or when an objective is queued from a mission that has no
queue yet: an enqueue with no explicit `queueId` — REST, Protocol
`queue-objective`, and the legacy auto-advance placement alike — resolves the
objective's own mission queue and creates it on demand. `queue-objective` still
prefers an explicit `--queue`, a predecessor's queue, or the objective's current
queue first, and falls back to the project default only when the mission has no
queue. Removing the last entry from a mission queue retires that queue, so a
project never accumulates one queue per mission. The default project queue is
unchanged where it already exists and is never auto-created.

`DELETE /api/run-queues/entries/:entryId` accepts an additive `{ force?: boolean }`
body under the same `execution_request:create` authorization. Forced removal is
the recovery path for an entry wedged in `dispatched`/`running`: it drops the
entry, clears the objective's still-active execution requests, and resets an
objective still parked in `launching` back to `draft` so it can be launched
again. An objective already `executing` keeps its live session. The response is
additive (`forced`, `objectiveId`, `previousState`, `objectiveReset`,
`clearedExecutionRequests`, `removedEmptyQueueId`); unforced removal behavior is
unchanged.

The same release additively exposes the existing Run Queue service to agents.
Protocol provides `reorder-run-queue`, `create-run-queue`, `update-run-queue`,
`delete-run-queue`, and `reorder-project-run-queues`; `run-queue` can derive
its project from an objective or mission and can narrow to one queue. Unlike
the phase plan's fixed project flags, the shipped Protocol queue-definition
commands use the same objective/mission project derivation. Hosted
MCP and connector shims provide `overlord_list_run_queues`,
`overlord_reorder_run_queue`, and action-dispatched
`overlord_manage_run_queue`, while `overlord_queue_objective` adds front/rank
placement. Entry membership and entry ordering require
`execution_request:create`; queue-definition lifecycle requires
`project:update`, so a mission-lifecycle-scoped token cannot create, rename,
pause, delete, or reorder queues.

### Version 105 Change Summary

A `USER_TOKEN` a signed-in user mints for themselves — CLI login and the
settings page — consents to every current and future workspace in the caller's
organization, and no longer requires a workspace selection. Workspace consent
narrowing exists for third-party OAuth clients, which continue to pass it
explicitly from the approval screen; there is no third party to narrow for when
a user authorizes their own CLI, and requiring a selection made token creation
fail outright for any account with more than one workspace. Runtime
authorization is unchanged: consent is always intersected with live membership
and per-workspace RBAC, so joining or leaving a workspace takes effect
immediately without reissuing the token. `user_tokens.workspace_id` and
`workspace_user_id` remain issuance attribution and are never authorization
inputs.

Token ownership resolves from the authenticated profile. Session and
`USER_TOKEN` requests establish no ambient workspace member, so an owner lookup
must not fall back to the instance's oldest profile for an authenticated caller.

### Version 104 Change Summary

Project Run Queues gain additive `PATCH /api/projects/:id/run-queues/order`,
authorized as a project-definition mutation. It accepts every live queue ID
exactly once and rewrites only queue display order; membership and dispatch
semantics are unchanged. This enables the Phase 4 multi-queue UI to order
independent queues through the Run Queue service rather than mutating
objective positions directly.

| Version | Changes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `108`   | Latch protocol-major-2 boundary (coo:815). Overlord retains Latch discovery and persistent-terminal create/inspect/open/attach/stop, updates discovery compatibility to require protocol version 2, and removes the retired v1 conversation integration: no `latch events`, `latch send`, session-level interaction capabilities, harness-event REST ingest, pending-input resolution, event cursor, harness ledger, or `TerminalSessionDto.observation`. A future conversation UI must be a native Latch v2 Conversation Hub client; protocol lifecycle remains independent of Latch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `107`   | Pending-delivery objectives read as live execution (coo:812). `ActivityFeedRunItemDto.state` additively accepts `pending_delivery` and `GET /api/activity-feed` includes `pending_delivery` objectives in its run projection alongside `launching`/`executing`. The mission aggregate `MissionDto.hasExecutingObjective` and the Live Activity running/completion projections now count an objective as executing while it is `executing` **or** `pending_delivery`, so a mission whose agent re-attached after finishing a turn keeps its executing presentation until it delivers. Field names, DTO shapes, authorization, and truncation semantics are unchanged; clients that only check `state === 'executing'` see one additional state value in an already open-ended union.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `106`   | Mission-scoped, lazily created Run Queues. `run_queues.mission_id` (nullable, unique among live rows) binds a queue to one mission; `RunQueueDto` adds `missionId`/`missionDisplayId`. An enqueue without an explicit queue resolves the objective's own mission queue and creates it on demand, and emptying a mission queue retires it, so queues exist only where work was actually queued. `DELETE /api/run-queues/entries/:entryId` accepts an additive `{ force?: boolean }` body that drops an in-flight entry, clears the objective's active execution requests, and resets a stuck `launching` objective to `draft`; authorization and unforced behavior are unchanged. The existing Run Queue service is also exposed through five additive Protocol subcommands and three MCP tools: entry operations require `execution_request:create`, while queue-definition operations require `project:update`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `102`   | Project-scoped Run Queue engine (coo:786 Phase 1). Add `run_queues` and `run_queue_entries` as the authoritative, target-neutral sequencing model ahead of the unchanged `execution_requests` delegation surface. Each project has one default queue and may have independent additional queues; each unpaused queue dispatches at most one entry at a time after its prior entry delivers, while held entries remain visible and are skipped. Add eight authorized REST operations for project queue reads, queue CRUD/pause, and entry enqueue/dequeue/move/reorder; add `ObjectiveDto.queueEntry` and derive compatibility `autoAdvance` from live membership. Dispatch runs through a project-deduped durable worker job, resolves target/config only at dispatch, promotes only the selected future objective, uses entry-scoped idempotency, and writes queue ordering through to objective positions. Existing runner and virtual-target wire behavior is unchanged; `requested_source = run_queue` is additive.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `101`   | MCP PM-management parity (coo:781). Add protocol `list-deliveries`, `launch-objective`, and `reorder-future-objectives`, then expose them as hosted and local-shim MCP tools. They forward only to the existing authorized delivery read, execution-request queue, and future-objective reorder services: delivery records keep the normalized `DeliveryDto[]` shape without raw payload JSON; launch keeps objective UUID/display-id addressing, `execution_request:create` authorization, explicit agent/model/reasoning-effort/target selection, and existing launchability/idempotency rules; reorder keeps the full ordered future-objective UUID list, `objective:update` authorization, and current state/position validation. Hosted and local MCP schemas and behavior stay aligned.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `100`   | Agent search surface (coo:789 Phase 3). Protocol adds canonical `search`, with `search-missions` retained as an exact compatibility alias; both accept v3 `entityTypes`, `objectiveStates`, and `matchesPerResult` controls when `--response-version 3` is selected. `overlord_search_missions` now returns v3 grouped results by default and accepts the same filters plus `detail` (`compact` default or `full`). Compact caps matches at two per mission, removes child snippets, and omits child `matchedTerms`, `createdAt`, and `updatedAt`; it retains ids, display ids, entity types, scores, and all envelope completeness metadata. Full preserves the v3 envelope. Hosted MCP and the local connector shim expose the identical schema and semantics. Existing callers that use `search-missions` without v3-only flags remain supported; artifacts are still unindexed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `99`    | Grouped v3 search retrieval (coo:789 Phase 2). Additive `GET /api/search/v3` returns `SearchResponseV3`: mission-anchored groups with ranked `matches[]` for identifiable objective and delivery hits, `anchorOnly`, `matchCounts`, `entityCounts`, and `truncatedCandidates`. `event` documents remain corroboration-only and never appear in `matches`. `matches` is matched-only, never a complete objective list. Child relevance is `docScore + 0.15 * missionScore`; group ordering stays the v2 MAX-plus-bounded-corroboration fusion. The coverage floor stays at mission level. Per-workspace candidate fetch is bounded at 500 documents; when that cap hits, `totalMatchedBeforeLimit` and `entityCounts` are lower bounds and `truncatedCandidates` is true. Additive filters: `entityTypes` (`mission`/`objective`/`delivery`, default all three), `objectiveStates`, `matchesPerResult` (default 3, max 10). `limit` and per-workspace quota still allocate mission groups; `matchesPerResult` is a separate per-group cap. Objective display-id queries return that objective as the group's single match. REST v3 accepts only stable project UUIDs. Protocol `search-missions --response-version 3` returns the same envelope with v3 defaults; v1 and v2 routes, corpus, and result sets are unchanged. Artifacts remain unindexed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `98`    | Unified mission search begins indexing bounded delivery documents (coo:789 Phase 1). `search_documents.entity_type` additively accepts `delivery`; adapter triggers and idempotent migration backfills maintain one document per live delivery, retaining the owning mission and objective provenance. A delivery body combines `summary`, `verification_summary`, `follow_up_notes`, and only the normalized `deliveryReport.agentReport` fields: `humanActions`, `knownRisks`, `deferredWork`, `assumptions`, and each tradeoff `decision`; it is capped at 20,000 characters. Mission documents add `constraints_text`, `output_format_text`, and `notes_text`. Notes remain excluded from attach/load context and prompt assembly, but are now searchable by every authorized caller, including agents. Artifacts are not indexed. Existing v1 and v2 search routes, protocol, CLI, and MCP retain their response shapes and result corpus (mission/objective/event documents); deliveries become returnable only through the subsequent v3 surface.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `97`    | Agent surfaces accept human project references, and "scheduled" becomes a searchable date (coo:781). Protocol `--project-id` and MCP `projectId` accept a project UUID, slug, or name; names match case-insensitively across the caller's live memberships. Because slugs and names are unique per workspace rather than per organization, a reference matching in more than one workspace returns the structured `project_selection_required` result — `{ status, message, projectRef, projects[] }`, each project labelled with its workspace — instead of a 409, and an additive `--workspace-id` / `workspaceId` (id, slug, or name) narrows the retry. This replaces the MCP-side UUID gate, which denied agents a resolver the protocol already had. `MissionSearchDateField` additively accepts `dueDatetime` alongside `createdAt` and `updatedAt`, and `MissionSearchResultV2` additively carries `dueDatetime`. A mission is _scheduled_ by its due date, not by an execution timer; because `missions.due_datetime` is nullable, a `dueDatetime` range excludes missions that were never scheduled, which is the opposite of the created/updated columns. REST v2 keeps its UUID-only `projectIds` and its frozen v1 response.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `96`    | My Missions is a status-_type_ board (coo:753). The aggregate surface renders exactly four columns — `next` / `execute` / `review` / `complete`, published as `MY_MISSIONS_COLUMNS` — instead of deduplicating project status _names_, which are project-defined and inconsistent across an organization. Missions whose status type is `draft`, `blocked`, or `cancelled` are not shown on My Missions; they remain on their project board. `MyMissionReorderRequest` replaces `statusId` with `statusType`: one `PATCH /api/workspace/my-missions/order` call carries the whole column across every project and workspace it aggregates, and the server resolves each moved mission's target status **inside that mission's own project** (the lowest-position active status of the type). A mission already in a status of the target type keeps its concrete status, so project-specific column naming survives a drag. A project with no active status of the requested type rejects the whole call with `STATUS_UNAVAILABLE_FOR_WORKSPACE` before any write. `my_mission_positions` is unchanged; positions are now assigned across the whole aggregated column, so an interleaved cross-project order round-trips.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `95`    | Cross-workspace authorization is organization-bounded: OAuth `USER_TOKEN`s bind to one organization with either explicit workspace consent or all current/future workspace consent, always intersected with live membership and RBAC; legacy tokens backfill only to their recorded issuance workspace. V2 search adds the organization-bounded, non-redistributed quota envelope and explicit date/project filter semantics while retaining the v1 REST response.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `94`    | Project status types gain `next`, a non-terminal lifecycle type for work intended to execute soon but not yet active. Every project has exactly one active `next` status, enforced by adapter partial unique indexes and service validation. New projects seed `Next Up` (`next_up`) with type `next`; the migration retags existing active `next_up` status rows from `draft` to `next`. Status type filters and discovery may return `next`; protocol update phases remain unchanged because they describe agent side effects, not board column vocabulary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `93`    | Project-scoped mission status definitions (coo:752). Status DTOs and mutation bodies are renamed from `Workspace*Status*` to `Project*Status*`, with every status carrying its owning `projectId`; status type values remain unchanged. Status CRUD moves to `/api/projects/:id/statuses*`, authorized with `project:read` / `project:update`, and `GET /api/workspaces/:id/project-statuses` provides the aggregate read. Both former workspace route families, `/api/workspace/statuses*` and `/api/workspaces/:id/statuses*`, are removed. Status-edit authorization therefore shifts from `workspace:update` to `project:update`; under the shipped roles this does not expand the effective editor set. A mission's `status_id` must belong to its own project. Scheduled duplicate targets are stored as `schedules.next_status_key` and resolved in the mission's project. Because board columns now vary per project, discovery is additive on the agent surfaces: Protocol gains read-only `statuses --project-id`, the CLI gains `ovld statuses list --project-id <id                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | slug | name>`, and MCP gains read-only `overlord_list_project_statuses`. The agent protocol is otherwise semantically unchanged — every status side effect and filter is expressed in status *types*, which do not move. `GET /api/missions/search`additively accepts a`statusTypes`CSV so the long-documented`ovld missions list --status` filter is actually applied; it filters types, never project-defined names. |
| `92`    | Objective-scoped live-session reads (coo:776 review). Authenticated `GET /api/agent-requests` and `GET /api/agent-session-inputs` accept optional `objectiveId` alongside `missionId`; a display id supplies the mission when omitted, a UUID still requires it, and supplying both validates that they name the same objective and mission. Request/input rows and `liveChannel` are filtered to that objective. `ovld requests` and `ovld inputs list` expose the same addressing, the local MCP shim delegates objective display-id parsing to the CLI, objective-scoped session-key reads never fall back to a sibling's mission-wide key, and cleanup removes every alias for a delivered session without removing a different live sibling's key. This also corrects v91 declarations that described `--mission-id` as unconditionally required.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `91`    | Objective-addressable tool surface (coo:776). Every CLI protocol subcommand, MCP tool, and REST-backed management command that accepted `--mission-id` / `missionId` now also accepts `--objective-id` / `objectiveId` (objective UUID or `{mission.display_id}.{display_key}`). Because an objective display id already names its parent mission, `--mission-id` may be **omitted** whenever a full objective display id is supplied — the CLI derives it client-side and the protocol/MCP layer derives it server-side — so an agent holding only `coo:756.k7xm` can attach, update, deliver, and reconnect without reconstructing the mission id. An objective **UUID** carries no parent, so it still requires `--mission-id`. Additive optional `--objective-id` on `load-context` (returns that objective's context instead of rediscovering the active one, which removes `ambiguous_active_objective` on parallel missions), `connect`, `discuss-objective` (submits that draft rather than the first draft found), `add-artifact` (stamps objective provenance with no live session key), `attachment-list`, and `attachment-download-url`. Hosted MCP `overlord_load_mission_context`, `overlord_attach_session`, `overlord_update_session`, `overlord_deliver_session`, and `overlord_add_artifact` take the same optional `objectiveId` with `missionId` no longer required when it is a display id. The objective/mission reference grammar moves to `@overlord/contract` (`parseObjectiveRef`, `formatObjectiveDisplayId`, `missionDisplayIdFromObjectiveRef`), re-exported unchanged from `@overlord/database`, so the CLI parses public ids without the persistence layer and no surface carries a private copy of the regex. `POST /api/runner/clear` `objectiveId` and artifact-create `objectiveId` accept display ids instead of silently matching nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `90`    | Profile-scoped Everhour identity (coo:769). An Everhour personal API key is stored once per profile in `ext_everhour_user_connections` as AES-256-GCM ciphertext (`EVERHOUR_API_KEY_ENCRYPTION_KEY`, falling back to `GITHUB_USER_TOKEN_ENCRYPTION_KEY`) and applies across every workspace the caller belongs to. Authenticated `GET`/`PUT`/`DELETE /ext/everhour/user-connection` are account-wide with no workspace permission, matching `/ext/github/user-connection`. Existing `/ext/everhour/integration` remains as a deprecated alias of that same profile-scoped surface. Timer and time-entry writes use the acting user's key so Everhour attributes them to that person; a member who has not connected is told to connect in Settings rather than sharing another member's secret. Workspace-scoped keys are adopted onto a profile only when `entity_changes` attribution uniquely identifies that profile as the connector. GitHub App installations and Everhour/GitHub resource links stay workspace/project/mission-scoped. Credentials never enter DTOs, logs, realtime, or change projections.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `89`    | Same-resource objective parallelism (coo:756 Phase E). With `missions.allow_parallel_objectives` true, `POST /api/objectives/:id/launch` no longer 409s a sibling on the **same** `resource_key`. Two concurrent objectives are kept apart by the Runner Layer instead: the shared branch/worktree planning algorithm gains an optional `isolation: { objectiveKey }` input that cuts a per-objective branch `<mission branch>-<objectives.display_key>` off the branch the launch would otherwise have shared, in its own worktree (regenerated `contract/branch-planning-vectors.json`). Isolation applies only in worktree mode and never overrides an explicit branch pin; a mission that runs without worktrees has one checkout and concurrent objectives deliberately share it. `POST /api/missions/:id/branch-prepared` accepts an additive `branchAutomation.isolated` boolean; an isolated branch is recorded on `objectives.branch` and audited, but never moves `missions.active_branch` or the mission-level branch observation. Default remains serial.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `88`    | Agent objective instruction editing (coo:767). Protocol `update-objective` additively accepts `--instruction-text` / `--instruction-text-file` alongside the existing auto-advance flags; at least one editable field is required. Instruction text may be changed only when the objective is in `draft` or `future` state (blank text remains allowed in those states). Hosted MCP `overlord_update_objective` forwards the same `instructionText` field to Protocol `update-objective`. REST `PATCH /api/objectives/:id` is unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `87`    | Activity feed blocking-question recency (coo:757). `GET /api/activity-feed` includes unseen `ask` mission events only when `mission_events.created_at` is within the past 3 days (rolling 72-hour window from the request instant). Older unseen questions remain acknowledgable via mission surfaces but no longer appear in the inbox live feed. DTO shapes are unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `86`    | Activity feed agent identity and objective provenance (coo:757). `ActivityFeedItemBaseDto` additively carries `createdByKind` / `createdByAgent` from the underlying objective (human fallback when the objective join is null), so inbox Running and Delivery cards can render the same agent-authored sparkle mark used on board/list surfaces. Feed agent projection treats the protocol session sentinel `unknown` as absent and falls back to the objective's `assigned_agent`, so cards show the connector icon and model rather than the literal label "unknown".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `85`    | Opt-in cross-resource objective parallelism (coo:756 Phase D). `missions.allow_parallel_objectives` is `boolean NOT NULL DEFAULT false`. When true, `POST /api/objectives/:id/launch` may queue a second objective on the same mission only if its effective `resource_key` differs from every launching/executing/pending-delivery sibling (null inherits the project's primary resource). Same-resource pairs still 409 until Phase E. `MissionDto` / `UpdateMissionBody` expose additive `allowParallelObjectives`. Unpinned protocol attach with two executing (or otherwise equally ranked active) objectives returns 409 `ambiguous_active_objective`. Deliver of A while B is still active does not move the mission to review. Session-channel bind never falls back to a mission-wide unbound channel when an objective id is known. Default remains serial. Per-objective worktrees are out of scope.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `84`    | Cross-workspace inbox activity feed (coo:757). Adds the additive read-only `GET /api/activity-feed` REST surface, authorized with `mission:read` against every workspace the caller actively belongs to in the active organization — the same membership rule the My Missions aggregate uses. It returns a bounded, time-descending `ActivityFeedDto` merging three projections: launching/executing objectives (`ActivityFeedRunItemDto`, with the contiguous auto-advance queue behind each one), the newest deliveries (`ActivityFeedDeliveryItemDto`, embedding the same normalized `DeliveryDto` the mission delivery-read projection returns), and unseen blocking questions (`ActivityFeedQuestionItemDto`, from `mission_events.type = 'ask'` under the existing `blocking_question` unseen predicate). Instruction text and event summaries are truncated server-side and neither `deliveries.payload_json` nor `mission_events.payload_json` is exposed verbatim. `ActivityFeedItemKind` is an open vocabulary: clients ignore unknown kinds, so a future item kind is additive. Ordinary `objective`, `delivery`, `mission_event`, `execution_request`, `agent_session`, and `mission` change-feed rows invalidate the feed query; no new realtime transport is introduced.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `83`    | Objective-centric attach pinning (coo:756 Phase B). Protocol `attach` gains optional `--objective-id` (UUID or `{mission.display_id}.{display_key}`). Selection order is explicit `--objective-id`, else `--execution-request-id` / `OVERLORD_EXECUTION_REQUEST_ID`, else today's `resolveActiveObjective`. `--mission-id` remains required. When a pin is present, attach must not rediscover another objective. Hosted MCP `overlord_attach_session` accepts optional `objectiveId`. Launch exports `OVERLORD_OBJECTIVE_ID` (display id), context/copied attach argv include `--objective-id`, and Latch `display.name`/`title` use `{objectiveDisplayId} — {objective title}`. Session-key and native-session caches re-key to include objective id with a mission-only read fallback. Sibling 409 and serial execution are unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `82`    | Objective display ids (coo:756 Phase A). `objectives.display_key` is a required 4-character Crockford Base32 key, unique among live rows of a mission. Public `displayId` is computed at read time as `{missions.display_id}.{display_key}` (e.g. `coo:756.k7xm`) and is not stored as a second column. `ObjectiveDto` and protocol `ObjectiveSummary` expose additive `displayKey` and `displayId`. `resolveObjectiveRef` accepts a UUID or full display id and rejects a bare mission id or a wrong separator (`:`, `\|`, `-`). `/api/objectives/:id` accepts either form. Launch, attach pinning, Latch titles, and sibling-lock behavior are unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `81`    | Pruned Latch sessions leave the mission panel (coo:755). Latch's "no session" absence (metadata reclaimed by prune/remove) is a distinct local-target failure `LATCH_SESSION_ABSENT`, not a reachability error and not a new inspect state — inspect still reports only `running` / `exited` / `stopping` / `lost` when the session exists. The mission panel hides that card immediately instead of showing the raw Latch error. `POST /api/missions/:id/terminal-sessions/forget` then drops the matching `execution_requests.metadata_json.providerSession` mapping so `MissionDetailDto.terminalSessions` no longer projects it. The execution-request row, protocol session, observation history, and mission events stay; Overlord does not resurrect a "this used to exist" card. Other inspect/open/events failures remain the existing client-side reachability condition over last-known state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `80`    | Latch session-lifecycle executable resolution and a single mission-panel Latch card (coo:750). Every Latch child process Overlord spawns — `inspect`, `open`, `stop`, `events`, `send` — resolves the configured executable through the same resolver discovery uses (PATH via `which`, then `~/.local/bin` and the macOS Homebrew prefixes) instead of spawning the bare `latch` name, and reports a named missing-binary condition rather than a raw `spawnSync latch ENOENT` when nothing resolves. A Finder/launchd launched process (the packaged desktop app, an installed runner service) inherits no interactive-shell PATH, so discovery reporting `found` while every lifecycle command failed was the observable bug. Resolutions are memoized per executable name and revalidated by existence check. Separately, `MissionDetailDto.terminalSessions` is presented as **one** card — the newest session for the mission's active objective, falling back to the newest session overall — with the remaining sessions collapsed into an accordion inside that card whose trigger counts the other Latch connections currently running. Collapsed sessions keep polling inspect and keep ingesting harness events, so display consolidation never narrows what Overlord observes. Presentation only: no DTO, REST, capability, or CLI surface changes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `79`    | Runner-service local fallback and service environment simplification (coo:726). Postgres runners still reconnect the queue long-poll immediately; SQLite now uses one jittered 5-second fallback interval instead of adapting between 3 and 5 seconds from recent launches or Desktop focus. The private `~/.ovld/runner-focus.json` convention and Desktop focus-event writer are removed; `~/.ovld/runner-service.json.currentPollIntervalMs` remains the diagnostic projection of the fallback interval. This changes only the Local/SQLite pickup latency by at most two seconds and removes no CLI, REST, database, or Desktop bridge surface. Separately, persistent service definitions capture a remote backend URL but deliberately omit a loopback URL so each supervisor process follows the current `overlord.toml` value after Desktop selects its Local port; other captured environment fields and service lifecycle ownership are unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `78`    | Runner service survives its own replacement, and a stalled `launching` request expires (coo:726). The persistent supervisor (`ovld runner supervise`) records the identity of its own program and entry script at startup, re-checks it on every poll, and exits `0` when either changes or disappears, so the OS service manager (`launchd` `KeepAlive` / `systemd` `Restart`) respawns it from the newly installed build. Exit `0` is deliberate — a non-zero exit reads as a crash. The check is cheap and non-fatal: any error other than "gone" must never terminate a healthy supervisor, and the exec identity lives in process memory, so `~/.ovld/runner-service.json` gains no field. The Desktop Shell does **not** orchestrate runner-service lifecycle around an app update — section 5 already forbids the shell from reimplementing service management, and this self-restart is what makes such orchestration unnecessary; it is also transport-agnostic, covering desktop auto-update, a manual bundle replace, and a package-manager CLI upgrade identically. Separately, the Queue Surface's expiry vocabulary is extended: a request left in `launching` whose runner never reported `launched` within the launch TTL may move to `expired`, alongside the existing stale-claim and launched-without-attach arms. `execution_requests.launch_started_at` already exists and is already written at `launching`, so there is no schema change. `POST /api/runner/requests/:id/launched` must tolerate a conflict on an already-expired request for the very-slow-launch case. Requests are never re-queued by expiry — launch-at-most-once is preserved by expiring plus the runner's reported failure.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `77`    | Window-or-tab preference delegated to Latch (coo:721). The resolved launch session's viewer additively carries `openAs` (`window` \| `tab`), projected from the stored `TerminalProfile.placement` rather than stored a second time — `chord` has no delegated equivalent and resolves to `window`. It is written into the versioned `terminal_profile_json` viewer block as `{"kind": "iterm", "openAs": "tab"}`, frozen onto the claim-time `launchSession` snapshot, and additively projected as `TerminalSessionDto.viewerOpenAs`. Both Latch open paths (runner viewer open, mission-panel re-open) send it explicitly as `latch open --with iterm --as window\|tab`, never relying on Latch's own `open.behavior` default, because two independent defaults that disagree leave the user unable to tell which one won. The flag is gated on the `productVersion` from `latch capabilities --json` (minimum `0.2608140931.0`): clap rejects unknown arguments, so an older CLI is sent the pre-existing argv and opens a window. A snapshot frozen before this field reads as `window`, which is what those runs did.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `76`    | Latch engine Overlord migration (coo:707). Observation and harness permission/question presentation come from the public Latch CLI (`latch events --json --from`, capability-gated `latch send --resolve`); Overlord does not re-parse transcripts. Channel 1 (protocol attach/update/deliver, artifacts, shared state, follow-up capture, touched-file attribution) has no Latch dependency and must work from a bare terminal. Binding is the agent's `ovld protocol attach` declaration; Latch turns without attach are surfaced as unattached presentation on `MissionDetailDto.terminalSessions[].observation`. Session-input-control UI and new Agent Session Exchange observe/decide/inject state are decommissioned — historical ASE tables remain, human resolve/enqueue/release return no active controls, and launch no longer mints a session channel. Delivery/artifact/`ovld protocol ask` notifications stay on Overlord; harness `awaiting_input` notifications are ingested from Latch and neither waits on the other. Launch create/open/inspect/stop is unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `75`    | Objective auto-advance on agent create/edit surfaces (coo:711). Protocol `create`, `prompt`, and `add-objectives` persist per-objective `autoAdvance` from `--objectives-json`/`--objectives-file` and honor additive `--auto-advance` / `--no-auto-advance` as the default when an item omits the field (omitted still defaults to off). Adds additive `ovld protocol update-objective` (`--objective-id` required plus `--auto-advance` or `--no-auto-advance`) so agents can toggle the flag on an existing objective. Hosted MCP `overlord_create_mission` and `overlord_add_objectives` accept the same boolean (mirrored by connector shims), and `overlord_update_objective` forwards to Protocol `update-objective`. REST `PATCH /api/objectives/:id` already supported `autoAdvance`; this change does not alter that path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `74`    | Multi-project checkout link metadata (coo:709). `.overlord/project.json` may record every project a `read_write` checkout is linked to via an additive `projects` array (each entry carries `projectId`, optional `projectName`, `resourceId`, optional `resourceKey` / `resourceIdsByExecutionTarget`, `isPrimary`, and `linkedAt`). Top-level `projectId` / `resourceId` / `resourceKey` / `resourceIdsByExecutionTarget` / `isPrimary` remain required as the **primary-project projection** so v1/v2 readers keep working. At most one `projects[]` entry has `isPrimary: true` — whichever project most recently set this resource as its primary; a non-primary link never demotes an existing primary. Readers accept a legacy single-project file as a one-element `projects` list. Protocol `discover-project` (and client-side discovery for a remote backend) resolves to that primary project for mission association; the additive `linkedProjects` array lists every linked project. Cwd fallback matches the requested project among all links, not only the primary. Agent guidance uses the `isPrimary: true` project (else the top-level / sole entry) when associating missions, and passes an explicit project id to target a different linked project.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `73`    | Latch terminal-session lifecycle on the mission panel (coo:702). `MissionDetailDto.terminalSessions` additively projects every execution-request `metadata_json.providerSession` mapping for the mission, including provider/session ids, execution request/objective/target ids, target/device label, stored session name, claim-time executable/viewer, creation time, and the last observed `running` / `exited` / `stopping` / `lost` state. Session state is independent of mission and agent-session state. The local-target boundary gains `inspectLatchSession`, `openLatchSession`, and `stopLatchSession`: each runs the public Latch CLI on the mapped execution target with argv (never a shell), and inspect/open failures are represented as a separate client-side reachability condition layered over the retained last-known state. The mission panel polls inspect while reachable and offers distinct `Open in <viewer>`, `Copy attach command`, and explicitly destructive `End session` actions. The copied command is presented as a developer path requiring the user's own SSH when the device is remote, not as a shipped remote transport. Delivering or completing an objective never invokes stop; no automatic-stop policy exists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `72`    | Latch settings surface on the execution target (coo:702). Terminal & IDE settings gain one `Session` choice — `Direct — the window runs the agent` vs `Persistent — Latch hosts the agent, the window just shows it` — written as the tri-state `TerminalProfileDto.executionProvider` (omitted keeps, `null` inherits the user default, a value overrides) and read back with its origin from `resolvedLaunchSession`. The existing terminal picker keeps its list and stored `launcher` value and only changes verb (`Open in` / launches when direct, `Show it in` / shows plus the closing-the-window note when persistent); a `Don't open a window automatically` entry appears only under the persistent provider and writes `openViewerOnLaunch = false` without touching `launcher`, so toggling the provider stays lossless. Latch is never added to the terminal list. Discovery is rendered inline from a new read-only local-target capability `discoverLatch` (`DiscoverLatchInput` / `DiscoverLatchResult`, dispatched over the desktop bridge and the dev in-process proxy) so the probe runs on the device that would host the process, never in the browser: version and resolved path when found, the standalone install command when missing, the missing capability when incompatible, and an explicit unavailable state when no local transport can answer. `Persistent` is offered only on a definite `found`; direct execution stays selectable in every state. The user-level default is editable through `PATCH /api/(workspaces/:id/)launch-settings/session-defaults` alongside the per-target override. Pre-command and pre-launch fields are unchanged and never rewritten; a typed `latch` invocation there raises a warning pointing at the Session toggle.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `71`    | Latch nesting avoidance for the runner (coo:702). When the claim-time `launchSession` selects the Latch provider and the runner's launch environment contains a non-empty `LATCH_SESSION_ID`, the runner runs the same composed terminal command string S inline in the existing PTY through `$SHELL -ilc`, rather than call `latch create` or open a viewer. It records that opaque session id as `execution_requests.metadata_json.providerSession` with `lastObservedState = running`; the marker is correlation only and confers no mission, session-channel, or protocol authority. Direct launches remain unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `70`    | Latch create-then-open execution provider in the runner (coo:702). When the claim-time `launchSession` snapshot resolves to `executionProvider.kind = latch` and Latch discovery on the execution target reports `found`, the runner replaces only the final terminal/process spawn: it composes the same terminal inner command string S used today (cd, env exports, pre-launch commands, pre-command wrapper, agent argv — command resolution unchanged), sends `latch create --manifest-file - --json` with a Latch format-version-1 manifest whose `launch.argv` is `[ $SHELL, "-ilc", S ]` (interactive login shell so quoting/`&&`/multi-line pre-launch and `.zshrc` PATH setup match iTerm/Terminal), `launch.cwd` and `launch.env` mirror the resolved launch environment, and `display.source` is `{ kind: "overlord", external_run_id: <execution request id> }` — never Latch credentials, socket tokens, or attachment grants. Spawn success is the durable launch boundary. The returned Latch session id is recorded as `execution_requests.metadata_json.providerSession` (`provider`, `providerSessionId`, `executionTargetId`, `agentSessionId` nullable, `createdAt`, `lastObservedState`) via an additive optional body on `POST /api/runner/requests/:id/launched`; that id is never written to `launched_session_id` or the harness-native resume id. `latch open <id> --with <viewer>` is a separate best-effort step gated by `viewer.openOnLaunch`; viewer failure yields a warning plus an attach action and must not fail the execution request or terminate the agent. When the provider is `direct` or Latch is unavailable/incompatible, direct launch continues unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `69`    | Latch capability discovery on the execution target (coo:702). Run read-only `latch capabilities --json` on the device where the process will run, parse `protocolVersion` / `productVersion` / the capabilities map, expose three distinguishable states (`found` with version and resolved path, `not_installed` with the standalone install command, `incompatible` naming the missing capability), cache per target with cheap mtime/`which` revalidation, and never install or upgrade Latch as a side effect. Direct execution remains selectable in every state. Contract DTOs: `LatchDiscoveryDto`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `68`    | Execution provider and viewer are separate launch settings (coo:702). `user_execution_target_preferences.terminal_profile_json` gains an additive versioned block — `version`, `executionProvider` (`{ kind: 'direct' \| 'latch', executable }`), and `viewer` (`{ kind, openOnLaunch }`) — written **only** when a target actually opts in, so a row that predates the field is byte-identical and keeps launching directly with its current terminal choice. `launcher` remains the single stored terminal choice and is the sole authority for the viewer; `viewer.kind` is a derived projection consulted only when a document carries no `launcher`, which is what makes toggling the provider on and off lossless. A user-level default lives in `profiles.metadata_json.launchSessionDefaults` and is written through the additive `PATCH /api/(workspaces/:id/)launch-settings/session-defaults` (`launch:configure`); it applies to any target that has not set its own provider, and a per-target value always wins. `LaunchSettingsDto` gains `launchSessionDefaults` and `resolvedLaunchSession` (effective values plus the origin of each), and `TerminalProfileDto` gains tri-state `executionProvider` / `openViewerOnLaunch` (omitted keeps the stored override, `null` clears it back to inheriting). Claiming an execution request freezes the resolved provider and viewer onto `execution_requests.metadata_json.launchSession` — a best-effort write that never fails a claim, absent for requests claimed before this version — so a later settings change cannot retroactively change what a run did. No credential, socket token, or attachment grant is stored. Data contract only: no runner or UI behavior changes yet.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `67`    | Creation provenance on missions and objectives (coo:669). `missions` and `objectives` each gain `created_by_kind` (closed vocabulary `human` \| `agent` \| `automation`, `NOT NULL DEFAULT 'human'`), `created_by_agent` (open connector/agent identifier), and `created_by_session_id` — a deliberate **soft** reference to `agent_sessions.id` with no foreign key, so a dangling provenance pointer can never block a delete. `MissionDto` and `ObjectiveDto` expose additive `createdByKind` (non-optional; pre-provenance rows report `human`), `createdByAgent`, and `createdByWorkspaceUserId`; `MissionDetailDto` additionally exposes `createdFrom` (`sessionId`, `missionId`, `missionDisplayId`, `agentIdentifier`, or null), resolved by a `LEFT JOIN` paid once per mission page and never on board/list rows. Protocol `create` and `prompt` accept the additive optional flags `--agent`, `--assigned-to`, and `--session-key`, and `record-work` accepts `--assigned-to`; `--assigned-to` resolves a workspace member by `workspace_users.id`, profile UUID, `orgid:username`, bare username, or email, and an unresolvable member is rejected rather than silently unassigned. MCP `overlord_create_mission` accepts an additive `assignedTo` input with the same resolution rules. Every field is additive and existing clients are unaffected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `66`    | Batched objective bodies on the mission list (coo:666). `GET /api/projects/:id/missions` accepts an additive opt-in `includeObjectives` query flag; when set, each `MissionDto` carries an `objectives` array (every non-deleted objective, ordered by `position`) resolved by one batched query for the whole board instead of one `GET /api/missions/:id/objectives` per mission. The flag additionally requires `objective:read`. The default response shape is unchanged, so existing clients are unaffected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `65`    | Project resource-source launch defaults (coo:657). Each `project_resource_sources.descriptor_json` may carry per-agent `launchDefaults` containing a pre-command and normalized flags. Project settings exposes an additive `PATCH /api/projects/:id/resources/:resourceId/sources/:sourceId` route to replace those defaults while preserving the source materialization descriptor. Effective launch resolution is objective override first, then the source selected for the objective resource and execution target, followed by the existing user-target and workspace fallbacks. `ObjectiveDto.launchConfigOverrides` and additive `UpdateObjectiveBody.launchConfigAgent` / `launchConfigOverride` fields persist proactive objective edits under the any-target `*` key; explicit launch requests use an exact target key when known and otherwise the same wildcard. Queue snapshots and claim-time re-resolution use the same priority.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `64`    | APNs `aps.badge` is the recipient's unread durable-notification count (coo:637). `buildPushNotificationPresentation` counts `notifications` rows with `read_at IS NULL` and `deleted_at IS NULL` for the profile instead of missions in `status_type = 'review'`. The home-screen badge now matches the notification drawer / history; missions-in-review remains a separate queue metric.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `63`    | Live Activity update-token APNs routing is per registration (coo:656). `live_activity_push_tokens` gains required `environment` (`sandbox` \| `production`) and `bundle_id`, matching `live_activity_start_tokens` / `device_push_tokens`. `PUT /api/mobile/live-activities/:activityId/push-token` now requires `{ pushToken, environment, bundleId, startedByPush? }`. The Live Activity dispatcher sends update/end pushes on the registration's own `<bundleId>.push-type.liveactivity` topic and environment host instead of the process-wide `OVERLORD_APNS_ENV` / `OVERLORD_IOS_BUNDLE_ID` defaults, so a sandbox debug build is not retired by a production-host `BadDeviceToken`. Existing update-token rows without routing metadata are cleared by migration and must re-register.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `62`    | Live Activity APNs `content-state.updatedAt` is Unix epoch seconds (coo:656). ActivityKit's push JSONDecoder cannot decode an ISO-8601 string into a Swift `Date`, so remote update and push-to-start payloads previously returned APNs 200 while iOS silently discarded every content-state. The bounded presentation snapshot now encodes `updatedAt` as an integer (`Math.floor(now.getTime() / 1000)`); mobile ContentState must consume the same numeric epoch-seconds representation. No REST route or registration shape change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `61`    | Unified notification preferences (coo:637 P4). `notification_preferences` is addressed by `(profile_id, type, transport)` and records `alert`, `silent`, or `off`; catalog notification types and catalog-declared transports are validated strictly. The reserved master row is `(type: all, transport: all)` and suppresses notification transports, never Live Activity delivery. Existing category rows fan out to every catalog-eligible transport during migration. The profile preference REST route canonically reads/writes `{ enabled, preferences: { type, transport, mode }[] }`; an additive APNs-only `{ categories: { category, mode }[] }` compatibility projection and request alias keep older mobile builds usable. The webapp now reads and writes that server-owned preference surface, retaining only an additive local device mute for browser/desktop notification permission.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `60`    | Webapp notification rendering (coo:637 P3). User-notification policy is server-owned: the webapp consumes only the caller's durable notification history and authorized `notification` entity changes, rather than inferring candidates from mission/objective/event changes. Notification history projections add a bounded, server-composed `{ body }` field while fixed labels remain catalog-owned; neither is stored on the row. The feature-detected desktop bridge exposes `window.overlord.showNotification({ title, body, tag?, soundUrl? })`, implemented only by the validated `overlord:show-notification` IPC handler; it accepts no navigation route, credentials, token, or arbitrary Electron capability. The webapp remains an ordinary REST/SSE consumer and displays a recoverable notification drawer. The separate app-update and CLI-update banner module is not a user-notification transport.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `59`    | Durable server-owned mission notifications (coo:637 P2). Adds the private `notifications` table: a workspace-scoped, profile-addressed, soft-dismissible history row carrying only notification type, mission/objective references, read timestamp, revision, and lifecycle timestamps. Core `emitNotification` resolves the assignee as the sole recipient, writes that row, and enqueues `overlord.notification.dispatch.v1` inside the originating lifecycle transaction; unassigned missions notify nobody. The new dispatcher delivers APNs only through the existing ordinary device-token path and emits a `notification` entity-change row for realtime consumers; it never mixes standard device tokens with either ActivityKit token family, and APNs presentation remains recomputed from ids at delivery time. Authenticated `GET /api/notifications`, `PATCH /api/notifications/:id/read`, and `DELETE /api/notifications/:id` expose only the caller's own non-dismissed history; read and dismiss require the current revision and emit the matching entity change. New server emits cover agent questions, delivery awaiting review, launch failures, mission completion, agent start, and return-to-execute; `agent_started` remains silent by default.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `58`    | Remove the unused terminal mission-link feature (coo:502). The local-only `ovld protocol mission-link` subcommand, launch and attach banners, and connector Stop-hook footers are retired because they did not reliably surface a terminal link. The active-mission pointer remains an internal CLI correlation mechanism for agent-session binding and edit attribution. Desktop ownership of credential-free `overlord://missions/<missionId>` URLs remains unchanged because mobile push notifications use that route.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `57`    | Remove project.json pre-commit guard (coo:651). The checkout-local `writeProjectMetadata` capability continues to write read-write `.overlord/project.json` link metadata and idempotently maintain the checkout-root `AGENTS.md`/`CLAUDE.md` no-edit instruction block and narrow `.overlord/tmp/` + `.overlord/logs/` `.gitignore` entries, but no longer installs or updates a cooperative Git `pre-commit` guard. Intentional `project.json` commits are no longer blocked at link time; agent instruction files remain the primary protection.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `56`    | Mission field cleanup and human-only notes (coo:639). Removes `acceptance_criteria_text` and `available_tools_json` from `missions` and from the REST `MissionDto` / `UpdateMissionBody` shapes so agents can no longer persist hidden mission metadata through the API. Adds `notes_text` (`notes` in REST DTOs) for private human viewer notes that are editable in the mission panel but are never included in protocol attach/load context or agent prompt assembly. Scheduled mission duplicates copy `notes_text` forward.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `55`    | Desktop-initiated Live Activities via ActivityKit push-to-start (coo:633). Adds a third, non-interchangeable APNs credential family and the private core table `live_activity_start_tokens` (globally unique `start_token` bound to exactly one profile, carrying `platform`/`environment`/`bundle_id`/`activity_type`). New authenticated Mobile → REST routes `PUT /api/mobile/live-activities/start-token` (`{ startToken, environment, bundleId, activityType?, appVersion? }`) and `POST /api/mobile/live-activities/start-token/revoke` (`{ startToken }`) register/rotate and revoke the token; it travels in the body, never the URL, and has no read surface. Holding a row is itself the account's consent to desktop-initiated Live Activities, and revoking withdraws it. When an assigned mission enters execution the server enqueues a coalesced `overlord.live_activity.start.v1` worker job alongside the existing refresh job; the Live Activity dispatcher recomputes the same bounded snapshot at delivery time and sends an APNs `liveactivity` payload with `event: "start"`, `attributes-type`, `attributes`, `content-state`, a stale date, and a bounded alert, at priority 10 on the registration's own `<bundleId>.push-type.liveactivity` topic and environment host. A start is suppressed when the account already has any `live_activity_push_tokens` registration, when no mission is running, and for five minutes after a start was sent. `live_activity_push_tokens` gains `origin` (`local` \| `push_to_start`); `PUT /api/mobile/live-activities/:activityId/push-token` accepts the additive optional `startedByPush` boolean so the per-activity **update** token handed over after a remote start is registered and then owns every later update and end. Unassigned missions still notify nobody, existing retry/token-retirement rules are unchanged, and payloads still contain only the bounded presentation snapshot.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `54`    | Account-owned unassigned mission capture (coo:574). Adds `inbox_items`, a private profile-owned draft resource containing only title, a v1 one-item objective array, due date, priority, and timestamps; it carries no workspace/project/status/execution state. Authenticated `GET/POST /api/inbox`, `GET/PATCH/DELETE /api/inbox/:id`, and `POST /api/inbox/:id/promote` expose the lifecycle. Promotion authorizes `mission:create` on the selected destination project, creates the normal mission in that project's workspace, and consumes the item atomically. Inbox mutations do not emit workspace entity changes; clients refetch their own inbox, while promotion emits normal mission changes. `ovld protocol create --inbox` and `ovld inbox create` capture explicitly; ordinary non-executing `protocol create` falls back to inbox only when it cannot resolve a project, while `prompt` and `record-work` still require one. Hosted MCP makes `projectId` optional on `overlord_create_mission` (omission returns an explicit unassigned inbox item) and adds `overlord_create_inbox_item`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `53`    | Mission-panel agent-session action surface (coo:563, objective 4). The authenticated human request inbox `GET /api/agent-requests` additively accepts `?missionId=<id-or-display-id>`, which resolves the mission inside the caller's own memberships and authorizes `session:read` against the mission's own workspace before returning only that mission's requests. Adds the human `POST /api/agent-requests/:id/release` operation — the "respond in terminal" control — which releases an `open` request to the native prompt with `released_reason = 'policy'`, is authorized by `session:attach` in the request's own workspace, and is honest about losing the race: a request that was already resolved or released returns `released: false` plus the terminal state rather than an error. `GET /api/agent-session-inputs` additively projects `liveChannel` (`id`, `state`, `agentIdentifier`, `adapterKey`, `capabilities`, `lastHeartbeatAt`, `endedAt`, `endReason`) alongside the existing `liveChannelId`, so a client can gate its controls on the live channel's effective capability snapshot as this contract already requires; the projection exposes no credential, credential prefix, lease, or native session id. No table, vocabulary, or adapter route changes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `52`    | Agent-session conformance hardening and documentation audit (coo:562, objective 7). Restores the authoritative contract version after the Phase 3 machine-readable contract advanced without its narrative counterpart, and adds connector-owned decision codec declarations compiled into `cli/src/agent-session/decision-codec-registry.generated.ts`. Adds the executable `decision-codec` fixture kind: it runs the shipped pure request decoder and native decision encoder, asserts the bounded/redacted request card, and pins exact allow/deny/defer bytes. A `supported` callback/extension decision capability may not rely on a payload-shape fixture alone.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `51`    | Agent-session inject path (coo:562, phase 3). Adds `ovld agent-session inbox --agent <key> [--payload-file -]` as the third fixed rendered hook action (`event`/`request`/`inbox`). Claude registers `asyncRewake` inbox on SessionStart/PostToolUse (stderr + exit 2 → Delivered) and Stop-boundary `decision:block` (Delivered). Cursor stop prefers inbox `followup_message` and records `delivery_outcome=queued_turn_boundary` — never Delivered. OpenCode sidecar drains claims via `POST /session/{id}/prompt_async`. Pi extension claims via inbox, calls `sendUserMessage`, then confirms. Adds nullable `agent_session_inputs.delivery_outcome` (`delivered`/`queued_turn_boundary`/`queued_next_turn`/`unsupported`), human `GET\|POST /api/agent-session-inputs` and `POST /:id/cancel`, `ovld inputs list\|send`, and a mission-panel Session instructions section that renders honest delivery labels including "Queued (turn boundary)". An emitted input remains unretriable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `50`    | Mission shared-context REST and UI footer (coo:566). Adds authenticated `GET /api/missions/:id/context` (mission:read) returning `SharedContextEntryDto[]` from `shared_context_entries`, and authenticated `PUT /api/missions/:id/context` (mission:update) that upserts one entry by `{ key, value }` using the same durable memory model as Protocol `write-context`. Writes emit `entity_type=shared_context_entry` entity-change rows in the same transaction. The web/desktop mission panel shows a collapsed-by-default Shared State footer for viewing and editing these entries.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `49`    | Agent-session blocking decision path (coo:562, phase 2). Adds the `ovld agent-session request --agent <key> --payload-file -` callback runtime: it gates before I/O, creates one channel-scoped request, holds and renews its server-side waiter lease, and returns only the connector-owned exact native allow/deny decision when a human resolution wins; timeout, interruption, malformed input, auth failure, and backend loss emit no decision and return control to the harness. Adds authenticated human `GET /api/agent-requests` and `POST /api/agent-requests/:id/resolve` surfaces, authorized by `session:read` and `session:attach` respectively in the request's own workspace; resolution requires revision CAS and an identified human actor. The adapter channel route adds a scoped request reader so a waiter that loses its CAS can observe the one terminal state without addressing another channel. Default windows remain 30 seconds for active presence and 30 minutes for away/unknown, are capped below harness timeout, and project policy may set zero for immediate terminal release. OpenCode's standing `always` permission remains absent from the remotely resolvable vocabulary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `48`    | Agent-session push path on two integration shapes (coo:562, phase 1). Adds the pure normalization core (`packages/core/service/agent-session/pure/`): a closed seven-value normalized event vocabulary (`session.started`, `session.ended`, `prompt.submitted`, `tool.called`, `tool.completed`, `file.edited`, `notice`), a nine-value normalized tool vocabulary, and the §9.1 description formatters keyed by **normalized** tool name with a closed ten-value `riskMarkers` set. Normalized event payloads are an **allowlist expressed as a type** with no index signature, so there is no field a raw native payload fits into. Producer event ids are **derived from content**, which is what makes at-least-once delivery — a retried hook, a reconnecting sidecar, a flaky network — idempotent without adapter-side state. Adds the connector-owned **event codec declaration** `connectors/adapters/<agent>/codec/<agent>.codec.yaml`, referenced from the descriptor's new optional `codecSpec` field and compiled by `yarn connectors:capabilities` into `cli/src/agent-session/codec-registry.generated.ts`. A codec is a declaration of _where native values live_, never executable code: it cannot name a destination the core interpreter does not implement, and unknown keys are a generation error. Adds the `normalized-event` fixture kind to the `agentSession.fixtureKinds` vocabulary — it runs the shipped codec and interpreter against a recorded native payload, asserts the exact envelope, and asserts that named raw content appears nowhere in it, which is what allows an `observe.*` capability to be graded `supported`. Adds two adapter runtime subcommands: `ovld agent-session event --agent <key> --payload-file -` (callback and extension shapes; silent on every path, exits 0 on every path, never blocks) and `ovld agent-session sidecar --agent <key> --port <port>` (control-plane shape; binds loopback only, mints a per-launch control-port secret held in memory, and re-reads harness state before resubscribing after a dropped stream). The sidecar is harness-agnostic: a sixth control-plane harness supplies a codec and a descriptor and adds zero subcommands. The shared `connectors/core/scripts/agent-session-hook.sh` now applies the scope gate **in bash, before the `ovld` spawn**, so a session with no `OVERLORD_SESSION_CHANNEL_ID` costs no process, no log, and no delay. Claude registers the rendered `scripts/agent-session-event.sh` on `PostToolUse`, `UserPromptSubmit`, and `SessionStart` **additively**: the legacy touched-file and follow-up hooks stay installed and a fixture guards both registrations, because the migration must not repair observation by regressing change attribution. Adds the `opencode` connector as the first `controlPlane` adapter — descriptor, codec, and README only; its launch integration and agent-catalog entry are not in this phase. Claude and OpenCode both derive `capabilityTier` 1 (Observational) from passing fixtures.                                                                                                                                                                                                                 |
| `47`    | Agent Session Exchange channel bootstrap and runtime skeleton (coo:562, phase 0B). Migrates the four Agent Session Exchange tables reserved by contract 45 — `agent_session_channels`, `agent_session_events`, `agent_requests`, `agent_session_inputs` — in both dialects, and brings the `/api/agent-session-channels/v1/*` adapter route family live. A session channel is created at the `launching` transition, **before** the agent process exists, so harness output published before attach lands somewhere durable and correctly authorized; the `POST /api/runner/requests/:id/launching` response additively carries a one-time `sessionChannel` bootstrap (`channelId`, `token`, `expiresAt`, `launchKind`) and the launch environment gains `OVERLORD_SESSION_CHANNEL_ID` and `OVERLORD_SESSION_LAUNCH_KIND`. The **credential is never exported into the launch script**, which lives in `<checkout>/.overlord/tmp`: it is staged in an owner-only (`0600`) file outside every checkout, keyed by a hash of the channel id, and removed on channel end or an auth failure; `OVERLORD_SESSION_CHANNEL_TOKEN` remains a supported input for virtual gateways that realize the bootstrap directly into the environment. `ovld protocol attach` gains the optional `--session-channel-id` flag — the id only, filled automatically from the environment — which binds the channel to the new `agent_sessions.id` and backfills the session id onto every pre-attach event and request; the working directory participates in no part of this. The adapter router is mounted **ahead of** the `/api` human authentication guard so a browser session, a `USER_TOKEN`, and the loopback-trusted local-operator fallback are all structurally unable to reach it, and every handler operates on the channel resolved from the credential rather than one named in a request. The scoped credential is stored hash-only with a lease-renewed bounded expiry and an absolute lifetime ceiling, and is revoked in the same transaction that ends or loses a channel — which also releases every open request to the terminal and expires every queued input, so no card stays answerable after its session is gone. Waiter leases and the server-side release sweep land here rather than with the answerable-request UI, because the guarantee that a held decision is released cannot live in the process that gets killed. `ovld agent-session bind` correlates a native session id through a heartbeat; a native id remains an alias that can neither create a binding nor authorize anything. Adds the shared `connectors/core/scripts/agent-session-hook.sh`, rendered per adapter at `ovld agent-setup` time as `scripts/agent-session-<action>.sh` with both the adapter key and a **fixed action** substituted from a closed two-value set, so an untrusted native payload can never select which CLI operation runs. Existing `PostToolUse` touched-file capture is untouched and keeps its attribution; Cursor's `postToolUse` hook is repaired to create `~/.ovld/logs` before redirecting into it (the missing `mkdir` made bash fail the whole redirect on a first-run machine, so `record-touched` never ran and attribution silently recorded nothing). |
| `46`    | Mid-turn mission artifact creation (coo:564). Adds authenticated `POST /api/missions/:id/artifacts`, authorized with `artifact:create` on the named mission, accepting `{ type, label }` plus at least one of `{ contentText, externalUrl }` (optional `objectiveId` / `sessionId` provenance). Leaves `delivery_id` null so agents can publish plans, notes, URLs, and similar records during a turn without delivering; `deliver` may still attach additional artifacts later. Adds the additive `ovld protocol add-artifact` subcommand (`POST /api/protocol/add-artifact`) and the equivalent `overlord_add_artifact` hosted-MCP tool (mirrored by the codex/cursor/antigravity connector shims). Both call the same `createArtifact` service: required `--mission-id` / `missionId`, `--type` / `type`, `--label` / `label`, plus content via `--content-text`/`--content-text-file` / `contentText` and/or `--external-url` / `externalUrl`. Optional `--session-key` / `sessionKey` (auto-injected from the attach cache when present) stamps session/objective provenance. Non-HTTP(S) external URLs are rejected. MCP/CLI introduce no new artifact types or direct database writes. Existing `update-artifact` / `overlord_update_artifact` remains the revise path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `45`    | Agent-session capability truth and the Connector → CLI Agent Session Runtime → REST surface (coo:562, phase 0A). Adds `contract/harness-capabilities.schema.yaml` and a required per-adapter `connectors/adapters/<agent>/harness-capabilities.yaml` descriptor: the only hand-authored source of agent-session interaction capability truth, graded by the closed four-value status vocabulary (`supported`, `unsupported`, `not-implemented`, `unverified`) over a closed 17-value capability-id vocabulary that describes behavior rather than Claude hook names. `supported` requires an executable fixture that CI runs; `unsupported` requires a reason plus a stable `evidenceRef`; `not-implemented` and `unverified` require a tracker id; promoting a status out of `unsupported` must replace `evidenceRef` with fixtures in the same change. `connector.integrationShape` (`callback` \| `extension` \| `controlPlane`) is declared so the core gates decision-window policy on a capability rather than on an agent identifier, and `connector.capabilityTier` (0 Unsupported, 1 Observational, 2 Answerable, 3 Conversational) is **derived from passing fixtures and never authored or persisted**. The existing four hook-named capability flags and the closed four-value `hookTypes` enum are retained as a **generated, deprecated projection** of the descriptor for one release; hand-editing them is now a contract violation, and `permissionHook`/`PermissionRequest` may only be projected when a descriptor capability actually rides on that native event — which removes Cursor's false universal `PermissionRequest` claim, since Cursor maps that event to null. Every shipped connector (Claude, Codex, Cursor, Pi, Antigravity) ships a descriptor and a mandatory unbound-session negative fixture; Antigravity begins `unverified` throughout rather than inheriting capabilities by resemblance. Adds the **Connector → CLI Agent Session Runtime → REST** interaction surface: connector integrations hand native payloads to `ovld agent-session` subcommands, and only the CLI talks to the backend, over the versioned `/api/agent-session-channels/v1/*` adapter route family authenticated by a channel-scoped credential that can affect exactly one channel and can neither read mission context nor resolve a human decision. A native session id is a **correlation alias only**; the verified channel/session credential is the authorization and mission scope, and neither the working directory nor an unverified native id is ever a binding authority. Phase 0A ships only the offline, human-facing `ovld agent-session capabilities` reader plus `ovld doctor` descriptor-digest and native-session-binding reporting, both of which make no network call for an unbound session; the adapter runtime subcommands and the REST route family land with the channel bootstrap.                                                                                                                                                                                                                                                                                                                                           |
| `44`    | Execution-target type vocabulary cleanup (coo:528). Removes the unused core `ssh` value from documented `execution_targets.type` core values and tightens the shipped database CHECK to `local` and `virtual` only; any legacy `ssh` rows are rewritten to `virtual` during migration. Drops the unread `execution_requests.target_kind` column and its hardcoded writers. The `local`/`virtual` claimant-security boundary is unchanged; transport variety remains in the provider registry, and `execution_targets.type` stays an open extension vocabulary for adapter-defined values.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `42`    | Project-link metadata protection (coo:520). The existing checkout-local `writeProjectMetadata` capability used by `ovld create-project`/`ovld add-cwd` and the Desktop bridge continues to write only a read-write resource's `.overlord/project.json` link metadata. On each link it also idempotently maintains the checkout-root `AGENTS.md` and `CLAUDE.md` with a marked rule that agents must not edit that metadata, adds only `.overlord/tmp/` and `.overlord/logs/` to the checkout's `.gitignore` (never `project.json` or all of `.overlord/`), and installs a cooperative Git `pre-commit` guard that rejects staged `.overlord/project.json` changes. Existing instruction files, ignore rules, and hooks remain preserved; the guard is local checkout policy and does not alter Git transport or any REST/protocol interface.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `41`    | Execution-target user control (coo:522, phase 4). `GET /api/workspaces/:id/execution-targets` additively projects safe local runner-instance diagnostics (instance id, native/adopted relation, label/version, supported agents, health, heartbeat, and bounded error code) and an honest unavailable reason; it never exposes capabilities blobs, credentials, or runner addressing. `PATCH /api/workspaces/:id/execution-targets/:targetId` additively accepts `{ status: 'active'\|'disabled' }` alongside label, requires workspace update permission, and emits the normal target change; disabled targets are excluded from eligibility and runners reject claims for them, while queued work remains target-level and may be re-enabled later.`LaunchObjectiveBody.executionTargetId`is an optional explicit target override: the launch service validates the target in the objective workspace for active member access, active/reachable eligibility, and primary-resource connectivity before stamping `execution_requests.execution_target_id`. The override selects a target only; any healthy runner serving it may claim, and no runner-instance selection surface is introduced.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `40`    | Local runner instances, AgentPod adoption, and honest local liveness (coo:522, phase 3). Adds the core `execution_target_runner_registrations` table: a **one-to-many** record of the live runner processes serving one `local` execution target, carrying `runner_instance_id`, a `native`/`adopted` `relation`, non-secret `label`/`runner_version`/`capabilities_json`/`supported_agents_json` diagnostics, and `health`/`last_heartbeat_at`/`last_error_code`. The gateway-owned `execution_target_registrations` table is **unchanged** and remains exactly one registration per `virtual` target. Adds the nullable `execution_requests.claimed_by_runner_registration_id` FK so a local claim records which runner instance won it, alongside the existing `claimed_by_execution_target_id` (target) and `claimed_by_gateway_instance_id` (virtual). `POST /api/runner/claim` additively accepts `executionTargetId`, `runnerInstanceId`, `runnerRelation`, `runnerLabel`, `runnerVersion`, `capabilities`, and `supportedAgents`: an explicit `executionTargetId` is **resolved and authorized** (existing, non-deleted, type `local`, `status = 'active'`, active access for the authenticated member) and never created, and a claim with a `runnerInstanceId` upserts **only** the runner-instance row. Runner claiming stays **target-level** — any healthy runner registered to the selected target may claim it; runner instances are never individually addressable. Local target reachability is now derived from a recent healthy/degraded runner registration rather than `devices.last_seen_at`; a target that has never had a runner registration keeps the legacy `devices.last_seen_at` behavior so existing installs stay reachable until their upgraded runner publishes its first heartbeat. Virtual liveness continues to read `execution_target_registrations` unchanged. The CLI sends a stable runner-instance identity (`OVERLORD_RUNNER_INSTANCE_ID`, otherwise a persisted local id) and honors `OVERLORD_EXECUTION_TARGET_ID` / `OVERLORD_RUNNER_RELATION`, which is how an AgentPod container adopts its host's execution target: it supplies the host target id plus its own distinct runner identity and `adopted` relation, and never falls back to declaring a target.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `39`    | Intentional execution-target onboarding (coo:522, phase 2). Creating a `local` `execution_targets` row is now reserved for exactly two acts a human performs during setup: `POST /api/protocol/register-target` (`ovld add-et`, and the new additive `POST /api/workspaces/:id/execution-targets` one-click desktop equivalent), and an **eligible machine-local checkout link** — a `local_checkout` project-resource source written by a caller that carries a real machine-local host hint. The per-machine launch-settings mutations (`PATCH /api/workspaces/:id/launch-settings/agents/:agentKey` and `/terminal-profile`) **no longer declare**; they now resolve the acting machine's already-declared target and fail with `409 no_execution_target_registered` when it has none, so a settings write is never an onboarding trigger. Creating a `local_checkout` source without an explicit `executionTargetId` and without a machine-local hint fails with `409 no_execution_target_registered` instead of silently creating a target or a pathless global source; an explicit `executionTargetId` (the browser editing a source for an already-selected target) and a Git/URL `sourceUrl` source (project-global) create nothing. `ovld auth login`, `ovld setup`, and `ovld runner install`/`start`/`supervise` create no execution target: setup declares only through the explicit `register-target` call it now makes on the user's behalf, and the runner reports the missing declaration. CLI, desktop, and webapp no-target states become actionable rather than blank. No schema change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `38`    | Execution targets are declared, never inferred (coo:522, phase 1). An `execution_targets` row of type `local` is now created **only** by an explicit declaration — `POST /api/protocol/register-target` (`ovld add-et`) or a machine-local project-resource checkout link — plus the per-machine launch-preference mutations that persist settings for the acting machine. No read, project-discovery, protocol-lifecycle, observation-attribution, or runner path may create one. The `x-overlord-device-*` request headers are demoted to an **optional machine-local lookup hint**: they identify which already-declared target a co-located CLI/desktop caller means, are never authentication, authorization, or audit identity, and never authorize provisioning. Ordinary browsers and the mobile app stop sending them entirely (the webapp's `localStorage` pseudo-fingerprint is removed); only the desktop shell bridge and the CLI send a real machine fingerprint. `POST /api/runner/claim` now resolves an existing non-deleted `local` target for the acting machine and fails with `409 no_execution_target_registered` when none exists instead of provisioning one. `LaunchSettingsDto.executionTargetId` and `.deviceLabel` become nullable (`null` = "this caller has no declared execution target"); reading launch settings no longer provisions anything, while the launch-config/terminal-profile mutations still declare the acting machine because they persist that machine's own preferences. Protocol session/resource attribution, project discovery, and target-resource/mission-branch observation attribution all tolerate a null acting target; observation reports for a target the caller is not acting as still fail `403 execution_target_mismatch`. The v34 orphan-device sweep and the browser/backend-host provisioning guards are retained as legacy cleanup, not as the model. No schema change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `37`    | Agent artifact update across protocol, MCP, and CLI (coo:518). Adds the additive `ovld protocol update-artifact` subcommand (`POST /api/protocol/update-artifact`) and the equivalent `overlord_update_artifact` hosted-MCP tool (mirrored by the codex/cursor/antigravity connector shims). Both call the existing REST-backed `updateArtifact` service used by `PATCH /api/missions/:id/artifacts/:artifactId`: required `--mission-id` / `missionId`, `--artifact-id` / `artifactId`, and `--expected-revision` / `expectedRevision`, plus any non-empty subset of `--label` / `label`, `--content-text` or `--content-text-file` / `contentText`, and `--external-url` / `externalUrl`. No session key is required so a later objective or follow-up can revise an artifact created earlier. Authorization remains `mission:update`; provenance and `contentJson` stay immutable; stale revisions return `409`. MCP/CLI introduce no new artifact types or direct database writes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `36`    | Hosted MCP delivery artifacts (coo:503). The additive `artifacts` input on `overlord_deliver_session` accepts the existing delivery-artifact shape (`type`, `label`, optional text `content`, optional HTTP(S) `url`) and forwards it unchanged to the existing Protocol Layer `deliver` operation. It is authorized, validated, persisted, and projected exactly as a CLI delivery artifact; MCP adds no direct database access, artifact type, or lifecycle transition. This permits a hosted scheduled review to publish its full report on a short publish objective while leaving the separately-created remediation objectives as drafts for later execution.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `35`    | Desktop mission deep links (coo:502). The Desktop Shell owns the additive `overlord://missions/<missionId>` custom URL, where `missionId` is a UUID or workspace display id matching `[A-Za-z0-9:_-]{1,64}`. The main process rejects all other mission URL shapes, focuses or creates the shell window, and sends the shell-local `overlord:navigate` preload notification with the fixed `/user/missions/<missionId>` route; the feature-detected SPA bridge navigates only to that supplied internal route. Mission deep links carry no credential, ticket, or payload. Existing `overlord://auth/callback` OAuth handling remains restricted to its opaque, single-use ticket and is security-separated from mission navigation. The CLI additionally owns the local-only `ovld protocol mission-link` formatter: it reads only the cwd active-mission pointer or launch environment and prints the same credential-free URL without a backend request. Connectors that already declare the approved `Stop` hook may invoke this formatter as a non-blocking terminal footer; no new hook type or capability is introduced.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `34`    | Orphan device cleanup (coo:456). A `devices` row is meaningful only while at least one non-deleted `execution_targets` row references it. Soft-deleting a workspace execution target also soft-deletes its linked device when that was the device's last live target. Workspace target projection and local-target provisioning opportunistically soft-delete any remaining devices that have no live execution target. Re-registering the same fingerprint revives the soft-deleted device (and recreates its local target) rather than inserting a conflicting row under the unique `(workspace_id, fingerprint)` index.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `33`    | Shared GitHub OAuth App registration for login and repository authorization (coo:448 follow-up). The repository-authorization flow now derives its client id and secret from the same `GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET` configuration as Better Auth GitHub sign-in. The grants remain security-separated: repository authorization is an explicit second flow requesting `repo read:org`, uses its own profile-bound single-use state, persists its own AES-256-GCM-encrypted token, and never exposes that token to Better Auth, clients, DTOs, logs, realtime, or change projections. Its callback is `/api/auth/callback/github/repository`, a subpath of the OAuth App's registered `/api/auth/callback/github` URL, and is handled before the Better Auth wildcard route. The workspace GitHub App installation and credentials remain entirely separate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `32`    | Composite mobile project initialization (coo:448). Adds authenticated `POST /api/projects/initialize`, explicitly authorized with `project:create` in the supplied workspace. The request supplies a caller-generated idempotency key, trimmed project name and description, plus optional `{ createGitHubRepository: true, githubOwnerLogin }`; it atomically creates the project and first draft mission/objective before any external call. GitHub provisioning is then retryable under the same key through the connected user OAuth credential, always creates a private empty repository at the selected eligible personal/organization owner, and never uses or alters the workspace GitHub App installation. A provisioning failure preserves and returns the project/mission with typed retryable status; success persists exactly one extension GitHub project link and exactly one non-primary read-only `git` project resource with the HTTPS clone URL. Extension-private `ext_github_project_initializations` records the request key, profile/workspace ownership, internal IDs, selected owner, provisioning state, and non-secret repository metadata; it contains no OAuth credentials and deduplicates retries.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `31`    | User-scoped GitHub repository-creation authorization (coo:448). Extends the existing `/ext/github` REST extension with authenticated `GET /user-connection`, `POST /user-connection/authorize`, `DELETE /user-connection`, and `GET /repository-owners`, plus a browser OAuth callback whose opaque, single-use state is server-persisted and profile-bound. The connection is distinct from Better Auth GitHub sign-in and the workspace GitHub App installation: it requests `repo read:org`, stores access and optional refresh credentials only as AES-256-GCM ciphertext under a server-held key, and never returns credentials or OAuth state through a DTO, mobile storage, logs, realtime, or change projections. Connection DTOs expose only configuration/connection/account metadata. Repository owners contain the authenticated user's personal account plus only active organizations whose GitHub organization policy permits that user to create private repositories; owner enumeration grants no Overlord workspace authority. Two extension-private tables, `ext_github_user_connections` (one active row per profile) and `ext_github_user_oauth_states` (hashed short-lived state, consumed once), retain account metadata and encrypted credentials without changing the existing one-installation-per-workspace GitHub App model.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `30`    | Editable mission artifacts (coo:449). Adds authenticated `PATCH /api/missions/:id/artifacts/:artifactId`, authorized with the existing `mission:update` permission on the artifact's mission. The route accepts `expectedRevision` plus any non-empty subset of `{ label, contentText, externalUrl }`, preserves immutable delivery/session/objective provenance and `contentJson`, rejects a stale revision with `409`, non-HTTP(S) external URLs, or a resulting artifact with no text, JSON, or URL content, increments the artifact revision, and emits an `artifact` entity-change projection in the same transaction. The web client renders `contentText` as safe Markdown and uses this route for in-place label, Markdown, and URL edits.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `29`    | Standard mobile push notifications (coo:444). Adds a standard alert/background push surface that is strictly separate from the coo:439 Live Activity surface while reusing the same durable dispatch machinery. New authenticated Mobile → REST routes `PUT /api/mobile/push/device-token` (`{ deviceToken, environment, bundleId, appVersion? }`) and `POST /api/mobile/push/device-token/revoke` (`{ deviceToken }`) register/rotate and revoke an opaque APNs **device** token; the token is carried in the body, never the URL, and has no read surface. New authenticated profile routes `GET`/`PUT /api/profile/notification-preferences` expose `{ enabled, categories: { category, mode }[] }` where `mode` is `alert` \| `silent` \| `off`. Two new private core tables: `device_push_tokens` (unique per `device_token`, bound to one profile, carrying `environment`/`bundle_id` so sandbox and production installs coexist) and `notification_preferences` (unique per `(profile_id, category)`). Categories are lifecycle-only: `mission_awaiting_review`, `agent_question`, `mission_complete`, `mission_failed`. The same lifecycle mutations that refresh Live Activities also enqueue deduplicated `overlord.push_notification.dispatch.v1` worker jobs carrying only ids; the new in-process dispatcher recomputes the presentation snapshot at delivery time and sends APNs `alert` (priority 10, one-hour expiration) or `background` (priority 5) pushes on the plain `<bundleId>` topic, collapsed per `(missionId, category)`. Invalid device tokens (`410`/`400 BadDeviceToken`) are retired. Payloads contain only bounded project name, sanitized mission title, display id, badge count, mission id, category, and deep link — never bearer tokens, ActivityKit tokens, objective instructions, agent prompts, delivery summaries, question text, file paths, or event payloads. ActivityKit tokens and device tokens are never interchangeable: separate tables, routes, topics, and push types.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `28`    | Mobile Live Activity push handoff (coo:439). Adds the `mobile` REST-consumer component and its authenticated Mobile → REST surface: `PUT /api/mobile/live-activities/:activityId/push-token` registers or rotates an opaque ActivityKit token for the caller's account-level activity, and `DELETE /api/mobile/live-activities/:activityId/push-token` revokes it. The core `live_activity_push_tokens` table stores registrations privately and is never exposed by a read endpoint. Mission/objective execution and completion changes enqueue coalesced `overlord.live_activity.dispatch.v1` jobs; the backend recomputes the same bounded account snapshot (two executing missions and one recent completion) and sends APNs `liveactivity` update/end payloads. APNs delivery is durable/retried, transition updates are immediate, visible-content-equal progress is suppressed for five minutes, and invalid APNs tokens are retired. Payloads contain only the presentation snapshot — never authentication tokens, objective instructions, prompts, or event detail.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `27`    | Individual project-resource source removal (coo:425). Adds the additive `DELETE /api/projects/:id/resources/:resourceId/sources/:sourceId` REST route, authorized with the existing project-update permission. It soft-deletes only the named active `project_resource_sources` descriptor scoped to the named project/resource, leaving the logical `project_resources` identity intact so it can be reconnected to another source. The resource revision advances and emits the existing `project_resource` entity-change projection.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `26`    | Standalone execution-target registration (coo:422). Adds the additive, "parentless" `register-target` protocol subcommand (`POST /api/protocol/register-target`; optional `--name` display label and `--workspace-id` [id, slug, or name]), surfaced as the top-level `ovld add-et --name <name>` CLI command, so a machine can announce itself as an execution target up front rather than only as a side effect of launching, claiming, or linking a resource. The backend provisions (or reuses, idempotently) the acting device's `local` execution target for the resolved workspace via the existing `ensureActingDeviceTarget` path — the CLI's `x-overlord-device-*` headers identify the machine — and applies `--name` as the target label (create or rename). Like `create-project` it resolves the workspace from the caller's active memberships: one membership registers there; an explicit `--workspace-id` naming a membership registers there; multiple memberships with none named return a non-error `{ status: 'workspace_selection_required', workspaces: [...] }`. Per-workspace `execution_request:claim` RBAC is enforced against the resolved target (already in the `mission_lifecycle` token scope, so runner/agent tokens can self-register); no scope change. No schema change — it reuses the existing `devices`/`execution_targets` tables.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `25`    | Record completed agent work as a review mission (coo:418). Adds the additive `overlord_record_work` hosted-MCP tool (mirrored by the codex/cursor/antigravity connector shims), alongside the existing `ovld protocol record-work` subcommand and `POST /api/protocol/record-work` route, so an agent can log work already finished in chat as one completed mission with no attach/deliver cycle. `record-work` now accepts its whole submission as a single `--payload-json`/`--payload-file` envelope (`{ objective, summary, title, changeRationales, changedFiles, artifacts }`) — explicit flags still win over envelope fields — and additionally accepts `--changed-files-json`/`--changed-files-file`. `recordWork` now records a `changed_files` row for every change rationale's file path (surfaced `covered` in review) plus any explicit changed files (surfaced `missing_rationale`), so the review file panel and rationale coverage match a normal `deliver`; these rows carry a NULL `session_id`/`resource_id` since record-work has no session. Delivery still enqueues the `overlord.delivery.compose.v1` compose job so the Gemini delivery summary runs identically to a normal delivery. `mission:*` in the `mission_lifecycle` scope already authorizes the tool; no scope change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `24`    | Agent project creation over protocol and hosted MCP (coo:405). Adds the additive `ovld protocol create-project` subcommand (`--name` required; optional `--workspace-id` [id, slug, or name], `--description`, `--slug`) and the equivalent `overlord_create_project` hosted-MCP tool (mirrored by the codex/cursor/antigravity connector shims). Both resolve the target workspace from the caller's active memberships: exactly one membership creates there; an explicit `--workspace-id`/`workspaceId` naming a membership creates there; when the caller has multiple memberships and none is named, the call returns a non-error `{ status: 'workspace_selection_required', workspaces: [...] }` result (rather than defaulting) so the agent asks the user to choose. Per-workspace `project:create` RBAC is enforced against the resolved target. The `mission_lifecycle` token scope preset now grants `project:create` (in addition to `project:read`) so hosted-MCP and runner tokens can create a project to work in; `project:update`/`project:delete` and other administration remain excluded.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `23`    | Runner claim long-polling (coo:414). `POST /api/runner/claim` remains the same authorized atomic claim surface and response shape, but on the Postgres adapter it now holds an otherwise-empty claim request for at most 25 seconds, using a dedicated LISTEN connection and an `execution_requests` queue notification to retry promptly. The response additively carries `longPoll: true` when that capability is available. The PostgreSQL trigger only signals queue insert/requeue candidates; services still perform authorization and atomic compare-and-set claims, so notification payloads grant no access and missed/competing notifications resolve to the normal empty response. SQLite preserves immediate claim behavior and returns `longPoll: false`, allowing local runners to use their existing fallback cadence. The CLI reconnects immediately after a Postgres long-poll timeout or empty wake and never busy-polls an idle hosted runner.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `22`    | Project list lifecycle filtering (coo:382). `GET /api/projects` and `GET /api/workspaces/:id/projects` now return active projects by default. Both accept the additive `lifecycle` query parameter: `active` (default), `archived`, or `all`. The parameter is authorization-neutral: the existing project-read membership check still scopes every result. Clients must request `archived` for archive-management views and `all` only when an interaction needs both lifecycle states (for example, full sidebar ordering); ordinary project selectors receive only active projects through the default query.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `21`    | Workspace transport retirement (coo:379). Removes `X-Overlord-Active-Workspace`, the `overlord_active_workspace` cookie, activation-derived request state, unscoped workspace-settings routes, `WorkspaceDto.isActive`, and `/api/meta.workspace` as authorization or request-scoping interfaces. Auth resolves an authenticated profile/credential only; named resources derive their workspace and membership, workspace-owned settings use `/api/workspaces/:id/...`, and account-wide lists/search/realtime aggregate the caller's authorized memberships server-side. Realtime/catch-up filters before projection and exposes additive `EntityChangeDto.workspaceId` while preserving its global monotonic cursor. Adds authenticated default-project preference routes (`GET\|PUT\|DELETE /api/profile/default-project`) backed by the account-wide `project_user_preferences.preferences_json.overlord.defaultProject` marker; the preference grants no access and is ignored if inaccessible. Parentless creates require an explicit workspace when multiple memberships exist.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `20`    | Agent launch flags as positional name/value pairs (coo:381). `AgentLaunchConfigDto.flags` and `UpdateAgentLaunchConfigBody.flags` are now `AgentLaunchFlagDto[]` (`{ name, value? }`) instead of `string[]`. Launch pipelines flatten each entry to separate argv tokens (`--permission-mode`, `auto`). Legacy stored `string[]` values (`--verbose`, `--permission-mode auto`, `--permission-mode=auto`) normalize on read via `normalizeAgentLaunchFlags` in `@overlord/contract`. CLI `ovld launch --flag` passthrough parses the same text forms.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `19`    | Reference (read) project resources (coo:368). Adds `project_resources.access_mode` (`'read' \| 'read_write'`, default `read_write` so existing rows are unchanged) surfaced additively as `ProjectResourceDto.accessMode` and settable via `CreateProjectResourceBody.accessMode` / `UpdateProjectResourceBody.accessMode`. A primary resource is always coerced to `read_write`; a non-primary resource defaults to `read` when the caller omits the field. `read` (reference) resources are readable/navigable context but behave differently from `read_write`: they are NOT linked into `.overlord/project.json` when added (the service and clients skip the metadata write), and they are NOT offered in the resource picker (`distinctProjectResourceKeys` filters them). They ARE still included in the resource-paths env — the `OVERLORD_PROJECT_RESOURCES` manifest carries `accessMode` per entry and file-path sources of any access mode contribute their path to `OVERLORD_PROJECT_RESOURCES_PATHS`; URL/git sources contribute no path and are therefore excluded. `ovld add-cwd` / `ovld add-url` accept `--access read\|read_write`. `OVERLORD_PROJECT_RESOURCES_PATHS` / `_CSV` are comma-separated absolute paths where every entry carries an explicit `:rw` (`read_write`) or `:ro` (`read`) suffix; parsers treat a missing suffix as `rw` and tolerate whitespace around entries. Launch flows always emit explicit suffixes. Shared helpers live in `@overlord/contract` (`formatProjectResourcePathsFromManifest`, `parseResourcePathsCsv`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `18`    | Rich delivery rollout parity (coo:364 phase 4). Full `mission.delivered` webhook envelopes add `delivery.report`, the same normalized `DeliveryReportPayloadV1` projection returned by the delivery REST read surface; it never exposes raw `deliveries.payload_json`, while thin webhook envelopes remain identifier/link-only. The delivery composition worker emits bounded, structured operational logs/metrics for queued, composed, fallback, failed, duration, model, and input-size counts, without logging prompt or delivery content.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `17`    | Asynchronous delivery composition (coo:364 phase 3). Activates the reserved `overlord.delivery.compose.v1` `worker_jobs` type: `deliverSession` / `recordWork` enqueue a durable compose job in the same transaction as the delivery commit, mark `deliveryReport.presentation.status` `pending`, and never wait on Gemini. A backend leased worker claims jobs, calls the built-in `compose-delivery` automation (schema-constrained Gemini JSON), sanitizes/reconciles against agent evidence and deterministic path-derived candidates, updates only `deliveryReport.presentation` via revision CAS, and emits `entity_changes(entity_type=delivery)`. Provider absence, timeout, invalid JSON, or hallucinated items leave the deterministic/fallback presentation intact. Adds the previously documented but unimplemented `worker_jobs` table to SQLite/Postgres migrations.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `16`    | Rich delivery read surface (coo:364 phase 2). Adds the additive `GET /api/missions/:id/deliveries` REST endpoint, authorized with the mission's existing `mission:read` permission and returning normalized `DeliveryDto` records rather than raw `payload_json`; legacy deliveries synthesize a deterministic V1 report at read time. Delivery `MissionEventDto` records now carry an additive `deliveryId` parsed from their event payload, allowing clients to open the corresponding immutable delivery record. `ovld mission deliveries` now calls this endpoint. The web client may lazily load delivery records and render the deterministic Markdown, blue human-actions section, and amber tradeoffs section; normal `mission_event` and future `delivery` change-feed rows invalidate the relevant delivery queries. No Gemini composition is introduced in this phase.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `15`    | Rich delivery evidence (coo:364 phases 0–1). Adds the additive, versioned `deliveryReport` payload stored in `deliveries.payload_json`. New deliveries normalize agent-provided `humanActions`, `tradeoffsMade`, `knownRisks`, `deferredWork`, and `assumptions`, filter Git operations and routine QA from human actions, and store an immediate deterministic presentation without depending on an AI provider. The existing summary remains the immutable agent narrative; legacy and summary-only deliveries remain valid with empty evidence arrays. `ovld protocol deliver` accepts this optional payload through its existing `--payload-json` / `--payload-file` envelope, while hosted MCP exposes equivalent optional evidence fields. `overlord.delivery.compose.v1` is documented as a core `worker_jobs.type` reserved for the later asynchronous composition phase; it is not enqueued by phases 0–1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `14`    | Per-project launch preparation (coo:359). Adds two additive, backward-compatible project launch-preparation surfaces stored as namespaced keys in `projects.settings_json`: `overlord.preLaunchCommands` (ordered shell command lines) and `overlord.launchEnvVars` (user-defined `NAME=value` environment variables). Both are surfaced read-side on `ProjectDto` (`preLaunchCommands: string[]`, `launchEnvVars: Record<string,string>`) and settable via `UpdateProjectBody` (`preLaunchCommands?`, `launchEnvVars?`); no new columns or closed-vocabulary values. The runner claim response (Runner→REST queue surface) carries both for the runner launch path; a manual `ovld launch` fetches them from the mission's project. The CLI launch flow (Runner/Launch component) exports `launchEnvVars` into the agent launch environment and then runs `preLaunchCommands` after the terminal enters the working directory and exports the launch env, but before the agent process starts. Both support `{VAR_NAME}` placeholder substitution against the resolved launch context (e.g. `{OVERLORD_PROJECT_RESOURCES_PATHS}`); unknown placeholders are left verbatim. The variable set is intentionally open-ended; built-ins are documented in the shared `LAUNCH_VARIABLES` catalog (`@overlord/contract`) and listed in Project Settings → Launch, with plan-build vs attach availability called out in the mission-launch-lifecycle docs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `13`    | Resource-derived workspace authorization and launch settings (coo:331). Adds `GET /api/workspaces/:id/launch-settings` plus `PATCH /api/workspaces/:id/launch-settings/agents/:agentKey`, `/terminal-profile`, and `/worktree-branch-automation`, authorized against the target workspace (`launch:read` to view, `launch:configure` to mutate; non-membership reads as 404) while retaining the legacy active-workspace routes. REST operations that name a project, mission, objective, execution request, execution target, stored object, webhook subscription, or extension-owned resource resolve the resource's owning workspace and the caller's matching `workspace_users` row before permission checks, persistence, external-provider access, or change-feed attribution; the active-workspace preference is only a navigation/create default.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `12`    | User account and token lifecycle settings (coo:322). Adds `DELETE /api/user-tokens/:id`, authorized by the existing `user_token:self:revoke` capability, which may tombstone only a token already in `revoked` state; active, expired, and rotated tokens remain retained until they are explicitly revoked or otherwise managed through their existing lifecycle. The route returns `204`, never exposes token material, and emits an `entity_changes` `delete` tombstone. The web account settings use Better Auth's existing authenticated account APIs (`list-accounts`, `link-social`, `unlink-account`) to display, link, and unlink configured social providers; these auth-internal tables remain inaccessible to the REST/service layers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `11`    | Workspace-equal access surfaces (coo:324). Adds the additive workspace-scoped agent-catalog routes `GET /api/workspaces/:id/agent-catalog`, `PUT /api/workspaces/:id/agent-catalog`, and `POST /api/workspaces/:id/agent-catalog/refresh`, authorized per target workspace via the coo:96/coo:135 membership pattern (`launch:read` to view, `launch:configure` to mutate; non-membership reads as 404) so every workspace's `workspaces.settings_json.agentCatalog` is manageable without switching the active workspace. The legacy active-workspace `/api/agent-catalog` routes are unchanged. Web app: new-mission surfaces (new mission modal, quick task bar) list active projects from **every** accessible workspace of the active organization and read the agent/model catalog of the selected project's own workspace; the project chooser on an _existing_ mission remains limited to the mission's own workspace (cross-workspace mission moves stay rejected); workspace-settings Models and Archived Projects pages operate on the workspace being viewed rather than requiring it to be active.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `10`    | Hosted-web GitHub OAuth completion (coo:318). Adds the browser-only `GET /api/auth/browser/callback` and one-time `POST /api/auth/browser/exchange` handoff. The callback accepts only a configured trusted web origin, redirects there with a short-lived opaque ticket, and the web client exchanges that ticket once for its Better Auth session token. Neither the callback URL nor browser redirect exposes a session token, GitHub credential, bearer token, or reusable OAuth state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `9`     | Desktop session-token isolation (coo:318 follow-up). Remote desktop API and realtime requests authenticate exclusively with the active profile's Better Auth session token. The desktop no longer mints, reads, or prefers a `USER_TOKEN` for in-app requests, and clears the legacy desktop bearer-token slot during startup. `USER_TOKEN` remains the credential for CLI, runner, agent, and other headless clients.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `8`     | Desktop-remote GitHub OAuth bootstrap (coo:318). Adds the browser-only `GET /api/auth/desktop/github` initiation route. Electron opens this route in the system browser so Better Auth writes and later verifies its short-lived OAuth state cookie in the same browser cookie jar; the route accepts no caller-controlled callback and may initiate only the configured GitHub provider with the existing desktop callback. It redirects to the provider without exposing a session token, GitHub credential, bearer token, or reusable OAuth state to the renderer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `7`     | Desktop-remote GitHub OAuth completion (coo:69). Adds the authenticated browser-only `GET /api/auth/desktop/callback` handoff and one-time `POST /api/auth/desktop/exchange` session-token exchange. The callback may redirect only to `overlord://auth/callback` and carries an opaque, single-use, short-lived handoff ticket — never a Better Auth session token, GitHub credential, or bearer token. The Electron main process consumes the ticket directly against the active backend, stores the returned session token only via per-profile `safeStorage`, then reloads the shell; the renderer never receives the token through the deep link or preload bridge.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `6`     | Execution-target and resource interface refactor (coo:266). Adds the additive admin-only `PATCH /api/workspaces/:id/execution-targets/:targetId` ({ label }) route to rename a workspace execution target. Web-app settings: the general-settings Execution Targets page is removed (its per-machine terminal/agent launch defaults fold into Terminal & IDE); workspace-settings targets gain inline name editing; project resources render as expandable accordions with per-resource source lists and an add-source flow that selects an execution target defaulting to the project's active target.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `5`     | Resource registration completion (coo:263). `ovld add-cwd --key` reuses the project-scoped logical resource identity and upserts its target-scoped `local_checkout` source. Adds `ovld add-url --url <git-url>` to register or update a project-global `git` source for that same identity. `POST /api/projects/:id/resources` accepts either a local `directoryPath` or a Git `sourceUrl`; source descriptors remain typed and secret-free.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `4`     | Workspace execution-target settings (coo:263). Adds the authenticated, workspace-scoped read projection `GET /api/workspaces/:id/execution-targets`, returning target identity, non-secret type/status/owner display metadata, reachability, and active workspace-member access count. The settings UI uses this projection to separate all workspace targets into individual expandable cards; it does not expose connection details, credentials, target creation, registration, or membership mutation. Project settings continue to select only the acting member's eligible targets through the existing project-target selection surface.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `3`     | Provider-neutral virtual execution targets and resource identity/source split (coo:258, coo:263). Adds `virtual` as a documented core `execution_targets.type`, the external **Virtual Target Gateway** boundary and its versioned `/api/virtual-targets/v1/*` REST surface (registration, claim, progress, launched, failed, grant exchange, mission resources, delegated actions), the `rest-consumer` conformance component type, seven new core tables (`execution_target_registrations`, `project_environment_definitions`, `project_resource_sources`, `execution_request_snapshots`, `execution_request_grants`, `execution_request_observations`, `mission_target_resources`) plus `execution_requests` snapshot/failure/gateway columns. `project_resources` is one project-scoped logical identity (`resource_key`, label, primary/lifecycle metadata) and `project_resource_sources` owns all target-specific/global materialization descriptors (local checkout paths, Git, bundles, opaque handles). Existing resource rows and observations are intentionally discarded so users re-add sources. The immutable `VirtualExecutionQueueItemV1` carries resource identities plus typed sources, not paths; `execution_requests.status` and every other closed vocabulary are unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `2`     | ChatGPT Apps publication surface (coo:224). Adds explicit MCP tool output schemas and safety annotations, standard `ui://overlord/*` MCP Apps widget resources, structured tool results, and OAuth resource binding across approval and token exchange. Hosted MCP errors now distinguish tool failures from transport failures without exposing local checkout or runner capabilities.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `1`     | Resource-bound objectives and sibling resource context (coo:169). Adds logical `project_resources.resource_key`, target-portable `objectives.resource_key`, resource-scoped mission branch observations, additive `attach-response-v3` project resource manifest fields, optional `executionTargetId` protocol context input, `.overlord/project.json` `resourceKey`, `ovld add-cwd --key`, branch-action `resourceKey`, and resource-scoped worktree-path vectors. The worktree path algorithm now includes `resourceKey`, forcing this contract version bump.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `0`     | Initial public release baseline (coo:144). Describes the full component registry (including the MCP Server), interaction surfaces, stable interfaces (`attach-response-v3`, webhook delivery, organization → workspace → project hierarchy, multi-target `.overlord/project.json` linking, target-workspace project APIs, deliver `observedDirtyPaths` reconciliation, and `ovld protocol changes` preflight), extension points, and controlled vocabularies as shipped in Open Overlord v0. Pre-release contract versions 1–8 were consolidated into this baseline at public release.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

---

---

### Version 101 Change Summary

The hosted MCP server and the local connector shim expose the same three
PM-management tools, each forwarding through Protocol to the established service
operation instead of defining a connector-only mutation:

- `overlord_list_deliveries` / `list-deliveries` reads one addressed mission's
  newest-first normalized `DeliveryDto[]` records. It uses mission read
  authorization and never exposes raw `deliveries.payload_json`.
- `overlord_launch_objective` / `launch-objective` queues the normal execution
  request for an objective UUID or display id. Its `agent` is required; model,
  reasoning effort, and an eligible execution target are optional. It retains
  `execution_request:create`, launchability and active-sibling checks, target
  resolution, and idempotent active-request behavior.
- `overlord_reorder_future_objectives` / `reorder-future-objectives` accepts a
  mission UUID plus the full ordered UUID list of that mission's future
  objectives. It retains `objective:update`, rejects non-future/missing/duplicate
  ids, and returns the existing full `ObjectiveDto[]` ordering.

These are additive command and MCP-tool interfaces. The hosted surface remains
unable to inspect local files, claim runners, mutate execution targets, or run
branch actions. The MCP conformance manifest validates against this contract
version.

### Version 95 Change Summary

Cross-workspace authorization is organization-bounded. OAuth `USER_TOKEN`s bind
to exactly one organization and consent to either an explicit non-empty
workspace allowlist or every current and future workspace in that organization;
runtime access is always consent intersected with live membership and
per-workspace RBAC, and legacy tokens backfill only their recorded issuance
workspace. Request context resolves immutable `authorizedWorkspaces` by
operation taxonomy: entity-addressed, single-workspace, aggregate parentless,
or parentless-write; no operation falls back to an oldest membership.

Cross-workspace search has deterministic, non-redistributed even per-workspace
quotas and no cross-organization fan-out. The quota and `limit` allocate mission
groups, never individual indexed documents. `matchesPerResult` on v3 is a
separate per-group cap and is not divided across workspaces. Existing
`GET /api/missions/search` remains v1; additive `GET /api/missions/search/v2`,
Protocol `search-missions --response-version 2`, and MCP `overlord_search_missions`
v2 return a versioned envelope with result labels, match evidence, filters, and
truncation counts. Additive `GET /api/search/v3` and Protocol
`search-missions --response-version 3` return grouped `SearchResponseV3` with
identifiable objective and delivery matches. Date filters select `createdAt`,
`updatedAt`, or `dueDatetime`, defaulting to `updatedAt` only with a range, and
use inclusive `from` / exclusive `to`. REST v2 and v3 take stable project UUIDs;
Protocol, CLI, and MCP resolve human project references and answer an ambiguous
one with `project_selection_required` (see Version 97).

### Version 97 Change Summary

A project reference on an agent surface is whatever the user called the project.
Protocol `--project-id` and MCP `projectId` accept a UUID, a slug, or a name,
matched case-insensitively against the caller's live memberships. The MCP-side
UUID gate is removed: it denied hosted agents a resolver the protocol layer
already implemented, leaving them with a bare 404 and nothing to ask the user
about.

Project slugs and names are unique per **workspace**, not per organization, so a
human reference can legitimately match twice. That is a question, not a failure.
Every protocol subcommand that resolves a project reference — `search-missions`,
`statuses`, `discover-project`, mission creation — returns

```json
{ "status": "project_selection_required", "message": "...", "projectRef": "...",
  "projects": [{ "id", "name", "slug", "workspaceId", "workspaceName", "workspaceSlug" }] }
```

instead of a 409, mirroring `workspace_selection_required` on the parentless
creates. The workspace labels are load-bearing: without them the caller holds two
identical-looking projects and cannot phrase the question. An additive
`--workspace-id` / `workspaceId` (id, slug, or name) narrows the retry; it only
ever narrows, never widening past live membership. The CLI resolves the same way
and reports candidates rather than silently taking the first match.

`MissionSearchDateField` additively accepts `dueDatetime`, and
`MissionSearchResultV2` additively carries `dueDatetime`. This is what makes
"what is scheduled for tomorrow" answerable: a mission is scheduled by its due
date, and `schedules` is a recurrence template that writes `due_datetime`
forward rather than a second notion of scheduling. Because the column is
nullable, a `dueDatetime` range excludes missions that were never scheduled —
the opposite of `createdAt`/`updatedAt`, and intended, since a due-date window is
a question about scheduled work.

REST `GET /api/missions/search/v2` is unchanged in project handling: it still
accepts only stable UUIDs in `projectIds`, so name resolution stays an explicit
step rather than an implicit one on the HTTP surface. `GET /api/missions/search`
remains frozen as v1.

### Version 99 Change Summary

`GET /api/search/v3` is the grouped retrieval surface for the v98 search index.
Results stay one row per mission — the v2 fields survive — and each row carries
ranked `matches[]` for the objectives and deliveries that matched, plus
`anchorOnly` and pre-cap `matchCounts`. `event` documents corroborate score but
are never returnable. `matches` is the single source for child identity; it is
matched-only, not a complete objective list.

Group ranking is unchanged from v2 (MAX plus bounded corroboration, recency as
rank fusion). Child ranking is `docScore + 0.15 * missionScore`. The coverage
floor stays at mission level. Entity weights are mission 3.0, objective 2.0,
delivery 1.5, event 1.0. Per-workspace candidate fetch is `ORDER BY doc_score
DESC LIMIT 500`; SQLite joins `search_documents` from the FTS rowid rather than
recreating the virtual table. When the cap hits, `truncatedCandidates` is true
and `totalMatchedBeforeLimit` / `entityCounts` are lower bounds.

Additive filters: `entityTypes` (`mission` | `objective` | `delivery`, default
all three; `event` is rejected), `objectiveStates`, `matchesPerResult` (default
3, max 10). Excluding `mission` still returns the mission as the group anchor
with `anchorOnly: true`. An objective display-id query returns that mission
group with the named objective as its single match. REST v3 accepts only stable
project UUIDs. `GET /api/missions/search` and `/api/missions/search/v2` keep
their shapes and their mission/objective/event result sets. Protocol
`search-missions --response-version 3` returns `SearchResponseV3` with v3
defaults; entity-type flags and the canonical `search` alias remain a later
agent-surface change.

### Version 96 Change Summary

My Missions is keyed on status **type**, not status name. The aggregate board
renders exactly four columns — Next (`next`), Executing (`execute`), Review
(`review`), Completed (`complete`) — published from the contract as
`MY_MISSIONS_COLUMNS` / `MyMissionsColumnType`. The previous behavior, merging
per-project status lists by lowercase name, is removed along with the
"Uncategorized" bucket: every rendered mission now has exactly one home column,
because the column key _is_ its `statusType`. Missions in `draft`, `blocked`, or
`cancelled` statuses are outside the board vocabulary and are not listed on My
Missions; the read endpoint still returns them and clients filter.

`MyMissionReorderRequest` carries `statusType` instead of `statusId`. Because a
type column aggregates projects and workspaces, one reorder call now spans all
of them: the server resolves each moved mission's destination inside that
mission's own project as the lowest-position active status of the requested
type, and leaves the mission's concrete status alone when it already has that
type. Every mission is validated before any write, so a project that defines no
active status of the type rejects the whole call with
`STATUS_UNAVAILABLE_FOR_WORKSPACE` rather than half-applying the move. Personal
positions in `my_mission_positions` are unchanged in shape but are now numbered
across the whole aggregated column, so an order interleaving several projects
round-trips exactly.

## Component Registry

Each component owns a defined set of responsibilities. No component may directly access another's internals — use only the declared interaction surfaces.

For a narrative reading order over the per-module reference specs cited below, see the [Architecture reading-order index](docs/src/content/docs/docs-for-agents/architecture.mdx) (published in the docs site under **Docs for agents → Architecture**). The `NN-` numbers on those specs are stable identifiers, not a reading order.

### 1. Protocol Layer

**Stable identifier**: `protocol`
**Reference spec**: [`cli/docs/03-agent-protocol.md`](cli/docs/03-agent-protocol.md)

Owns:

- All `ovld protocol` subcommand names and their required/optional flags
- The `packages/core/` protocol/service core source package: shared service functions, repository helpers, and generated database types used by protocol, REST, CLI tests, and database harnesses
- Session lifecycle: `attach → (update|heartbeat)* → (ask|deliver)`
- `resolveObjectiveRef`: accepts an objective UUID or `{mission.display_id}.{display_key}` public id (e.g. `coo:756.k7xm`); rejects a bare mission id and a wrong separator (`:`, `|`, `-`). Protocol `attach` accepts optional `--objective-id` (same forms). When `--objective-id` or `--execution-request-id` is present, attach pins to that objective and must not rediscover via mission state. `--mission-id` may be omitted when `--objective-id` is a full display id; an objective UUID still requires it. Unpinned attach that would have to choose among two or more equally ranked active objectives (`executing`, else `launching`, else `pending_delivery`) returns 409 `ambiguous_active_objective` instead of guessing. Session-channel bind never falls back to a mission-wide unbound channel when an objective id is known.
- Opt-in objective parallelism: `missions.allow_parallel_objectives` (default false). When true, two objectives on the same mission may execute concurrently on any resource, including the same one — the Runner Layer separates same-resource pairs with per-objective branch/worktree isolation, and a mission that runs without worktrees shares its single checkout by design. Deliver does not move the mission to `review` while any objective is still `launching` / `executing` / `pending_delivery`.
- Context assembly format returned by `attach`, including additive `displayKey` / `displayId` on `ObjectiveSummary`
- Delivery payload structure and validation rules
- Versioned delivery-report evidence and deterministic/pending presentation in `deliveries.payload_json`, with optional asynchronous Gemini composition of `presentation` only
- Objective/session-ledger changed-file synchronization plus optional update,
  delivery, and record-work rationale annotations. Accepted changed-file and
  rationale writes emit scoped `entity_changes` in the same transaction so the
  changed-file-first REST projection refreshes without scanning a worktree
- Idempotency key scope naming for protocol operations (`protocol.*`)
- In-place mission artifact updates via `ovld protocol update-artifact` / `POST /api/protocol/update-artifact`, which apply the same editable-field and revision rules as REST `PATCH /api/missions/:id/artifacts/:artifactId` without requiring a live session key
- Mid-turn mission artifact creation via `ovld protocol add-artifact` / `POST /api/protocol/add-artifact`, which creates an artifact without a delivery using the same `createArtifact` service as REST `POST /api/missions/:id/artifacts`; an optional session key stamps session/objective provenance when present
- Run Queue agent operations: `queue-objective`, `dequeue-objective`, `run-queue`, `reorder-run-queue`, `create-run-queue`, `update-run-queue`, `delete-run-queue`, and `reorder-project-run-queues` use display-id/project resolution and the existing queue service. Hosted MCP and connector shims expose the same agent surface through `overlord_queue_objective`, `overlord_list_run_queues`, `overlord_reorder_run_queue`, and `overlord_manage_run_queue`. Entry membership and entry ordering require `execution_request:create`; queue-definition lifecycle requires `project:update`. `queue-objective` accepts queue, predecessor, front, and rank placement without selecting an execution target, and with no explicit destination targets the objective's own mission queue, creating it on demand before falling back to the project default
- `autoAdvance` is a deprecated compatibility view derived from live Run Queue membership. Protocol creation and update auto-advance options enqueue/dequeue; legacy storage writes retire next release and column removal requires a later contract bump
- Per-item `agent` and `model` launch selection for `add-objectives`: the optional
  fields persist to the new objective's existing `assigned_agent` and `model`
  columns; `model` requires a non-empty `agent`, while omitted fields preserve
  the existing project launch-preference defaults

Does NOT own:

- Connector installation (→ Connector Layer)
- Database schema (→ Database Layer)
- Authentication mechanism (→ Auth Layer)
- Runner queue/device/project-resource management commands (→ CLI / Runner / REST Layers)

### 2. Database Layer

**Stable identifier**: `database`
**Reference spec**: [`database/docs/09-database-schema-contract.md`](database/docs/09-database-schema-contract.md)

Owns:

- Table definitions, column types, indexes, foreign keys, CHECK constraints
- `objectives.display_key`: required Crockford Base32 key (lowercase, typically 4 characters), unique among non-deleted rows of the same mission. The public id `{missions.display_id}.{display_key}` is computed at read time and is not persisted as a second column. Soft-deleted keys may be reused.
- `missions.allow_parallel_objectives`: `boolean NOT NULL DEFAULT false`. Opt-in permission for two objectives on the same mission to execute at once, on the same `resource_key` or different ones. Domain enforcement is service-layer (no unique partial index); checkout safety is the Runner Layer's per-objective branch/worktree isolation.
- The `organizations` table (grouping layer above `workspaces`: `id` uuid, `name`, `settings_json` incl. `logoUrl`, timestamps, `deleted_at`, `revision`) and the required `workspaces.organization_id` FK; workspaces remain the sole RBAC layer, and slugs are unique per organization, not instance-wide
- `execution_target_runner_registrations`: the core, **one-to-many** record of live runner processes serving one `local` execution target (`runner_instance_id`, `native`/`adopted` `relation`, non-secret `label`/`runner_version`/`capabilities_json`/`supported_agents_json`, and `health`/`last_heartbeat_at`/`last_error_code`). It is the source of truth for **local** target reachability; `devices.last_seen_at` reverts to last observed machine traffic with no scheduling effect, and is honored for reachability only for a target that has never had a runner registration (upgrade compatibility). This table is distinct from the gateway-owned `execution_target_registrations`, which stays one registration per `virtual` target. `execution_requests.claimed_by_runner_registration_id` records which runner instance won a local claim
- The virtual execution target core tables (`execution_target_registrations`, `project_environment_definitions`, `project_resource_sources`, `execution_request_snapshots`, `execution_request_grants`, `execution_request_observations`, `mission_target_resources`) and the additive `execution_requests` snapshot/failure/gateway columns — these are **core** (not `ext_` extension) because they drive queue status, audit, authorization, and UI. The immutable `execution_request_snapshots` row (canonical `payload_json` + SHA-256 `payload_digest`) is created in the same transaction as its `queued` request and is never updated; retrying only increments `attempt_count`. Grant records store hashes/opaque IDs, never bearer values
- Optional per-agent `launchDefaults` in each `project_resource_sources.descriptor_json`; these source-owned pre-commands and normalized flags are project configuration and contain no credentials
- The **Agent Session Exchange** core tables — `agent_session_channels`, `agent_session_events`, `agent_requests`, and `agent_session_inputs` — declared by this contract and migrated with the channel bootstrap. They are core rather than `ext_` because they drive authorization, audit, presence, and UI gating. The scoped channel credential is stored hash-only and scoped to exactly one channel; `native_session_id` is a correlation alias and never an authorization key; raw native payloads are never persisted (no transcripts or transcript paths, no raw tool input or output, no file contents, no environment variables), and stored summaries are bounded, redacted, and carry a `formatter_version`. No derived capability tier is persisted — a connector's static tier is derived from fixtures at build time and a session's effective capabilities live in `capabilities_json` on its channel. The documented-but-never-migrated `hook_events` and `permission_requests` designs are **superseded** by them: sanitized hook events become normalized `agent_session_events`, and a permission becomes one kind of `agent_requests`. The closed `permission_requests.status` vocabulary and the `mission_events.type = permission_request` value remain valid; only the dedicated tables are retired, and no component may begin writing them
- Controlled vocabularies (closed and open sets)
- Soft-delete and revision semantics
- Migration versioning via `schema_migrations`
- Extension table naming rules (`ext_<name>_`)
- Logical types and their adapter mappings
- The async `DatabaseClient` adapter that owns the underlying handle/pool and serves every read/write on both editions through `?`-placeholder SQL (including the SQLite-only `sqliteDataVersion()` external-write probe); production callers never hold a raw synchronous `better-sqlite3` handle
- Ambient transactions: while a root `DatabaseClient`'s `transaction(async tx => ...)` callback is running, any query issued through that same root client from inside that async context automatically joins the open transaction (via `AsyncLocalStorage`, scoped per root instance) instead of deadlocking (SQLite) or executing outside the transaction on another pooled connection (Postgres); a transaction-scoped client captured and reused after its transaction has committed or rolled back throws `TransactionClosedError`

Does NOT own:

- Service-layer business logic (owned by the layer calling into the database)
- REST response shapes (→ REST API Layer)

### 3. CLI Layer

**Stable identifier**: `cli`
**Reference spec**: [`cli/docs/02-cli-first-product-surface.md`](cli/docs/02-cli-first-product-surface.md)

Owns:

- Management command names and argument shapes, including `ovld setup` (interactive first-run configuration), `ovld serve` (boot a fully-initialized local instance in local/backend packages), `ovld config set` (select local loopback backend URL or hosted/cloud backend URL), and `ovld auth login` (first-run backend onboarding before authentication)
- Project linking and discovery from working directory
- Configuration file locations and formats (`overlord.toml`, `.overlord/project.json`), resolved by walking up from the working directory with a documented packaged-mode fallback to the OS app-data directory (e.g. `~/Library/Application Support/Overlord/overlord.toml`), including backend URL settings (`backend_url` for the active REST/backend endpoint, `backend_mode` as `local` or `cloud`), optional legacy database settings consumed only by local/backend packages. Terminal launch settings live on `user_execution_target_preferences.terminal_profile_json` for the local execution target fingerprint (provisioned from the device fingerprint during `ovld setup`); that document additionally carries the optional versioned execution-provider/viewer block, whose absence means direct execution with the stored terminal choice, and whose user-level default lives on `profiles.metadata_json.launchSessionDefaults`. `.overlord/project.json` remains CLI-owned local link metadata: the legacy top-level `resourceId` stays required for backward compatibility, while the additive `resourceIdsByExecutionTarget` object may map `execution_targets.id` to target-scoped `project_resources.id`; readers must accept either shape and prefer the target-specific id when they know the acting execution target. The additive `resourceKey` field records the logical project resource key for this checkout and is reused when linking the same logical resource from another target. The additive `projects` array lists every project this checkout is linked to; top-level `projectId` / `resourceId` / `isPrimary` (and optional `projectName`) are the primary-project projection — the unique `projects[]` entry with `isPrimary: true`, or the most recently linked entry when none is primary. Writing a link with `isPrimary: true` demotes every other entry so at most one primary remains (the project that most recently set this resource as primary). A non-primary link is merged without stealing primary. Readers accept v1/v2 files that omit `projects` as a one-element list synthesized from the top-level fields. Only `read_write` resources are linked into `.overlord/project.json`; `read` (reference) resources are intentionally never written there. Config resolution layers, highest precedence first: an explicit runtime export of the channel variable (shell / container launcher) set before any env file loads, then the resolved `overlord.toml` `backend_url` (per-instance, e.g. written by `ovld config set`), then the profile env-file default (`.env.local`/`.env.prod`, backfilled), then a hardcoded fallback — see `resolveBackendUrl` in `cli/src/config.ts`. Development and production read **separate** backend env vars so the two channels never collide: development resolves `OVERLORD_BACKEND_URL_DEV` (`.env.local`, e.g. a dev backend on `:4320`) and never reads or writes the production `OVERLORD_BACKEND_URL`; production resolves `OVERLORD_BACKEND_URL` (`.env.prod`). No tooling aliases the dev channel into the production variable. The dev-only `OVERLORD_BACKEND_URL_DEV` must never reach a production app: a bare `ovld` invocation chooses its profile by build origin — an **installed/published** CLI (under `node_modules`) runs as `production` and never auto-loads `.env.local` or reads `OVERLORD_BACKEND_URL_DEV`, even inside a dev checkout, while only the in-repo source build and the dev/test tooling that runs it (`yarn dev`, `with-ovld-home`) default to `development` (`detectCliEnvProfile` in `cli/src/env.ts`). The webapp server and desktop pass their own profile explicitly (source-vs-bundled). A backfilled env-file value is only a default: an explicit `overlord.toml` outranks it (so `ovld config set`/`ovld init` take effect), and only a deliberate shell export of the channel variable outranks the toml. `overlord.toml` and `.overlord/project.json` are per-instance/per-deployment artifacts that are **not committed** — they are gitignored like `.env.local`/`.env.prod`, generated for a deployment by `ovld init`/`ovld setup`, with `overlord.toml.example` as the committed template. Local development needs no `overlord.toml`: it runs off `.env.local` plus code defaults. Each `ovld` process resolves its own config independently from its own working directory/env. A CLI running inside a container/agent pod is a pure consumer of the injected runtime `OVERLORD_BACKEND_URL` and **must not persist config**: `writeConfig` refuses to write `overlord.toml` when `isRunningInContainer()` (an explicit launcher `OVERLORD_IN_POD` marker, or the Docker `/.dockerenv` sentinel) is true unless `OVERLORD_ALLOW_CONFIG_WRITE=1` overrides it — preventing a context-specific value from leaking through a host-mounted file onto every host process that resolves it.
- Project resource registration commands: `ovld add-cwd` accepts optional `--key <resourceKey>` to bind/upsert a local checkout source for a stable logical resource key; `ovld add-url --url <git-url>` binds/upserts a project-global Git source for that same identity.
- Execution-target registration command: `ovld add-et --name <name>` (backed by the `register-target` protocol subcommand / `POST /api/protocol/register-target`) announces the acting machine as an execution target for the resolved workspace, declaring or reusing the device's `local` target idempotently and applying `--name` as its label. Parentless workspace resolution and the `workspace_selection_required` fallback mirror `ovld create-project`.
- Execution-target onboarding triggers: `ovld add-et` and an eligible machine-local checkout link (`ovld add-cwd`, `ovld create-project --directory`, the desktop local bridge) are the only CLI acts that create an execution target. `ovld auth login`, `ovld runner install`, `ovld runner start`, and `ovld runner supervise` never create one — credentials, an installed CLI, and a running process are all disqualified as triggers because container images ship all three. `ovld setup` creates a target only by making the same explicit `register-target` call, reported to the user as its own step. Commands that need a declared target and find none fail with an actionable message naming `ovld add-et` and `ovld add-cwd`.
- Runner identity environment: `OVERLORD_RUNNER_INSTANCE_ID` pins this runner process's stable instance identity (otherwise a local id is generated and persisted alongside the runner service state), `OVERLORD_EXECUTION_TARGET_ID` names an existing execution target to serve instead of resolving the acting machine's own, and `OVERLORD_RUNNER_RELATION` (`native` | `adopted`) declares the relationship. An AgentPod container sets the host's `OVERLORD_EXECUTION_TARGET_ID`, its own `OVERLORD_RUNNER_INSTANCE_ID`, and `OVERLORD_RUNNER_RELATION=adopted`; none of these ever create a target, and an unresolvable one fails loudly with `no_execution_target_registered`. Bare `OVERLORD_DEVICE_FINGERPRINT` remains a compatibility host hint, not the adoption model
- The optional per-repo `.overlordignore` file: gitignore-style patterns for paths the client-side objective ledger must never record or synchronize (see [`cli/docs/11-review-artifacts-and-change-tracking.md`](cli/docs/11-review-artifacts-and-change-tracking.md)). Parsed and applied entirely on the client at the ledger insertion boundary.
- The owner-only objective/session change ledger and local-only `ovld protocol capture-change` command. Ledger writes are cross-process safe, store normalized workspace-relative paths and bounded attribution metadata only, and require an explicit objective identity; cwd alone never resolves scope.
- The **agent-session runtime**: the `ovld agent-session` command family and the compiled harness catalog (`cli/src/agent-session/catalog.generated.ts`). It is the only local module that carries agent-session traffic to the backend, and it does so with the channel-scoped `OVERLORD_SESSION_CHANNEL_TOKEN` — never the user's broad `USER_TOKEN`. The runtime reads the compiled catalog, never the descriptor YAML, because parsing YAML on a hook path would violate the latency invariant and the installed adapter may be older than the CLI. `ovld agent-session capabilities [<agent>] [--json]` is a human/agent-facing offline reader of that catalog: no network call, no write, no subprocess. `ovld doctor` additionally reports each connector's installed descriptor digest against the compiled catalog and which mission the current working directory's native session is bound to, and makes no network call to do either — an unbound session must be able to run it with nothing requested on its behalf. Adding a sixth harness must add zero agent-session subcommands
- Human-readable CLI output format conventions

Does NOT own:

- Protocol commands (→ Protocol Layer)
- Agent connector installation (→ Connector Layer)

### 4. Connector Layer

**Stable identifier**: `connector`
**Reference spec**: [`connectors/docs/05-connectors-and-agent-plugins.md`](connectors/docs/05-connectors-and-agent-plugins.md)

Owns:

- Connector core workflow instructions
- Per-agent plugin/adapter files and managed file manifests
- Hook scripts and their event contracts
- `ovld agent-setup <agent>` / `ovld agent-setup all` and `ovld doctor` behavior
- Connector capability declarations in the harness descriptor
- The per-adapter **harness capability descriptor** (`connectors/adapters/<agent>/harness-capabilities.yaml`, validated by [`contract/harness-capabilities.schema.yaml`](contract/harness-capabilities.schema.yaml)) and its executable fixtures. The descriptor is the only hand-authored source of agent-session interaction capability truth: it is simultaneously the runtime gating input, the CI contract, and the source from which generated `CAPABILITIES.md`, `connectors/HARNESS-MATRIX.md`, and the compiled CLI catalog are produced. Capability ids, statuses, integration shapes, tiers, and fixture kinds come from the closed `agentSession` vocabulary in `contract/extension-points.yaml`; `capabilityTier` is derived from passing fixtures and is never authored or persisted
- Connector-owned native event-normalization and decision codecs. Native event and decision contracts belong to this layer; the CLI owns execution and transport but never becomes the source of truth for a harness dialect. `resolve` emits the harness's exact native decision shape, never a normalized Overlord shape, and every failure path returns _no decision_ so the harness's own behavior remains the floor
- Per-adapter decision codec declarations at `connectors/adapters/<agent>/codec/<agent>.decision-codec.yaml`, referenced from the harness descriptor and compiled into `cli/src/agent-session/decision-codec-registry.generated.ts`. The pure interpreter owns bounded/redacted request-card construction; the connector declaration owns native event paths and exact literal response bodies. Exact allow, deny, and defer behavior is proven by `decision-codec` fixtures that execute the shipped interpreter

Does NOT own:

- Protocol command implementations (→ Protocol Layer)
- Harness extension catalog (→ Extension System)
- Any network call to the Overlord backend. An adapter translates and hands off to the CLI, which owns transport, auth, retry, and redaction (→ CLI Layer)
- The live session's effective capability snapshot, which the backend computes from the catalog plus runtime probes and policy (→ REST API Layer)

### 5. Runner Layer

**Stable identifier**: `runner`
**Reference spec**: [`cli/docs/04-runner-and-launch-execution.md`](cli/docs/04-runner-and-launch-execution.md)

Owns:

- `execution_requests` queue claiming and launch
- Working directory resolution, ordered as explicit `workingDirectory`, objective `resource_key` on the claiming execution target, primary project resource for the target, then matching cwd `.overlord/project.json` fallback (the link whose `projectId` matches the mission project, among any number of projects recorded in that file); a missing objective-bound resource fails with `objective_resource_not_connected`
- Launch-config resolution for manual, auto-advance, and claim-time launches: an explicit objective override wins, then per-agent defaults on the selected `project_resource_sources` descriptor, then user execution-target and workspace defaults
- Per-mission branch/worktree preparation before local agent spawn when enabled, including the deterministic branch/worktree **planning** algorithm (`cli/src/branch-planning.ts`) — co-owned with the service layer and pinned to `contract/branch-planning-vectors.json` (see "Shared Deterministic Algorithms")
- Same-mission branch/worktree reuse: if the planned worktree already exists and is checked out on the expected mission branch, the runner may launch into it even when it has uncommitted changes, so sequential objectives can continue work on the same branch without forcing an intermediate commit
- Per-objective checkout isolation for parallel objectives: when the mission has `allowParallelObjectives` on, uses worktrees, has no explicit branch pin, and a sibling objective on the **same** effective `resource_key` is already `launching`/`executing`/`pending_delivery`, the runner plans `<mission branch>-<objectives.display_key>` in its own worktree instead of joining the sibling's dirty checkout, and reports `branchAutomation.isolated` on `branch-prepared`. The suffix is the objective's stable display key, so the decision needs no coordination between simultaneous launches and a relaunch returns the objective to its own worktree. Without worktrees the mission has one checkout and concurrent objectives share it deliberately — attribution comes only from each explicitly bound objective/session ledger, never from a checkout-wide scan
- Runner launch environment variables, including `OVERLORD_PROJECT_RESOURCES` as a JSON sibling-resource manifest resolved for the launching execution target (each entry additively carries `accessMode`; `read` and `read_write` file-path sources both contribute their path to `OVERLORD_PROJECT_RESOURCES_PATHS` with explicit `:ro`/`:rw` suffixes, while URL/git sources contribute none)
- Runner-instance identity and heartbeat: each runner process carries a stable `runnerInstanceId` and a `native`/`adopted` relation to the execution target it serves, published on every claim so the backend can report honest local liveness. Several runner instances may serve one target (a host runner plus containers adopting it); claiming stays target-level and a runner instance is never individually addressed
- `ovld runner` commands
- The persistent runner service: `ovld runner supervise` (the long-lived loop that delegates each claim-and-launch path to `ovld runner once`) and the `ovld runner service <install|start|stop|restart|status|uninstall>` management commands that register/control an OS-level user service (macOS `launchd` LaunchAgent `io.overlord.runner`, Linux `systemd --user` unit `overlord-runner.service`). A Postgres-backed runner continuously reconnects the queue claim long-poll immediately after its 25-second timeout or empty notification wake; SQLite uses one jittered 5-second cadence as a portable fallback. Local diagnostic state (installed service kind/identifier, resolved exec path, backend URL, last heartbeat/claim/launch timestamps, last error, current poll interval) is stored in `~/.ovld/runner-service.json`; it is local-only and not a backend source of truth. Service definition files embed a captured environment snapshot (a non-loopback backend URL, `OVLD_HOME`, user token, minimal `PATH`, and — when the installer itself runs under Electron-as-Node, e.g. driven by the desktop shell — `ELECTRON_RUN_AS_NODE=1` so the recorded Electron exec path keeps running as the CLI) and are written owner-read/write only. A loopback backend URL is deliberately omitted so the service follows the current `overlord.toml` after Desktop selects its Local port. Windows is not yet supported. The desktop app may drive these commands but must not reimplement the supervisor or service management
- Supervisor self-restart on program replacement: at startup the persistent supervisor records the identity of its own program (`process.execPath`) and its resolved entry script, re-checks that identity on every poll, and exits `0` as soon as either changes or disappears, so the OS service manager respawns it from the newly installed build. Exit `0` is required — a non-zero exit reads as a crash to `launchd`/`systemd`. The check is cheap and non-fatal: any inspection error other than the file being gone must never terminate a healthy supervisor. The recorded identity is process-local state and adds no field to `~/.ovld/runner-service.json`. This is the whole of the update story: the Desktop Shell does **not** stop, reinstall, or otherwise orchestrate the runner service around an app update (see "Desktop Shell → CLI (Process Supervision Surface)", which already forbids the shell from reimplementing service management) — the supervisor noticing its own replacement is what makes that orchestration unnecessary. Because the trigger is the program identity rather than any update transport, it covers desktop auto-update, a manual application-bundle replace, and a package-manager CLI upgrade identically
- Execution target selection logic
- Runner queue REST operations used by `ovld runner`: `GET /api/runner/status`, `POST /api/runner/claim`, `POST /api/runner/clear`, `POST /api/runner/requests/:id/launching`, `POST /api/runner/requests/:id/launched` (additive optional `providerSession` body persisted onto `metadata_json.providerSession`), and `POST /api/runner/requests/:id/failed`
- Branch-preparation acknowledgement through `POST /api/missions/:id/branch-prepared`, including the additive `isolated` flag that marks a per-objective parallel branch
- Latch capability discovery on the execution target: run read-only `latch capabilities --json` on the device where the process will run (never a different laptop), parse `protocolVersion` / `productVersion` / the capabilities map, expose three distinguishable states (`found` with version and resolved path, `not_installed` with the standalone install command, `incompatible` naming the missing capability), cache per target with cheap revalidation, and never install or upgrade Latch as a side effect. Direct execution remains selectable in every state
- Latch create-then-open launch path: when the claim-time launch-session snapshot selects Latch and discovery reports `found`, replace only the final process/terminal spawn with `latch create --manifest-file - --json` (same composed terminal command string S run via `$SHELL -ilc`), record the returned session id as `execution_requests.metadata_json.providerSession` (never as `launched_session_id` or a credential), then best-effort `latch open`; create failure fails the request, viewer failure does not. Direct launch remains the path when the provider is direct or Latch is unavailable
- Latch terminal-session lifecycle capabilities on the execution target: `inspectLatchSession`, `openLatchSession`, and `stopLatchSession` invoke the public Latch CLI with argv on the device that owns the mapped session, resolving the executable exactly as discovery does (PATH, then `~/.local/bin` and the macOS Homebrew prefixes) and reporting a named missing-binary condition when nothing resolves, so a process started without an interactive-shell PATH cannot see discovery report `found` while every lifecycle command fails. Inspect reports only the closed `running` / `exited` / `stopping` / `lost` projection when the session still exists; Latch `no session` absence (pruned or removed metadata) is `LATCH_SESSION_ABSENT` and is not a reachability failure. Reachability remains a separate capability-result/client condition for other lifecycle failures. Stop is explicit and destructive and is never coupled to objective delivery/completion. Overlord does not invoke or emulate Latch's removed v1 conversation commands; a future conversation integration must speak the v2 Conversation Hub protocol natively
- Launch does not mint Agent Session Exchange channels. Protocol attach is the binding declaration and does not require a Latch session or a session channel. Channel 1 (artifacts, shared state, delivery, follow-up capture, objective-ledger attribution) works with Latch uninstalled

Does NOT own:

- Protocol session lifecycle (→ Protocol Layer)
- Connector installation (→ Connector Layer)
- Virtual execution target claiming and environment realization (→ REST **Virtual Target Queue Surface** + external gateway). The runner claims and launches only local/device execution targets; it never resolves a working directory for a `virtual` target

### 6. REST API Layer

**Stable identifier**: `rest`
**Reference spec**: `database/docs/09-database-schema-contract.md` → REST API Boundary section

Owns:

- Project resource-source launch-default mutation through additive `PATCH /api/projects/:id/resources/:resourceId/sources/:sourceId`, authorized as a project update and preserving materialization descriptor fields

- URL paths and HTTP method contracts
- Project Run Queue REST family: `GET /api/projects/:id/run-queues`, `POST /api/projects/:id/run-queues`, `PATCH /api/projects/:id/run-queues/order`, `POST /api/projects/:id/run-queues/entries`, `PATCH /api/run-queues/:queueId`, `DELETE /api/run-queues/:queueId`, `PATCH /api/run-queues/:queueId/order`, `PATCH /api/run-queues/entries/:entryId`, and `DELETE /api/run-queues/entries/:entryId`. Reads require project/objective read access; planning mutations require `execution_request:create`; queue-definition mutations require `project:update`. The matching Protocol commands are `queue-objective`, `dequeue-objective`, `run-queue`, `reorder-run-queue`, `create-run-queue`, `update-run-queue`, `delete-run-queue`, and `reorder-project-run-queues`; MCP maps them through `overlord_queue_objective`, `overlord_list_run_queues`, `overlord_reorder_run_queue`, and `overlord_manage_run_queue`. Queue entries never select an execution target. `ObjectiveDto.queueEntry` additively projects live membership and `autoAdvance` is derived from that membership for compatibility. A queue may be mission-scoped (`RunQueueDto.missionId`/`missionDisplayId`); an entry POST without `queueId` targets the objective's own mission queue and creates it only if that mission has none, and emptying a mission queue retires it. `DELETE /api/run-queues/entries/:entryId` accepts an additive `{ force?: boolean }` body that also drops an entry wedged in `dispatched`/`running`, clears the objective's active execution requests, and resets a stuck `launching` objective to `draft`.
- Request/response DTO shapes (derived from the logical schema's camelCase field names)
- Creation-provenance projection: `MissionDto` and `ObjectiveDto` carry `createdByKind` (`human` \| `agent` \| `automation`; non-optional, with a `human` fallback in the mapper so no client writes a null branch), `createdByAgent` (connector/agent identifier or null), and `createdByWorkspaceUserId` (the workspace member the authoring actor acted as, or on behalf of). `MissionDetailDto` additionally carries `createdFrom` — `{ sessionId, missionId, missionDisplayId, agentIdentifier }` or null — resolved from the soft `created_by_session_id` reference by a `LEFT JOIN` that tolerates a missing session; it is paid once per mission page and is deliberately absent from the board/list projections. Provenance is a permanent row attribute, never seen-tracked mission state, and REST exposes no filter or sort on it.
- Objective display ids: `ObjectiveDto` additively carries `displayKey` and computed `displayId` (`{mission.displayId}.{displayKey}`). `/api/objectives/:id` (and launch/prompt/attachment routes that take an objective id) accept an objective UUID or a full display id. UUID lookup stays globally unique; display ids resolve within the selected organization's `authorizedWorkspaces` set, return `404` outside that set, and return `409` on genuine ambiguity.
- Opt-in parallel objectives: `MissionDto` and `UpdateMissionBody` additively carry `allowParallelObjectives` (default false). `POST /api/objectives/:id/launch` 409s an active sibling only while this flag is false; when it is true a sibling never blocks the launch, on the same `resource_key` or a different one. `POST /api/missions/:id/branch-prepared` accepts an additive `branchAutomation.isolated` boolean: an isolated per-objective branch is recorded on `objectives.branch` and audited as a mission event, but must not move `missions.active_branch` or the mission-level branch observation off the shared mission branch.
- Profile-owned inbox capture: authenticated `/api/inbox` CRUD and `/api/inbox/:id/promote`. Every item route resolves the caller's profile and returns `404` for an item owned by another profile. Promotion checks `mission:create` only on the destination project, calls the ordinary mission-create transaction with the capture fields, and consumes the item atomically. Account-owned inbox mutations are intentionally absent from workspace `entity_changes`; promotion retains normal mission realtime behavior.
- Cross-workspace activity feed: authenticated `GET /api/activity-feed` returns a bounded, time-descending `ActivityFeedDto` (launching/executing/`pending_delivery` objectives with their auto-advance queue, newest deliveries as normalized `DeliveryDto` records, and unseen blocking-question mission events from the past 3 days) across every workspace the caller actively belongs to in the active organization, authorized with `mission:read` per workspace. `ActivityFeedRunItemDto.state` is `launching` \| `executing` \| `pending_delivery`; a `pending_delivery` objective is live work the agent re-attached to, so it stays in the run projection instead of dropping out of the feed between its last update and delivery. Every item carries additive objective `createdByKind` / `createdByAgent` (human fallback when the objective join is null). Agent identity treats the protocol session sentinel `unknown` as absent and falls back to the objective's `assigned_agent`. Truncation is reported through `counts` rather than implied, and no raw payload JSON is exposed.
- Delivery read projection: `GET /api/missions/:id/deliveries` returns authorized `DeliveryDto` records with a normalized versioned report, while delivery mission events expose their additive `deliveryId` without exposing raw event or delivery payload JSON
- Editable mission artifacts: authenticated `PATCH /api/missions/:id/artifacts/:artifactId` accepts `expectedRevision` plus a non-empty subset of `{ label, contentText, externalUrl }`, requires `mission:update` on the named mission, retains delivery/session/objective provenance and `contentJson`, rejects stale edits with `409` and non-HTTP(S) external URLs, and emits an `artifact` entity-change row in the write transaction. The client renders `contentText` as safe Markdown and edits these human-facing fields in place. The same mutation is also reachable through Protocol `update-artifact` and MCP `overlord_update_artifact` (see Protocol / MCP ownership).
- Mid-turn mission artifact creation: authenticated `POST /api/missions/:id/artifacts` accepts `{ type, label }` plus at least one of `{ contentText, externalUrl }`, requires `artifact:create` on the named mission, optionally stamps `objectiveId` / `sessionId` provenance, leaves `delivery_id` null, rejects non-HTTP(S) external URLs, and emits an `artifact` entity-change insert in the same transaction. The same mutation is reachable through Protocol `add-artifact` and MCP `overlord_add_artifact`.
- Mission shared context: authenticated `GET /api/missions/:id/context` returns `SharedContextEntryDto` records (`key`, `value`, `valueKind`, `tags`, `updatedAt`, `revision`) authorized with `mission:read`; authenticated `PUT /api/missions/:id/context` upserts one entry by `{ key, value }` with `mission:update`, mirrors Protocol `write-context`/`read-context`, and emits a `shared_context_entry` entity-change in the same transaction. The web client renders these entries in a collapsed Shared State mission footer and supports in-place edits.
- User-scoped Everhour identity: authenticated `GET /ext/everhour/user-connection` reports non-secret connection metadata for the caller's profile; authenticated `PUT /ext/everhour/user-connection` validates the personal API key against Everhour then stores it only as AES-256-GCM ciphertext under a server-held key; authenticated `DELETE /ext/everhour/user-connection` revokes/soft-deletes only the caller's credential. These account-wide routes do not use an ambient workspace or a `workspace:*` permission. Existing `GET`/`PUT`/`DELETE /ext/everhour/integration` are a deprecated alias of the same profile-scoped surface. Timer, time-entry, and project-link writes use the acting user's key. DTOs, logs, browser/mobile storage, realtime, entity changes, and errors never contain an Everhour API key or server encryption key. Workspace-scoped GitHub App installation and Everhour/GitHub resource links are unchanged.
- User-scoped GitHub repository authorization: authenticated `GET /ext/github/user-connection` reports configuration and non-secret connected-account metadata; `POST /ext/github/user-connection/authorize` creates a profile-bound, hashed, short-lived OAuth state and returns the GitHub authorization URL; public `GET /api/auth/callback/github/repository` consumes that state once, exchanges the code server-to-server, encrypts the access and optional refresh credentials with AES-256-GCM, and stores one active connection for the profile; authenticated `DELETE /ext/github/user-connection` revokes/soft-deletes only the caller's credential; authenticated `GET /ext/github/repository-owners` returns the personal owner plus only active organizations whose policy permits the caller to create private repositories. Repository authorization reuses the Better Auth GitHub OAuth App client registration (`GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET`) but remains a distinct, explicit grant and token lifecycle requesting `repo read:org`; the callback is a subpath of the registered `/api/auth/callback/github` login callback and runs before the Better Auth wildcard. These account-wide routes do not use an ambient workspace or alter the existing workspace GitHub App installation. DTOs, logs, browser/mobile storage, realtime, entity changes, redirects, and errors never contain a GitHub credential, raw OAuth state, or server encryption key.
- Composite project initialization: authenticated `POST /api/projects/initialize` names its target workspace and requires `project:create` there. The caller-provided idempotency key creates the project, first mission, and initial draft objective atomically; optional private GitHub provisioning runs outside that transaction through the already connected user OAuth credential and is retryable with the same key. A failure never rolls back the created idea. On success the extension records one GitHub project link and the core service records one non-primary read-only `git` resource for the HTTPS clone URL. The selected owner must be eligible for the connected account, and this flow never reads, changes, or depends on the workspace GitHub App installation.
- Realtime `EntityChangeDto` projections from `entity_changes`, including `changedFields` parsed from `changed_fields_json` and additive `workspaceId`; streams and catch-up scans aggregate only the caller's current readable memberships, filter before projection, and advance the global cursor past filtered rows
- Read-only derived mission branch metadata (`MissionBranchDto`) from `missions.active_branch`, predicted via the service layer's copy of the shared branch/worktree planning algorithm (`backend/branch-planning.ts`) — co-owned with the Runner Layer and pinned to `contract/branch-planning-vectors.json` (see "Shared Deterministic Algorithms")
- On-demand branch-action git mutations (`POST /api/missions/:id/branch/action`: commit / merge-with-parent / push-parent / publish) run host-side against the project's worktrees under `~/.ovld/worktrees` and the selected project resource (`resourceKey` request field, defaulting to the primary resource) (`backend/repository.ts`). The Runner Layer owns launching the _agent_ into a worktree, not these on-demand mutations.
- REST auth integration points
- Client machine identity: the `x-overlord-device-fingerprint` / `-label` / `-platform` request headers are an **optional machine-local lookup hint**, never authentication, authorization, or audit identity. They exist only so a co-located CLI or desktop-bridge caller can be matched to an execution target that was already declared for that machine, and the backend still authorizes the resolved target against the authenticated actor's membership and access. A hint must never cause an `execution_targets` row to be created on a read, protocol-lifecycle, observation, or runner path. Ordinary browsers and the mobile app send no such headers; only the CLI and the desktop shell bridge do.
- Execution-target declaration: `POST /api/protocol/register-target` (`ovld add-et`), the equivalent `POST /api/workspaces/:id/execution-targets` (the desktop one-click affordance; same `execution_request:claim` authorization as `register-target`, optional `{ label }`), and an **eligible machine-local project-resource checkout link** are the only REST operations that may create a `local` execution target. Every other route — including the per-machine launch-settings mutations — resolves an existing target or reports its absence.
- Leased execution targets: persistent is the default. A human may explicitly create an independent local target with `{ lifecycle: { kind: 'leased', leaseSeconds, graceSeconds } }` through an existing declaration surface; runners, environment variables, and adoption never select or mutate this policy. A successful authorized local runner heartbeat renews only that active target's lease. Expiry is derived offline/ineligible state, never deletion or access/resource/preference cleanup; a returning runner can renew during grace. `disabled` suspends cleanup and renewal while retaining the target, and re-enable starts a fresh lease. The backend's idempotent `overlord.execution_target.lease_cleanup.v1` worker owns post-grace cleanup: it never silently reroutes queued target-bound work, waits for live claims to recover, fails remaining queued work with redacted `execution_target_lease_expired`, then uses the ordinary target deletion cascade. Lifecycle changes and queue transitions emit the normal entity-change/mission-event projections.
- Machine-local eligibility for declaration: a caller may declare the acting machine only when it carries a real machine-local host identity — the process host on a co-located Local backend, otherwise a non-browser, non-backend-host `x-overlord-device-*` hint. A caller with no such identity is refused with `409 no_execution_target_registered` and an actionable message; it is never given an inferred target.
- Project-resource source declaration semantics: writing a `local_checkout` source with an explicit `executionTargetId` updates that already-declared target's source and creates nothing (this is how an ordinary browser edits a source); writing a `sourceUrl` Git source creates a project-global source (`execution_target_id IS NULL`) and creates nothing; writing a `local_checkout` source with no `executionTargetId` declares the acting machine when it is machine-local-eligible, and otherwise fails `409 no_execution_target_registered` rather than declaring the caller or silently writing a pathless global source.
- Launch-settings mutations (`PATCH /api/workspaces/:id/launch-settings/agents/:agentKey` and `/terminal-profile`) require an already-declared acting target and fail `409 no_execution_target_registered` when the machine has none. Configuring settings is not an onboarding trigger. `/launch-settings/session-defaults` is the exception by construction: the user-level provider/viewer default is stored on the profile, not a target, so it neither requires nor declares one.
- Desktop OAuth handoff endpoints: browser-only `GET /api/auth/desktop/github` starts only the configured GitHub provider with the fixed desktop callback, so Better Auth's OAuth state cookie is created in the system browser; authenticated `GET /api/auth/desktop/callback` mints an opaque, short-lived, single-use handoff ticket from that browser session and redirects only to `overlord://auth/callback`; unauthenticated `POST /api/auth/desktop/exchange` consumes that ticket once and returns the Better Auth session token to the desktop shell's main process. These routes never return GitHub credentials, bearer tokens, or reusable OAuth state, and the ticket itself is not an API credential
- Hosted-web OAuth handoff endpoints: browser-only `GET /api/auth/browser/callback` validates the requested return origin against configured trusted web origins, mints an opaque, short-lived, single-use handoff ticket from the authenticated browser session, and redirects only to that origin; unauthenticated `POST /api/auth/browser/exchange` consumes the ticket once and returns the Better Auth session token to the web client. These routes never return GitHub credentials, bearer tokens, or reusable OAuth state in a URL, and the ticket itself is not an API credential
- The versioned **agent-session channel route family** (`/api/agent-session-channels/v1/*`): adapter event, request, input, and heartbeat operations for exactly one channel, authenticated **only** by a channel-scoped credential. A normal user or session token is rejected here, and a channel credential is rejected on every human route. The separate authenticated human surfaces — resolving a session's effective capabilities, listing (optionally filtered to one mission) and answering requests, releasing a request back to the terminal so the native prompt owns it, and enqueuing or cancelling input — use ordinary session/token auth plus resource-derived RBAC, require the permission-resolution RBAC and workspace policy ceiling to resolve a decision, and apply revision CAS with immutable audit attribution. REST additionally applies a second server-side payload allowlist and bounds every summary, option, id, sequence, timestamp, and diagnostic code; formatter output renders as escaped plain text with explicit truncation. Capability projections are diagnostics: a client gates controls on the live channel's effective capability snapshot, never on the static connector catalog and never on the agent identifier
- Objective-scoped human agent-session reads: `GET /api/agent-requests` and `GET /api/agent-session-inputs` accept optional `missionId` and `objectiveId` query parameters. A full objective display id supplies its mission; a UUID requires `missionId`. When `objectiveId` is present, returned request/input rows and the `liveChannel` projection are restricted to that objective, and a mismatched mission/objective pair is rejected.
- SSE/WebSocket realtime endpoint
- SQL Studio launch metadata in `/api/meta` when the optional external SQL Studio process is enabled
- Webhook subscription management (`/api/webhooks*`: list/create, update/delete, rotate-secret, test, deliveries, redeliver) requires an explicit `workspaceId` for parentless list/create operations (or derives it from the named project); entity-addressed mutations derive and authorize the subscription's owning workspace. The in-process webhook dispatcher (`backend/webhook-dispatcher.ts`, a `RealtimeHub`-style singleton polling loop) claims `outbox_messages` rows and delivers HMAC-signed HTTP POSTs. SSRF guards (HTTPS-only, private-network block) apply to external endpoints; hosts matching the `OVERLORD_WEBHOOK_INTERNAL_HOSTS` env allowlist (plus implicit `localhost` in Local edition) are treated as internal — exempt from the SSRF block and the HTTPS requirement, and default to the `full` payload mode
- Full `mission.delivered` webhook envelopes expose the authorized normalized `delivery.report` projection (never raw delivery payload JSON); thin webhook envelopes remain identifier/link-only
- Organization management endpoints: list organizations for the caller, update an organization (name/logo; org-admin gated), and list/add/remove organization admins (transactional invariant: an org admin is `ADMIN` of _every_ constituent workspace)
- The shared onboarding endpoint (`POST /api/onboarding`, zero-membership users only): creates an organization, a workspace (default `general`), the caller's membership, and the `ADMIN` role in one transaction, then returns `/api/meta` — used verbatim by both the web onboarding screen and the `ovld org-setup` CLI command so the two clients cannot drift
- Storage routes for the `organization-images` bucket key (`/api/storage/organization-images/…`) with a `PUBLIC organization_image:read` grant, mirroring `workspace_image:read`
- Workspace target projection (`GET /api/workspaces/:id/execution-targets`): authenticated members may view every non-deleted, executable target in that workspace (browser-only web sessions are not execution targets and are excluded), including non-secret identity/status/owner metadata, reachability, and active-member access count. Workspace admins may rename a target label via `PATCH /api/workspaces/:id/execution-targets/:targetId` (`{ label }`), and may soft-delete a target via `DELETE /api/workspaces/:id/execution-targets/:targetId` when it has no active queued work; deletion clears target-scoped project resources and project target preferences pointing at it, and soft-deletes the linked `devices` row when it no longer has any live execution target. The projection also soft-deletes any remaining orphan devices (no live execution target) before returning. The projection never exposes `connection_json` or credentials; project selection remains constrained to the caller's eligible targets.
- Workspace agent catalog and launch-settings management only through their workspace-scoped routes under `/api/workspaces/:id/...`, authorized against the target workspace's own membership (`launch:read` to view, `launch:configure` to mutate; non-membership reads as 404). Resource-owned REST and extension operations derive both the workspace and the caller's membership in it before authorization, persistence, provider access, and change-feed attribution.
- Authenticated default-project preference routes: `GET /api/profile/default-project`, `PUT /api/profile/default-project` (`{ projectId }`), and `DELETE /api/profile/default-project`. The read result is `{ projectId: string | null }`; `defaultProjectId` is additive on `/api/meta`. A preference is profile navigation state, grants no access, and is ignored when its project is no longer readable.
- User-token lifecycle endpoints: authenticated owners may list, create, rename, and revoke their own tokens; `DELETE /api/user-tokens/:id` shares the existing `user_token:self:revoke` authorization and only soft-deletes an already-revoked token, returning `204` and emitting an `entity_changes` delete tombstone. Token list/create/rename/revoke/delete responses never expose raw secrets or hashes. `POST /api/user-tokens` takes no workspace or organization selection: a token minted by the signed-in user for themselves consents to all current and future workspaces in that caller's organization, recorded as `all_workspaces` with no narrowing allowlist, and its issuance workspace is audit attribution only. Every request still intersects that consent with live membership and per-workspace RBAC. Explicit workspace consent is the third-party OAuth approval path only. These endpoints resolve the owning profile from the authenticated caller, never from the instance's oldest account.
- Hosted MCP OAuth endpoints: dynamic client registration (`POST /oauth/register`), authorization-code + PKCE token exchange (`POST /oauth/token`), revocation (`POST /oauth/revoke`), browser authorization redirect (`GET /oauth/authorize` → web approval UI), and authenticated approval helpers (`POST /oauth/authorize/request`, `POST /oauth/authorize/approve`). The request/approval helpers expose and require an explicit single-organization consent choice when more than one organization is available; approval selects explicit live workspace ids or all current/future workspaces in that organization. `GET /api/authorized-workspaces` is the read-only projection of the caller's organization/workspace authorization snapshot and never changes scope.
- The versioned **virtual-target gateway route family** (`/api/virtual-targets/v1/*`): `PUT …/registration`, `POST …/claim`, `POST …/requests/:id/progress`, `POST …/requests/:id/launched`, `POST …/requests/:id/failed`, `POST …/grants/:id/exchange`, `GET …/missions/:id/resources`, and `POST …/missions/:id/actions`. REST owns the DTO shapes, target-authenticated auth/RBAC enforcement, per-request idempotency, and bounded/redacted output for this surface. The immutable `VirtualExecutionQueueItemV1` payload and its digest are built by the service layer at queue time and returned only to the authenticated claiming gateway; the local `/api/runner/*` routes are unchanged
- Mobile Live Activity registration routes: authenticated `PUT` and `DELETE /api/mobile/live-activities/:activityId/push-token`. Registration accepts an opaque ActivityKit push token, required `environment`/`bundleId` for per-registration APNs routing, and an optional `startedByPush` handoff marker, and is private to the caller's profile; neither registrations nor tokens have a read surface.
- Mobile Live Activity push-to-start routes: authenticated `PUT /api/mobile/live-activities/start-token` and `POST /api/mobile/live-activities/start-token/revoke` for opaque ActivityKit push-to-start tokens (body-carried, private to the caller's profile, no read surface). The stored registration is the account's consent record for desktop-initiated Live Activities.
- In-process Live Activity dispatcher (`backend/live-activity-dispatcher.ts`), which claims durable `overlord.live_activity.dispatch.v1` and `overlord.live_activity.start.v1` worker jobs, recomputes the bounded account presentation snapshot, and sends APNs `liveactivity` start/update/end events.
- Standard push-notification routes: authenticated `PUT /api/mobile/push/device-token` and `POST /api/mobile/push/device-token/revoke` for opaque APNs device tokens (body-carried, private to the caller's profile, no read surface), plus authenticated `GET`/`PUT /api/profile/notification-preferences` for the caller's own per-category preferences.
- In-process standard push dispatcher (`backend/push-notification-dispatcher.ts`), which claims durable `overlord.push_notification.dispatch.v1` worker jobs, recomputes the alert presentation snapshot, and sends APNs `alert`/`background` pushes on the plain bundle-id topic. It shares the APNs signing/transport helpers with the Live Activity dispatcher but never shares tokens, topics, or push types with it.

Does NOT own:

- Database schema (→ Database Layer)
- Protocol CLI surface (→ Protocol Layer)

### 7. Auth Layer

**Stable identifier**: `auth`
**Reference specs**: [`auth/docs/07-user-token-authentication.md`](auth/docs/07-user-token-authentication.md), [`auth/docs/08-role-based-access-control.md`](auth/docs/08-role-based-access-control.md)

Owns:

- Token creation, rotation, revocation, tombstoning of revoked credentials, and hash storage
- Role assignment CRUD
- Permission check interface
- Audit log attribution fields
- Better Auth implementation tables (`user`, `session`, `account`, `verification`, `apikey`) — these are auth-internal and must not be read directly by other components
- Self-service account deletion trigger (`user.deleteUser`), delegating the cascade to a backend-supplied callback
- Sign-up/sign-in email verification: an optional `sendVerificationEmail` callback (`CreateAuthOptions.sendVerificationEmail`) delegates delivery to a backend-supplied sender, mirroring the `onDeleteUser` delegation pattern. When provided, Better Auth also enables `emailAndPassword.requireEmailVerification` so unverified accounts cannot sign in. When omitted (the default, e.g. Local/offline editions with no configured email provider), verification stays fully disabled and behavior is unchanged. The backend's concrete sender (`backend/email-verification.ts`) uses Resend, configured via the `RESEND_API_KEY` env var and the `notifications.cooperativ.io` sending domain
- Numeric one-time codes (OTP): an optional `sendEmailOTP` callback (`CreateAuthOptions.sendEmailOTP`) enables Better Auth's `emailOTP` plugin (6-digit codes, 1-hour expiry), the same caller-supplied-callback pattern as `sendVerificationEmail`. When enabled, the sign-up verification email carries **both** the existing magic link **and** a real 6-digit code minted via `auth.api.createVerificationOTP` and passed to `sendVerificationEmail` as `otp` — replacing the previous behavior of showing the raw (untypable) verification link token in the code block. The plugin also exposes `/api/auth/email-otp/*` server endpoints (notably `verify-email`, `check-verification-otp`, `send-verification-otp`, `sign-in/email-otp`, `forget-password`/`reset-password`) and the corresponding `authClient.emailOtp.*` client methods for typed-code sign-in and password reset. When `sendEmailOTP` is omitted, the plugin is left off and no OTP endpoints exist. The backend sender is `emailOTPSenderFromEnv()` in `backend/email-verification.ts` (Resend-backed)
- Hosted MCP OAuth consent and token issuance: OAuth approval creates a scoped `USER_TOKEN` with the `mission_lifecycle` preset after an authenticated browser session approves the request. Authorization codes are short-lived, single-use, PKCE-protected, and exchanged for bearer access tokens; refresh tokens are not issued through contract version `2`. A code that expires unexchanged, or whose exchange fails its client/PKCE/resource checks, revokes the `USER_TOKEN` it would have delivered, so no orphaned active token outlives its authorization code.

Does NOT own:

- User identity schema (co-owned with Database Layer via schema contract)
- Application business logic gating (callers act on the result of a permission check)

### 8. Automations Layer

**Stable identifier**: `automations`
**Reference spec**: [`automations/docs/01-automations-overview.md`](automations/docs/01-automations-overview.md)

Owns:

- The pure `planRunQueueDispatch` planner. It plans each project queue independently, permits one dispatched/running entry per queue, retains and skips held entries, and never performs I/O or target selection.

- Pluggable automation interface (`Automation`) and built-in automation registry
- Provider configuration for optional Gemini-backed tools (`GEMINI_API_KEY`, model selection)
- Reference summarization tools (text summarization, objective title generation)
- Built-in `compose-delivery` automation for schema-constrained delivery presentation drafts
- Fire-and-forget automation helpers that callers invoke through injected persistence callbacks
- Downstream automation loading via `OVERLORD_AUTOMATIONS_MODULE` (the `custom-automation` extension point)

Does NOT own:

- Database schema (→ Database Layer)
- Mission/objective lifecycle state machines (→ CLI / service layer callers)
- Agent protocol or connector behavior (→ Protocol / Connector Layers)

### 9. Extension System

**Stable identifier**: `extension`
**Reference specs**: `database/docs/09-database-schema-contract.md` → Extension Points section; [`connectors/docs/agent-harness-configuration-architecture.md`](connectors/docs/agent-harness-configuration-architecture.md)

Owns:

- `user_harness_extensions` authoring workflow
- `workspace_harness_extensions` catalog promotion workflow
- Extension table namespace (`ext_<name>_`)
- Extension migration component naming (`ext:<name>`)
- Namespaced JSON metadata keys inside core tables

Does NOT own:

- Core table schemas (→ Database Layer)
- Built-in connector catalog (→ Connector Layer)

### 10. Desktop Shell

**Stable identifier**: `desktop`
**Reference spec**: [`desktop/docs/desktop-app.md`](desktop/docs/desktop-app.md)

An **optional** Electron wrapper around the webapp. It is a thin desktop shell, not a reimplementation of product logic: it loads the web control center in a hardened `BrowserWindow`, supervises the local processes the webapp depends on (Local mode), and gives the user a native app shell.

The shell runs in one of two modes, selected by the active **backend profile** (Local is the default and the unchanged behavior all prior contract text describes):

- **Local profile** (unchanged): fork the embedded web/REST server (`utilityProcess`) and load the bundled SPA from the loopback origin; API base = the loopback origin; Better Auth session cookies are same-origin; CSP/nav guard are scoped to that single loopback origin.
- **Remote profile** (additive): **do not** fork the embedded server. The bundled SPA still loads from a local origin (privileged renderers never load remote content), but its runtime-injected **API base URL** points at the hosted backend. In-app auth uses the active profile's Better Auth **session bearer token**, obtained at login and persisted via `safeStorage`/keychain; it never reads or mints a `USER_TOKEN`. Spawned `ovld` processes receive their separate `USER_TOKEN` credential only when required. CSP `connect-src` and the navigation guard are recomputed from the active profile to allow the remote REST + `wss://` origins while keeping navigation shell-local; `/api/health` checks support HTTPS with a Local fallback.

Owns:

- The Electron app/window lifecycle and the hardened `BrowserWindow` security baseline (`contextIsolation`, `sandbox`, `nodeIntegration: false`, `preload`), the loopback-scoped CSP, single-instance lock, external-navigation handling, native window chrome (macOS `hiddenInset` title bar + inset traffic-light position so the webapp nav serves as the title bar), and the sandboxed renderer's native context menu (spellcheck/edit roles)
- Process supervision of the bundled web/REST server (forked as a Node `utilityProcess` in **Local** mode only; **not forked** in Remote mode) and, optionally, a local runner
- Persisted backend **profiles** (`{ id, label, mode: 'local' | 'remote', backendUrl }` + active profile id) in `userData/settings.json`, per-profile Electron **session partitions** (`persist:backend-<id>`), per-backend bearer-token storage via `safeStorage`, and the reload-and-login switcher / first-run chooser
- The minimal `preload` bridge surface exposed to the renderer as `window.overlord` (feature-detected by the SPA; never required by it), including the additive backend-config ops `getActiveBackend()`, `listBackends()`, `addBackend()`, `removeBackend()`, and `switchBackend(id)`, plus `writeProjectMetadata(...)` for local checkout metadata writes in remote-client mode, `invokeLocalTarget(call)` for unified checkout-local capabilities, and `runnerService.{getStatus,install,start,stop,restart,uninstall}()` which drives the CLI-owned `ovld runner service <action>` operations as a subprocess and returns their parsed JSON result (the shell never manages the service process itself)
- The feature-detected native notification bridge `window.overlord.showNotification({ title, body, tag?, soundUrl? })`, implemented only through the `overlord:show-notification` IPC handler. It accepts bounded, user-visible presentation only; it never carries a route, credential, token, raw event content, or arbitrary Electron capability.
- Remote OAuth callback ownership: registers `overlord://auth/callback`, receives only an opaque handoff ticket, exchanges it directly from the Electron main process with the active backend, and persists the returned Better Auth session token in the matching profile's `safeStorage`; neither the deep link nor the preload bridge carries a token
- Mission deep-link ownership: registers and consumes `overlord://missions/<missionId>` where `<missionId>` matches `[A-Za-z0-9:_-]{1,64}`. The main process rejects all other mission URL shapes, focuses or creates the shell window, and emits the shell-local `overlord:navigate` bridge notification for the fixed internal `/user/missions/<missionId>` route. The link carries no credential, ticket, or payload; the feature-detected SPA navigates only to that supplied internal route.
- Desktop packaging metadata and the build/sign/notarize pipeline (`electron-builder`, entitlements, app-data layout)

Does NOT own:

- REST/SSE URL paths or DTO shapes (→ REST API Layer)
- CLI/launch/terminal configuration, including per-user terminal profiles on `user_execution_target_preferences` (→ CLI / Runner Layers)
- Authentication mechanism (→ Auth Layer)
- Database schema or adapter selection (→ Database Layer)

Depends on:

- `rest` — loads the SPA and calls `/api/*` over the loopback origin
- `cli` / `runner` — spawns `ovld serve` / `ovld runner` / `ovld launch` as subprocesses
- `auth` — in-app auth uses Better Auth session cookies on the same loopback origin; spawned CLI uses `USER_TOKEN` when a credential is required

The dependency arrow points one way only: `webapp`, `cli`, and `database` MUST NOT depend on `desktop`.

### 11. Mobile REST Consumer

**Stable identifier**: `mobile`

The native Overlord Mobile app is an external REST consumer. It owns the local
ActivityKit lifecycle and the per-activity APNs token stream; it never accesses
Overlord's database or APNs credentials.

Owns:

- The account-level ActivityKit activity identifier and `pushTokenUpdates` lifecycle
- Registration/rotation/revocation calls on the Mobile → REST surface
- Ending local activities before sign-out and ensuring the corresponding registration is revoked
- Presentation of the additive creation-provenance fields it decodes from `MissionDto` / `ObjectiveDto` (`createdByKind`, `createdByAgent`, `createdByWorkspaceUserId`). The mobile mirror in `OverlordCore/Contract.swift` decodes them as optionals defaulting to `human`, per the existing additive-decode convention, so an older server response still decodes. Where the mission chat feed reads objectives back as messages the operator sent, an `agent`-authored objective must be attributed rather than presented as the operator's own text, and the attribution must also lead the VoiceOver label — a client that renders provenance only as a fill or glyph misattributes it for assistive technology.

Does NOT own:

- Server-side registration persistence, account snapshot selection, APNs authentication, delivery, or retries (→ REST / Database Layers)
- Mission/objective lifecycle and authorization (→ Protocol / REST / Auth Layers)

Depends on:

- `rest` — authenticated HTTPS/JSON calls only

### 12. MCP Server

**Stable identifier**: `mcp`
**Reference spec**: [`mcp/README.md`](mcp/README.md)

An optional hosted Model Context Protocol surface for cloud agents such as
ChatGPT, Claude, and other MCP clients. It runs inside the backend process and
is enabled only when `OVERLORD_MCP_ENABLED=true`.

Owns:

- MCP endpoint path (`/mcp`) and supported MCP protocol-version advertisement
- MCP tool names, input/output schemas, safety annotations, MCP Apps resource
  URI patterns, prompt names, and MCP JSON-RPC response shaping. The public
  ChatGPT surface is mission-first: project resolution and selection, mission
  search, mission-context reads, and explicit mission/session writes. Widget
  resources are presentation-only and consume the structured result of a
  corresponding render tool; they never call local filesystem or runner APIs.
- OAuth protected-resource metadata for the hosted MCP resource
- Mapping MCP tool calls to existing service/protocol operations
- Hosted `overlord_update_artifact` tool, which forwards to Protocol `update-artifact` and never writes artifact rows directly
- Hosted `overlord_add_artifact` tool, which forwards to Protocol `add-artifact` and never writes artifact rows directly
- Hosted `overlord_create_inbox_item` and the unassigned fallback of `overlord_create_mission`, both forwarding to the profile-owned inbox service; an omitted `projectId` returns an explicit `{ unassigned: true, inboxItem }` result and never chooses a workspace or project.
- Additive `assignedTo` input on `overlord_create_mission`, forwarded to the same service-layer member resolver as Protocol `--assigned-to` (accepting `workspace_users.id`, profile UUID, `orgid:username`, bare username, or email). An unresolvable member is an error, never a silent fallback to unassigned. The MCP surface never resolves members itself, and `assignedTo` is meaningless on the unassigned inbox fallback above.
- Mission and objective results returned through MCP carry the same `createdByKind` / `createdByAgent` / `createdByWorkspaceUserId` provenance fields as the REST DTOs they project; rows an MCP caller creates are authored on the protocol surface and are therefore `agent`-kind.

Does NOT own:

- Database schema or persistence rules (→ Database Layer)
- Authentication mechanism, token issuance, consent, or RBAC policy (→ Auth Layer)
- Protocol lifecycle semantics such as attach/update/deliver state transitions (→ Protocol Layer)
- REST URL/DTO contracts outside `/mcp` and OAuth discovery metadata (→ REST API Layer)
- Local filesystem, worktree, runner claim, or branch-action operations (→ CLI / Runner / REST Layers)

Depends on:

- `auth` — resolves the MCP caller to an authenticated profile/workspace actor
- `protocol` / service layer — executes mission lifecycle and project discovery operations
- `database` — reached only through existing service/protocol functions

---

## Interaction Surfaces

These are the **only sanctioned paths** between components. Bypassing these surfaces is a contract violation.

### Agent → Protocol (Primary Mission Surface)

- **Transport**: Subprocess CLI invocation of `ovld protocol <command>`
- **Authentication**: Session key passed as flag or environment variable
- **Required sequence**: `attach → (update|heartbeat)* → (ask|deliver)`
- **Response format**: JSON on stdout; non-zero exit on error
- **Inbox capture**: `ovld protocol create --inbox` creates an account-owned unassigned capture. Ordinary non-executing `create` uses an explicit project first, then discovered project, then this inbox fallback; `prompt` and `record-work` continue to fail without a project because they imply executable work.
- **Shell-special content**: Must use `--summary-file -` / `--payload-file -` with stdin heredoc
- **Delivery evidence**: `deliver` accepts an optional versioned `deliveryReport.agentReport` object. Its `humanActions`, `tradeoffsMade`, `knownRisks`, `deferredWork`, and `assumptions` fields are advisory agent evidence, normalized to empty arrays when absent. Malformed advisory items are discarded independently with bounded warnings; evidence, change tracking, and model availability never delay delivery. Human actions must exclude Git operations and routine review/testing; the protocol applies this exclusion before persistence. The stored `deliveryReport.presentation` begins as an immediate deterministic fallback and is marked `pending` when a compose job is enqueued; a background worker may later replace only `presentation` with a Gemini-composed or `fallback` result. The original delivery summary remains immutable.

### Protocol → Database (Service Layer)

- **Transport**: Service layer function calls within ACID transactions
- **Rule**: No direct table writes from protocol handlers; use the service layer
- **Concurrency**: Optimistic compare-and-set via `revision`; zero-row update = `409` conflict
- **Change feed**: `entity_changes` appended in same transaction as domain mutation
- **Effect queue**: `enqueueWebhookEvent()` (`packages/core/service/webhook-events.ts`) appends matching `outbox_messages` rows in the same transaction as the domain mutation — a second shared-single-writer rule alongside `insertEntityChange`, called from both the protocol service core and the REST data layer so no origin path silently skips webhook delivery
- **Idempotency**: Protocol-scope idempotency keys in `idempotency_keys`
- **Execution-target provisioning**: Service functions reached from protocol lifecycle, discovery, observation, launch-settings writes, or any read may only _look up_ the acting machine's execution target; reads must tolerate `null` and writes that require one must fail with `no_execution_target_registered`. Creating a `local` `execution_targets` row is reserved for the two explicit declaration paths (`register-target` and eligible machine-local checkout linking), which are the only callers permitted to reach the declaration primitive
- **Leased-target lifecycle**: `execution_targets.lifecycle_kind = leased` is an explicit declaration-time policy for a disposable independent local target. A lease expiry only removes eligibility/reachability; it cannot itself delete links, preferences, access, or a target. Lease renewal and cleanup are compare-and-set service operations; cleanup is a durable backend worker which rechecks target revision/policy and active claims before using the normal deletion cascade.
- **Runner-instance registration**: `execution_target_runner_registrations` rows are written only by an authorized runner claim/heartbeat for an execution target that already exists, is `local`, is `active`, and is accessible to the authenticated member. Upserting a runner instance never creates, re-enables, or renames its target. Local target reachability is derived from these rows; `devices.last_seen_at` is consulted only for a target that has never had one

### CLI → REST (Backend Client Surface)

- **Transport**: HTTP/JSON to the configured backend URL (`backend_url`) over loopback for local mode or HTTPS for cloud mode
- **Rule**: The published npm CLI must not open SQLite, import database adapters, or write tables directly; mutations go through REST/backend endpoints that use the service layer/database runtime behind the backend process
- **Project context**: Resolved via backend APIs plus `.overlord/project.json` and working-directory hints sent in request payloads
- **Runner**: The CLI runner remains local and launches agents, but queue claim/status/mark-success/mark-failure operations go through backend APIs

### Connector → Protocol (Hook Surface)

- **Hooks**: harness-native lifecycle callbacks declared by the connector descriptor
- **Transport**: Shell scripts invoking `ovld protocol hook-event` for follow-up activity or the local-only `ovld protocol capture-change` command for file evidence
- **Rule**: Hook scripts must not write to the database directly; use protocol commands only
- **Scope**: file capture requires an explicit objective identity and its attached protocol session. The working directory is only the normalization root and never selects an objective. An unbound callback performs no attribution.
- **Direct edit capture**: a post-tool callback must pass its connector key, and may append `declared_edit` / `direct` evidence only when that connector's compiled codec normalizes the event as `file.edited` with an exact path. The CLI bounds the normalized workspace-relative path and applies `.overlordignore` before writing the owner-only objective/session ledger. Generic path-key parsing is forbidden; known read-only callbacks are silent, while mutation-capable, shell, unmapped, and no-path callbacks record bounded unavailable health and claim no path.
- **Mutation-window foundation**: each shipped connector declares a fixture-backed
  `mutationHooks` classification in its harness descriptor. A paired pre/post
  runtime may run only when strict fixtures and the installed connector both
  prove matching session, call, workspace, tool, outcome, and path/window
  semantics. It records `window_observed` / `window` evidence with honest
  overlap state. `post-only` and `unsupported` classifications never trigger a
  worktree-wide delta or synthesize attribution.
- **Privacy boundary**: native payloads, file contents, diffs, raw commands,
  transcript content or paths, environment values, fingerprints, and absolute
  host paths never cross this adapter surface.
- **Follow-up capture**: `UserPromptSubmit` records `user_follow_up` activity even when the original delivery ended the session; it must not reopen implementation work by itself

### Connector → CLI Agent Session Runtime → REST (Agent Session Surface)

The Connector → Protocol hook surface above carries **mission lifecycle** activity. This is a
separate, narrower surface for **session interaction** — observing what an agent is doing,
answering a decision it is blocked on, and injecting an instruction into it.

- **Transport**: a connector integration (a callback script, an in-process extension, or a
  supervised control-plane sidecar) hands the harness's **native payload** to
  `ovld agent-session <action> --agent <key> --payload-file -`. The CLI then calls the
  versioned `/api/agent-session-channels/v1/*` route family. Adapters make **zero** network
  calls; one privacy review therefore covers every connector instead of one review per connector
- **Authentication**: the CLI presents a channel-scoped, hash-stored
  `OVERLORD_SESSION_CHANNEL_TOKEN` bound to exactly one session channel — never the user's
  broad `USER_TOKEN`. A channel credential can affect only its own channel; it cannot read
  mission context, cannot resolve a human decision, and is rejected on human routes. A normal
  user or session token is rejected on the adapter route family. Credentials travel only through
  environment, stdin, or an authorization header — never argv, URLs, descriptor files, events,
  diagnostics, or crash text
- **Binding authority**: a native session id is a **correlation alias only**. Authorization and
  mission scope come from the verified channel/session credential. The working directory may
  help _locate_ an existing local binding but never creates one and never authorizes an event,
  because cwd answers "which project is this?" and every decision here is session-scoped
- **Scope gate ordering**: scope resolution runs before logging, interpreter spawn, filesystem
  write, or network access. The only permitted pre-gate operation is a bounded stdin read plus a
  native-id/channel lookup in already-known local state. An unbound session produces no request,
  no event, no database row, and no observable delay
- **Failure rule**: the harness's own behavior is the floor. Timeout, backend error, missing
  binding, auth failure, or malformed response all return _no decision_, which yields the
  harness's native prompt. No failure path may manufacture an approval, and a decision call must
  never retry with a broader credential
- **Payload reduction happens on the machine, before transmission.** Raw native payloads are
  never persisted: no transcripts or transcript paths, no raw tool input or output, no file
  contents, no environment variables. What is persisted is the native session id, a normalized
  tool name, a bounded redacted summary derived by a deterministic formatter, option ids and
  labels, timestamps, and resolver attribution. Follow-up prompt text is the one deliberate
  exception, persisted in full for bound sessions because the activity feed exists to show it
- **Capability gating**: what a surface may offer for a session is the effective capability
  snapshot on the live channel — never the static catalog, and never the agent identifier.
  Installed harness version, connector drift, project policy, and runtime probes may all
  downgrade a session below its catalog maximum
- **Human surfaces are a separate auth family.** Listing, answering, and enqueuing use ordinary
  authenticated session/token auth plus resource-derived RBAC. A channel credential can never
  present itself as a human actor, and a human credential can never impersonate an adapter

### Runner → REST (Delegation Surface; formerly Queue Surface)

- **Transport**: HTTP/JSON to the configured backend URL
- **Endpoints**: `GET /api/runner/status`, `POST /api/runner/claim`, `POST /api/runner/clear`, `POST /api/runner/requests/:id/launching`, `POST /api/runner/requests/:id/launched` (additive optional `providerSession` body), `POST /api/runner/requests/:id/failed`, and `POST /api/missions/:id/branch-prepared`
- **Claiming**: Backend performs an atomic compare-and-set claim on `execution_requests`; verifies launchable state
- **Run Queue boundary**: project Run Queues are upstream planning state and are invisible to runners and virtual-target gateways. A dispatcher resolves target/config at dispatch and creates an ordinary `execution_requests` row with additive open `requested_source = run_queue` and an entry-scoped idempotency key; claiming and all runner DTO/state semantics remain unchanged.
- **Long poll**: On Postgres, an otherwise-empty `POST /api/runner/claim` waits up to 25 seconds on a dedicated (non-pooled) `LISTEN` connection for queue notifications, then retries the same scoped atomic claim. Its additive `longPoll` response flag is `true`; the runner immediately reconnects after an empty long-poll response. Notifications are wake hints only and never replace authorization or claiming. SQLite returns immediately with `longPoll: false` and retains the portable fallback cadence.
- **Side effects**: Backend claim and state transitions append `mission_events` and `entity_changes` in the same transaction
- **Claimant resolution**: A claim resolves an **already declared** non-deleted `local` execution target and never creates a device or execution target. An explicit `executionTargetId` in the claim body is resolved and authorized (existing, non-deleted, type `local`, `status = 'active'`, and an active `workspace_user_execution_targets` row for the authenticated member); otherwise the acting machine is resolved from the process host on a co-located Local backend or from the `x-overlord-device-*` hint. When no such target exists — or an explicit target id does not resolve in that workspace — the claim fails with `409 no_execution_target_registered` and an actionable message naming `ovld add-et`, so a machine with a runner but no declaration is visibly unconfigured rather than silently minting a target. A runner polling several workspaces reports this only when **no** workspace in its scope resolves a target
- **Runner instance registration**: The claim body additively carries `runnerInstanceId`, `runnerRelation` (`native` | `adopted`), `runnerLabel`, `runnerVersion`, `capabilities`, and `supportedAgents`. When `runnerInstanceId` is present the backend upserts **only** the caller's `execution_target_runner_registrations` row for the resolved target — health `healthy`, heartbeat `now` — and never the target itself. Several runner instances may serve one target: a physical host registers a `native` runner, and a container adopting that host (AgentPod) registers an additional `adopted` runner against the **same** target row rather than declaring its own. `relation` defaults to `native` when the target was resolved from the acting machine's own hint and `adopted` when an explicit `executionTargetId` names a target belonging to a different device. A claim without `runnerInstanceId` (an older runner) still claims normally and simply registers no instance
- **Claim competition is target-level**: any healthy runner registered to the selected execution target may claim its work. Runner instances are diagnostics and liveness, never an addressable routing unit; no request field selects one
- **Claim metadata**: Claim must set `claimed_by_device_id`, `claimed_by_execution_target_id` when known, `claimed_by_runner_registration_id` when the claim registered a runner instance, and `claim_expires_at`
- **Local liveness**: A `local` target is reachable when at least one of its non-deleted runner registrations is `healthy`/`degraded` with a heartbeat inside the reachability TTL. The 25-second claim long poll carries the heartbeat; there is no second polling loop. For upgrade compatibility a target that has **never** had a runner registration continues to fall back to `devices.last_seen_at`, so a pre-upgrade install stays selectable until its runner publishes its first heartbeat; once any runner has registered for a target, `devices.last_seen_at` no longer affects its reachability
- **State transitions**: `queued → claimed → launching → launched|failed`; active requests may be cleared; stale claims, launched-without-attach requests, and a `launching` request whose runner never reported `launched` within the launch TTL may move to `expired`; other terminal statuses are sinks unless a new request is explicitly queued. The launch TTL is measured from `execution_requests.launch_started_at`, which is written when the request enters `launching`. Expiry never re-queues — a runner that died mid-launch may already have spawned an agent the backend cannot see, so expiring the row plus the runner's own reported failure is what preserves launch-at-most-once. Because a very slow launch can be expired underneath itself, `POST /api/runner/requests/:id/launched` must tolerate the resulting conflict on an already-`expired` request rather than treat it as an unexpected error
- **Attach correlation**: Protocol attach links the launched request to `agent_sessions` via `execution_requests.launched_session_id` when the launched context supplies an execution request id
- **Terminal-session projection**: `MissionDetailDto.terminalSessions` returns bounded Latch provider-session mappings from all non-deleted execution requests for the mission, including target/device label, stored session name, claim-time executable/viewer, the claim-time window-or-tab shape (`viewerOpenAs`, absent meaning `window`), and last observed state. It never exposes launch environment, terminal output, conversation data, credentials, socket tokens, or attachment grants. A mapping Latch has already reclaimed (`LATCH_SESSION_ABSENT`) is omitted from the projection after `forget` drops `metadata_json.providerSession`; the panel does not keep a tombstone card. The mission panel presents remaining sessions as one card — the newest session for the active objective, else the newest overall — with the rest collapsed into an accordion whose trigger counts the other running Latch connections. Mission and objective lifecycle mutations never stop a terminal session. Protocol attach is the binding record and does not depend on Latch.
- **Latch v2 boundary**: `POST /api/missions/:id/terminal-sessions/forget` drops a provider-session mapping after Latch reports that session absent; it is idempotent, does not stop a process, and does not delete the execution request. Overlord exposes no harness-event ingest or pending-input resolution routes and maintains no conversation cursor or fold. A future conversation surface must use Latch's authenticated, server-first `/v2/sessions/:id/conversation` WebSocket and Hub-owned revision/operation semantics rather than reintroducing transcript observation or PTY injection.

### Virtual Gateway → REST (Virtual Target Queue Surface)

- **Transport**: HTTPS/JSON to the configured backend under the versioned `/api/virtual-targets/v1/*` route family
- **Authentication**: A gateway principal credential scoped to exactly one `execution_targets` row of type `virtual` (target-authenticated, **not** device-authenticated). A gateway cannot enumerate, claim, or observe another target's work; `execution_requests.claimed_by_execution_target_id` is the universal claimant identity and `claimed_by_device_id` stays nullable for virtual claims
- **Registration**: `PUT /api/virtual-targets/v1/registration` registers/refreshes the target's gateway identity (`gateway_key`, `gateway_instance_id`), advertised capabilities, supported agents, supported queue schema versions, adapter key/version, and heartbeat. Registration **replaces** rather than creates a target when a stable gateway identity matches
- **Claiming**: `POST /api/virtual-targets/v1/claim` atomically claims a queued request assigned to the caller's target and returns the immutable `VirtualExecutionQueueItemV1` snapshot, a `claimId`, an expiry, and only request-scoped opaque launch-grant references — never filesystem paths or raw secrets. Replaying the same gateway + request returns the same claim and payload. `resolveWorkingDirectory` never runs on a virtual claim
- **Observed state**: `POST …/requests/:id/progress` appends a bounded, monotonic preparation observation and does **not** change status; `POST …/requests/:id/launched` validates request/claim/target/digest, records a `VirtualTargetLaunchObservationV1`, then makes the normal `launching → launched` transition exactly once; `POST …/requests/:id/failed` records a typed, redacted `VirtualTargetFailureV1` and makes the allowed transition to `failed` (its `retryable` flag informs existing retry policy but does not itself requeue). All three are idempotent by request and observation `sequence`
- **Grants**: `POST …/grants/:id/exchange` swaps an authenticated, unexpired opaque grant for narrowly scoped credentials or attachment-download access, bound to request + target + gateway instance; every exchange is audited and never returns the user's Overlord token
- **Mission resources / actions**: `GET …/missions/:id/resources` returns summarized opaque lifecycle resources and source-compatibility observations; `POST …/missions/:id/actions` authorizes an explicit `start`/`stop`/`archive`/`delete`/`enqueue`/`retry`/`dequeue` action and delegates it to the target. Delegated-action output becomes an observation and can **never** change mission completion — only an agent protocol delivery completes an objective
- **Boundary rule**: Desired launch state (Overlord's immutable snapshot) and observed realization state (gateway reports) are kept separate. Paths and raw credentials never cross this boundary. The gateway owns source/environment realization and observed-state reporting; Overlord owns durable queue delivery, leases/retries, and status transitions

### REST API → Database (HTTP Surface)

- **Transport**: Service layer function calls; same service layer as CLI and Protocol
- **Auth**: Token or session auth via Auth Layer before service call
- **Response shape**: Derived from logical schema camelCase field names
- **Idempotency**: REST-scope idempotency keys for retried writes
- **Delivery reads**: `GET /api/missions/:id/deliveries` authorizes against the named mission's owning workspace with the existing `mission:read` permission. It returns bounded, normalized `DeliveryDto` records and synthesizes the deterministic V1 report for legacy payloads; neither `deliveries.payload_json` nor `mission_events.payload_json` are exposed verbatim.
- **Account-owned inbox**: Inbox CRUD resolves the authenticated `profiles.id` rather than an ambient workspace. Reads and mutations are scoped to that profile; promotion derives authorization and tenancy solely from its destination project. Inbox CRUD writes do not append workspace `entity_changes`, because those records have no workspace; promotion appends the ordinary mission change in the destination workspace.
- **Workspace resolution taxonomy**: Entity-addressed reads and writes derive a workspace from their operand and require it in the immutable request `authorizedWorkspaces` set. Single-workspace UI reads require an explicit `workspaceId` and return `400` when omitted. Aggregate parentless reads fan out only across the selected organization's authorized set; when a session holder belongs to multiple organizations, selection is explicit. Parentless writes use `resolveParentlessWorkspace` only for `create-project` and `register-target`; mission writes derive tenancy from their named project and inbox operations are profile-owned. No route may select the oldest membership.
- **Authorized workspace shape**: Auth resolves once per request `{ organizationId, workspaces: [{ workspaceId, workspaceUserId, roleKeys }] }`, where workspaces are active live memberships intersected with token consent (when present). Services may retain one operation-local `resolvedWorkspaceId`, but must not reinterpret the set from request headers or persistence.
- **Mission search versioning**: `GET /api/missions/search` is frozen as its v1 array-shaped response. `GET /api/missions/search/v2` returns `SearchMissionsResponseV2`: `{ version: 2, results, appliedFilters, totalMatchedBeforeLimit, workspaceCounts }`. Each result includes project/workspace labels, `dueDatetime`, `relevance` (deterministic fused score with exact display-id / exact-title precedence), `snippet`, `matchedTerms`, and `matchedIn`. Ranking is portable across adapters: display-id short-circuit, meaningful-term coverage floor `min(3, max(1, ceil(termCount / 2)))`, `max(documentScore)` plus bounded corroboration, and recency as rank fusion rather than an eligibility gate. Empty or stop-word-only queries use an explicit `fallback` mode. Complete and cancelled missions stay eligible by default. The REST route accepts only stable `projectIds`, plus `resourceKeys`, status types, `dateField` (`createdAt`, `updatedAt`, or `dueDatetime`), inclusive `from`, exclusive `to`, and global `limit`; it applies no implicit date window. The Protocol, CLI, and MCP v2 surfaces additionally resolve a human project reference and answer an ambiguous one with `project_selection_required` (Version 97). A `dueDatetime` range excludes missions with no due date, because the column is nullable and a due-date window asks about scheduled work. Fan-out is one organization only, with `floor(limit / workspaceCount)` per workspace and UUID-ordered remainder slots; unused quota is not redistributed. V1 and v2 search only the mission, objective, and event index corpus even after delivery indexing ships. Additive `GET /api/search/v3` returns `SearchResponseV3`: mission-anchored groups with `anchorOnly`, ranked `matches[]` (`objective` | `delivery`, each with id, display id, snippet, and score), `matchCounts` before `matchesPerResult` capping, envelope `entityCounts`, and `truncatedCandidates`. `event` is corroboration-only and never appears in `matches`. `matches` is matched-only. Child relevance is `docScore + 0.15 * missionScore`; the coverage floor stays at the group's concatenated haystack. Per-workspace candidate fetch is bounded at 500 documents — above that cap, counts are lower bounds. `entityTypes` defaults to mission, objective, and delivery (`event` is rejected); `objectiveStates` filters returnable children; `matchesPerResult` defaults to 3 and maxes at 10 and is not divided across workspaces. The mission row always anchors the group, including when `anchorOnly` is true. An objective display-id query returns that objective as the group's single match. REST v3 accepts only stable `projectIds`. Protocol `search-missions --response-version 3` returns the same envelope with v3 defaults. Artifacts are not indexed.

### Mobile → REST (Live Activity Push Surface)

- **Transport**: Authenticated HTTPS/JSON with the existing Better Auth session or `out_…` bearer token; no ambient workspace input is accepted.
- **Registration**: `PUT /api/mobile/live-activities/:activityId/push-token` accepts `{ pushToken, environment, bundleId, startedByPush? }`, upserts the caller's private `(profile, activityId)` registration, and replaces a rotated token. `environment` is `sandbox` or `production` and selects the APNs host per registration; `bundleId` forms the `…push-type.liveactivity` topic. `activityId` and `pushToken` are opaque values and are never returned or exposed through realtime, logs, or read APIs.
- **Revocation**: `DELETE /api/mobile/live-activities/:activityId/push-token` deletes only the caller's registration and returns `204`; it is safe to retry after a local activity has already ended.
- **Delivery**: Server lifecycle mutations enqueue coalesced `overlord.live_activity.dispatch.v1` jobs. The dispatcher recomputes the caller's two-running-objectives-plus-one-recent-completion snapshot and delivers APNs `liveactivity` update/end events on each registration's own `<bundleId>.push-type.liveactivity` topic and environment host (not the process-wide `OVERLORD_APNS_ENV` default). Running rows are **objective-grain**: `title` and `displayId` name the executing objective (`coo:756.k7xm`), `id` stays the parent mission id so ActivityKit attributes need not change, and additive `objectiveId` / `missionDisplayId` / `missionTitle` carry the mission as secondary context. Completing one objective does not end the activity while another objective for the same account is still executing. `content-state.updatedAt` is Unix epoch seconds (JSON number), never an ISO-8601 string — ActivityKit's push decoder cannot parse ISO timestamps into `Date`. State transitions are sent promptly; progress that leaves visible content unchanged is suppressed for at least five minutes. Invalid APNs tokens are retired.
- **Push-to-start registration**: `PUT /api/mobile/live-activities/start-token` accepts `{ startToken, environment, bundleId, activityType?, appVersion? }` and upserts the caller's private push-to-start registration, rotating an existing token in place. `environment` is `sandbox` or `production` and selects the APNs host per registration; `activityType` defaults to the account-level mission activity attributes type. A start token is globally unique and bound to exactly one profile, so registering a token held by another profile reassigns it. Like a device token it is opaque, travels in the request body rather than the URL, and is never returned or exposed through realtime, logs, entity changes, or read APIs.
- **Push-to-start consent and revocation**: Holding a `live_activity_start_tokens` row **is** the account's consent to desktop-initiated Live Activities; with no row the server can never remotely start one. `POST /api/mobile/live-activities/start-token/revoke` accepts `{ startToken }`, deletes only the caller's registration, returns `204`, is safe to retry, and is how that consent is withdrawn. Clients must revoke before clearing credentials on sign-out.
- **Push-to-start delivery**: A mission entering execution enqueues a coalesced `overlord.live_activity.start.v1` job for the mission's assigned profile only — never broadcast to a workspace — alongside the ordinary refresh job. The payload carries only the target profile and triggering mission ids; the worker recomputes the bounded snapshot at delivery time and sends an APNs `liveactivity` payload with `event: "start"`, `attributes-type`, `attributes`, `content-state`, a stale date, and a bounded alert, at `apns-priority: 10` on the registration's own `<bundleId>.push-type.liveactivity` topic and environment host. A start is suppressed when the account already holds any `live_activity_push_tokens` registration, when no mission is running, and for five minutes after a start was sent to that token. Delivery is durable and retried with backoff; `410 Unregistered` and `400 BadDeviceToken` retire the start registration. With no APNs credentials configured, enqueueing still succeeds and dispatch no-ops.
- **Update-token handoff**: after APNs remotely starts an activity, the client registers the per-activity **update** token through `PUT /api/mobile/live-activities/:activityId/push-token` with the additive optional `startedByPush: true`, recorded as `live_activity_push_tokens.origin = 'push_to_start'` (default `local`). From that point the ordinary dispatch job owns every update and the end for that activity. A push-to-start token, a per-activity update token, and a standard device token are three distinct credentials with distinct lifetimes and are never interchangeable: separate tables, routes, and payload events. Local starts from the app remain supported and unchanged.
- **Privacy**: APNs payloads include only the bounded display snapshot, the epoch-seconds `updatedAt` timestamp, and — for a start — the static account label attribute and an alert built from that same bounded snapshot. They never include bearer tokens, ActivityKit tokens, objective instructions, agent prompts, event payloads, or full mission detail.

### Mobile → REST (Standard Push Notification Surface)

- **Transport**: Authenticated HTTPS/JSON with the existing Better Auth session or `out_…` bearer token; no ambient workspace input is accepted.
- **Separation from Live Activities**: standard APNs **device** tokens and ActivityKit tokens are distinct credentials with distinct lifetimes and are never interchangeable. They use separate tables (`device_push_tokens` vs `live_activity_push_tokens`), separate routes, separate APNs topics (`<bundleId>` vs `<bundleId>.push-type.liveactivity`), and separate `apns-push-type` values (`alert`/`background` vs `liveactivity`). Passing one where the other is expected is a contract violation.
- **Registration**: `PUT /api/mobile/push/device-token` accepts `{ deviceToken, environment, bundleId, appVersion? }` and upserts the caller's registration, rotating an existing token in place. `environment` is `sandbox` or `production` and selects the APNs host per registration. A device token is unique globally and bound to exactly one profile; registering a token held by another profile reassigns it. Tokens are opaque, are carried in the request body rather than the URL, and are never returned or exposed through realtime, logs, entity changes, or read APIs.
- **Revocation**: `POST /api/mobile/push/device-token/revoke` accepts `{ deviceToken }`, deletes only the caller's registration, returns `204`, and is safe to retry. Clients must revoke before clearing credentials on sign-out.
- **Preferences**: `GET`/`PUT /api/profile/notification-preferences` read and merge the caller's own preferences as `{ enabled, preferences: { type, transport, mode }[] }`. `type` is exactly a shared notification-catalog id, `transport` is exactly a catalog-declared eligible transport, and `mode` is `alert`, `silent`, or `off`; absent rows default to the catalog's `defaultMode`. The reserved `(type: all, transport: all)` master row powers `enabled` and suppresses notification transports without affecting Live Activity delivery. Unknown types, transports, type/transport pairs, and modes are rejected `400`. For additive mobile compatibility, responses also project APNs-capable types as `{ categories: { category, mode }[] }`, and requests may use that legacy field as an APNs-only update alias; new clients use `preferences`.
- **Delivery**: The same lifecycle mutations that refresh Live Activities enqueue deduplicated `overlord.push_notification.dispatch.v1` worker jobs addressed to the mission's assigned profile only — never broadcast to a workspace. Job payloads carry ids alone; the dispatcher recomputes presentation content at delivery time. Alerts are `apns-push-type: alert`, `apns-priority: 10`, expiring after one hour; `silent` categories are `apns-push-type: background`, `apns-priority: 5`, `content-available: 1`, with no alert body. Notifications collapse per `(objectiveId ?? missionId, category)`. Delivery is durable and retried with backoff; `410 Unregistered` and `400 BadDeviceToken` retire the registration. With no APNs credentials configured, enqueueing still succeeds and dispatch no-ops so local development is unaffected.
- **Privacy**: Payloads contain only the bounded project name, the sanitized bounded title and display id of the objective when the row has an `objective_id` (otherwise the mission), a badge count, the mission id, optional objective id/display id, the category, and a deep link (`overlord://objectives/<displayId>` when an objective is known, else `overlord://missions/<missionId>`). They never contain bearer tokens, session keys, ActivityKit tokens, objective instructions, agent prompts, delivery summaries, change rationales, file paths, diffs, question text, or any `mission_events.payload_json` content. User-authored text reaches a payload only through the shared markdown-stripping bounded-title sanitizer.

### MCP Server → Auth (Hosted Agent Auth Surface)

- **Transport**: HTTP bearer credentials on `GET/POST /mcp`
- **Discovery**: `GET /.well-known/oauth-protected-resource` and
  `GET /.well-known/oauth-protected-resource/mcp` advertise the MCP resource URL
  and authorization-server metadata locations
- **Authorization server**: OAuth-aware clients use dynamic registration (`POST /oauth/register`), browser approval (`GET /oauth/authorize` redirecting to the web approval page), authorization-code + PKCE token exchange (`POST /oauth/token`), and token revocation (`POST /oauth/revoke`). The web app may serve same-domain `.well-known` metadata and proxy `/mcp` plus `/oauth/*` traffic to the backend when deployed separately from the backend.
- **OAuth resource binding**: when an OAuth client supplies the RFC 8707
  `resource` parameter, approval and token exchange bind it to the canonical
  hosted `/mcp` resource; a mismatched resource is an OAuth `invalid_target`
  error. This prevents an authorization code minted for Overlord from being
  replayed against a different resource.
- **Rule**: MCP handlers must resolve the caller through the Auth Layer before
  listing or invoking tools. A request that is missing or fails authentication
  returns an OAuth-compatible `WWW-Authenticate: Bearer` challenge.
- **Workspace binding**: MCP authenticates a profile/credential but does not select an ambient workspace. Project and mission resources derive their workspace; account-wide tools aggregate authorized memberships; writes require explicit project identity where no parent resource supplies scope.

### MCP Server → Service Layer

- **Transport**: In-process function calls from MCP tool handlers to existing
  service/protocol functions
- **Rule**: MCP handlers must not read or write database tables directly.
- **Authorization**: Every tool call must pass through the same RBAC gates as the
  corresponding REST or protocol operation.
- **Project context**: Hosted MCP cannot observe a cloud agent's current working
  directory. A caller that supplies `projectId` creates a normal mission after
  resolving that resource; an omitted project for the explicit inbox-capable
  creation tools produces an account-owned inbox item, never an implicit project.
- **Locality**: Hosted MCP must not expose local filesystem/worktree inspection,
  runner queue claiming, execution target mutation, or branch-action tools.
- **Widget safety**: MCP Apps resources use `ui://overlord/*` URIs and return
  self-contained HTML. They may render only the associated tool's
  `structuredContent`; resources must not embed third-party frames, browser
  credentials, secrets, or unrestricted network access.

### Auth → Database (Identity Bridge)

- **Transport**: Better Auth's configured database adapter for auth tables; direct service-layer/database adapter queries for identity bridging. The auth domain query helpers (`queryOne`/`queryAll`/`execute`) run against the async `DatabaseClient` (preferred) or, for tests/legacy callers, a raw `better-sqlite3` handle or a bare `PostgresQueryExecutor`; the webapp routes both editions through the `DatabaseClient`
- **Auth tables owned**: `user`, `session`, `account`, `verification`, `apikey` — auth-internal; no other component accesses them directly
- **Identity bridge**: Auth Layer reads `profiles` (via `profiles.id = Better Auth user.id`) to resolve an authenticated identity. It resolves `workspace_users` and role assignments only for an explicitly named workspace, a named resource's workspace, or each membership in an account-wide operation; it never reads a workspace header or cookie as request scope.
- **Role resolution**: Auth Layer reads `role_assignments` for the resource-derived or explicitly selected workspace user to build the authorization subject
- **Rule**: Auth Layer must not write to core domain tables (missions, projects, etc.) itself
- **Exception**: the `deleteUser` `beforeDelete` hook invokes a backend-supplied `onDeleteUser(userId)` callback (`backend/account-deletion.ts`, injected via `CreateAuthOptions`) to hard-purge the `ON DELETE RESTRICT` children of `profiles` and `workspace_users` — `workspace_users`, `user_tokens`, `user_images`, `role_assignments`, `workspace_user_execution_targets`, `project_user_preferences` — before the auth `user` row is hard-deleted, which in turn hard-cascades `session`, `account`, `profiles`, and `user_execution_target_preferences`. `RESTRICT` blocks on row existence regardless of `deleted_at`, so this must be a hard purge, not a tombstone; each purge still emits an `entity_changes` row. The write happens inside the caller-supplied callback, not in Auth Layer code, mirroring the Service → Automations delegation pattern (caller-supplied persistence callbacks)
- **Email delivery**: sign-up/sign-in verification emails are sent through a backend-supplied `sendVerificationEmail(params)` callback (`backend/email-verification.ts`, injected via `CreateAuthOptions`), the same caller-supplied-callback pattern as `onDeleteUser`. Auth Layer invokes the callback with Better Auth's `{ user, url, token }` plus a minted 6-digit `otp` (when the `emailOTP` plugin is enabled via `sendEmailOTP`); it does not know about or depend on the concrete provider (Resend, configured via `RESEND_API_KEY` and the `notifications.cooperativ.io` domain). The verification email shows the `otp` in its code block and uses `token` only to build the clickable link — the raw `token` is never displayed as a typed code
- **One-time codes (OTP)**: when `sendEmailOTP` is injected, the `emailOTP` plugin is registered and writes codes to the auth-internal `verification` table (identifier `email-verification-otp-<email>` etc.). It adds `/api/auth/email-otp/*` endpoints; the sign-up confirmation code is minted inside `sendVerificationEmail` (no separate email), while `sendEmailOTP` delivers standalone codes for passwordless sign-in and password reset. Codes are 6 digits and expire after 1 hour

### Service → Automations (Automation Surface)

- **Transport**: Service-layer function calls to the automations module's exported package API (`@overlord/automations`)
- **Browser-safe subpaths**: SPA / renderer callers that need pure lifecycle or scheduling helpers MUST import `@overlord/automations/objective-manager` or `@overlord/automations/scheduling-engine` instead of the package root. The root barrel also re-exports Gemini-backed title tools and therefore pulls `@google/genai` into the client graph when imported from the webapp.
- **Configuration**: Environment variables documented in `.env.prod.example` / `.env.local.example` (`GEMINI_API_KEY`, optional model override)
- **Rule**: Automations must not read or write domain tables directly; persistence goes through caller-supplied store interfaces (e.g. `ObjectiveTitleStore`)
- **Fallback**: When a provider is unavailable or a call fails, automations return `null` and callers use deterministic local fallbacks
- **Downstream extension**: Forks add automations via `OVERLORD_AUTOMATIONS_MODULE` (`custom-automation` extension point), registering through the module API — never by editing the built-in registry

### Extension → Core (Extension Surface)

- **Database**: Migrations in `ext_<name>_` prefixed tables; `schema_migrations.component = 'ext:<name>'`
- **Metadata**: Namespaced keys in `metadata_json` / `settings_json` (reverse-DNS or package-style)
- **Reactions**: Via service APIs, `entity_changes`, and `outbox_messages`; no direct writes to core tables
- **Connector extensions**: Via `user_harness_extensions` → `workspace_harness_extensions` promotion

### Desktop Shell → REST (Renderer Surface)

- **Transport**: The hardened `BrowserWindow` loads the SPA and calls `/api/*` (including the SSE realtime stream) — over the loopback `http`/`ws` origin in **Local** mode, or over the hosted backend's HTTPS/`wss` origin (a separate origin from the local shell) in **Remote** mode, via the SPA's runtime-injected API base URL. Either way the SPA is just another HTTP client of the existing REST surface
- **Auth**: In **Local** mode, Better Auth session cookies work natively because the renderer and API share the same loopback origin; the shell injects no headers and stores no tokens. In **Remote** mode (cross-origin), the shell uses only the active profile's Better Auth **session bearer token**, obtained at login, persisted per-backend via `safeStorage`, and injected into the SPA's API and realtime clients. It does not mint, read, or prefer a `USER_TOKEN`; that credential remains for CLI/headless clients. Selected workspace is client-side navigation state and is never injected into requests.
- **Remote OAuth handoff**: Social OAuth starts at `/api/auth/desktop/github` in the system browser, which lets Better Auth create and validate its OAuth state cookie in that browser's cookie jar. After Better Auth establishes its browser session, `/api/auth/desktop/callback` redirects only to `overlord://auth/callback?ticket=…`; the ticket is opaque, single-use, and short-lived. Electron's main process exchanges it over HTTPS at `/api/auth/desktop/exchange`, writes the session token to the active profile's `safeStorage`, and reloads the local shell. The renderer and custom URL never receive a session token, GitHub token, client secret, or reusable OAuth state
- **Mission deep links**: Electron accepts only `overlord://missions/<missionId>` values whose id matches `[A-Za-z0-9:_-]{1,64}`. It focuses or creates the shell window and forwards only the fixed internal `/user/missions/<missionId>` route through the feature-detected `window.overlord.onNavigate` bridge; this URL contains no credentials, ticket, or payload.
- **Rule**: The shell must not fork or modify the SPA; desktop-only affordances are exposed only through the feature-detected `window.overlord` preload bridge. Navigation stays scoped to the shell origin in both modes
- **Durable notifications**: The SPA consumes profile-owned `GET /api/notifications` history and authorized `notification` entity changes. It does not derive notification policy from mission, objective, execution-request, or mission-event realtime changes. When available, it invokes the bounded `window.overlord.showNotification` bridge; a browser uses the Web Notification API. The separate app-update and CLI-update banner UI is not a user-notification transport.

### Desktop Shell → CLI (Process Supervision Surface)

- **Transport**: Subprocess / Node `utilityProcess` invocation of the bundled CLI and server (`ovld serve`, `ovld runner`, `ovld launch`, `ovld runner service <action>`), analogous to the Agent → Protocol subprocess pattern
- **Credential**: For spawned CLI that needs a network credential (remote/shared deployments), the shell passes a `USER_TOKEN` via `Overlord_USER_TOKEN`; pure-loopback execution talks to the local service layer and needs none
- **Persistent runner control**: The `runnerService.*` preload ops spawn `ovld runner service <install|start|stop|restart|status|uninstall> --json` in the shell's main process, injecting the active backend URL and (remote mode) token so an install captures the right credential; the parsed JSON status is returned to the renderer. The shell never claims runner queue work, supervises the loop, or generates service files itself — those stay CLI/Runner-owned
- **Rule**: The shell triggers existing CLI/runner behavior; it must not reimplement launch, terminal, or config logic the CLI/Runner Layers own

---

## Conformance Requirements

Any component, connector, or extension that ships against Overlord must:

1. **Provide a conformance manifest** (`conformance-manifest.yaml`) at its root declaring:
   - `contractVersion`: the version this component was validated against
   - `componentType`: one of `connector`, `extension`, `database-adapter`, `auth-provider`, `rest-module`, `rest-consumer`, `desktop-shell`, `mcp-server`
   - `componentKey`: stable lowercase identifier
   - All capabilities and extension points it uses

2. **Pass validation** via `ovld contract check <manifest-file>`

3. **Respect ownership boundaries**: only interact with other components through declared surfaces

4. **Use namespaced identifiers** for all extension values — table names, JSON keys, event types, vocabulary extensions

5. **Declare all vocabulary extensions** from open vocabularies in its conformance manifest

### Agent connector conformance

In addition to the requirements above, a connector must:

1. **Ship a harness capability descriptor** at `connectors/adapters/<agent>/harness-capabilities.yaml`
   that validates against [`contract/harness-capabilities.schema.yaml`](contract/harness-capabilities.schema.yaml),
   and record its path, schema version, and SHA-256 digest in the conformance manifest.
2. **Prove every `supported` capability with an executable fixture that CI runs.** Checking that
   a named file exists would let an empty fixture claim a capability into existence. Claimed-but-
   unproven is worse than absent, because the UI renders controls that do nothing.
   For callback/extension decision capabilities, a recorded payload shape is necessary but not
   sufficient: a `decision-codec` fixture must execute the shipped request decoder and response
   encoder and pin the bounded request card plus exact allow/deny/defer bytes.
3. **Justify every other status.** `unsupported` requires a reason and a stable `evidenceRef`;
   `not-implemented` and `unverified` require a tracker id. Promoting a status out of
   `unsupported` must replace `evidenceRef` with executable fixtures in the same change, so
   turning a hard limit into work leaves a trace.
4. **Ship the mandatory unbound-session negative fixture**, demonstrating that an unbound session
   in an unrelated directory is untouched and unblocked by every registered integration. A side
   effect the fixture records must be declared as a hazard with a tracker.
5. **Never hand-edit generated descriptor metadata.** `integrationShape`, `capabilityTier`, and
   `harnessCapabilities` are produced from the descriptor. The tier is derived from passing
   fixtures; a connector that declares more than its fixtures prove fails conformance.
6. **Make no network call from adapter code.** Adapters translate and hand off to the CLI, which
   owns transport, auth, retry, and redaction.

### Virtual Target Gateway conformance

A **Virtual Target Gateway** is a `rest-consumer` (e.g. Racecar) that realizes virtual execution requests. In addition to the requirements above, it must:

1. **Never access the Overlord database directly.** It interacts only through the documented `/api/virtual-targets/v1/*` REST surface; no direct DB access is possible or required.
2. **Be idempotent by `executionRequestId`.** Claim, retry, duplicate delivery, and gateway restart for the same request must yield exactly one realized environment and one run; a post-launch gateway crash must not destroy or duplicate the environment.
3. **Preserve the normal protocol lifecycle.** A gateway records observations and drives the `launching → launched|failed` transition, but it never completes an objective — only an agent protocol delivery does. It must not introduce a parallel request state machine.
4. **Vendor the published virtual-target DTOs**, validate against this contract version, and declare its called endpoints and its namespaced adapter key (e.g. `racecar`) in its `conformance-manifest.yaml` (`componentType: rest-consumer`). The adapter key is an open-vocabulary value, not a new `execution_targets.type`.

See [`contract/conformance-manifest.schema.yaml`](contract/conformance-manifest.schema.yaml) for the required manifest shape, and [`contract/examples/rest-consumer-racecar-conformance-manifest.yaml`](contract/examples/rest-consumer-racecar-conformance-manifest.yaml) for a Virtual Target Gateway template.

---

## Shared Deterministic Algorithms

A few pure, deterministic computations must produce **identical** results in more than one component. Rather than share a runtime package (which couples the components' build/publish graphs), each component keeps its own copy of the algorithm and both copies are pinned to a single committed **conformance fixture** of golden input→output vectors. CI runs both component test suites against the same fixture, so the implementations cannot drift apart.

### Per-mission branch/worktree planning

- **Algorithm**: `planMissionBranch`, `previewMissionBranch`, `missionWorktreePath`, `sanitizeBranchName`, `canonicalMissionBranch`, `slugifyBranchTitle` (and the `BranchDecision*` / `BranchIsolation` types). `missionWorktreePath` takes `worktreeRoot`, `projectSlug`, `resourceKey`, and `branch`, and the resource key is always included between the project slug and branch leaf. `planMissionBranch` takes an optional `isolation: { objectiveKey }`: when set (and no `overrideBranch` is pinned) it returns `<the branch it would otherwise have planned>-<objectiveKey>` — created from that branch, reused when it already exists unmerged, cycled with a numeric suffix once merged — so two objectives running in parallel on one resource never plan the same worktree. `previewMissionBranch` never isolates; it predicts mission-level branch metadata.
- **Implementations** (must stay byte-for-byte equivalent in behavior):
  - Runner Layer — `cli/src/branch-planning.ts`, used by `cli/src/branch-preparation.ts` to **prepare** the real branch/worktree before launch.
  - Service layer (backend) — `backend/branch-planning.ts`, used by `backend/repository.ts` to **predict** `MissionBranchDto` branch metadata surfaced over REST.
- **Conformance fixture**: [`contract/branch-planning-vectors.json`](contract/branch-planning-vectors.json). Both `cli/test/branch-planning.test.ts` and `backend/branch-planning.test.ts` assert their implementation against every vector, including resource-scoped worktree paths.
- **Rule**: Neither component may import the other's copy across the boundary. Any change to the algorithm MUST update both copies, regenerate the fixture, and **bump the contract version** — the fixture is a stable interface.

## Extension Points

The only sanctioned ways to extend Overlord:

| Extension Point        | Mechanism                                                                | Boundary                |
| ---------------------- | ------------------------------------------------------------------------ | ----------------------- |
| Custom agent connector | New adapter + `conformance-manifest.yaml` + `harness-capabilities.yaml`  | Connector Layer         |
| Custom harness         | `user_harness_extensions` + workspace promotion                          | Extension System        |
| Custom automation      | Module(s) via `OVERLORD_AUTOMATIONS_MODULE` calling `registerAutomation` | Automations Layer       |
| Database adapter       | Implement full logical schema + pass conformance tests                   | Database Layer          |
| Database extension     | `ext_<name>_` tables + `schema_migrations.component`                     | Database Layer          |
| Auth/RBAC provider     | Auth Layer service boundary                                              | Auth Layer              |
| REST extension         | Namespaced endpoint prefix (`/ext/<name>/`)                              | REST API Layer          |
| Open vocabulary values | Namespaced values declared in manifest                                   | Database/Protocol Layer |

Attempting to extend Overlord through any other path — patching core tables directly, adding undeclared hook types, using closed vocabulary values — is a contract violation that must be resolved before the component ships.

See [`contract/extension-points.yaml`](contract/extension-points.yaml) for machine-readable
extension declarations and the agent-session capability vocabulary.

---

## Controlled Vocabularies

### Closed (contract-version-bump required to add values)

- `objectives.state`: `future`, `draft`, `submitted`, `launching`, `executing`, `pending_delivery`, `complete`
- `missions.created_by_kind`, `objectives.created_by_kind`: `human`, `agent`, `automation`
- `project_statuses.type`: `draft`, `next`, `execute`, `review`, `complete`, `blocked`, `cancelled`
- `execution_requests.status`: `queued`, `claimed`, `launching`, `launched`, `failed`, `cleared`, `cancelled`, `expired`
- `run_queue_entries.state`: `waiting`, `blocked`, `dispatched`, `running`
- `agent_sessions.delivery_state`: `not_delivered`, `delivered`, `pending_redelivery`
- `mission_events.type`: `update`, `user_follow_up`, `alert`, `discussion_summary`, `decision`, `ask`, `permission_request`, `delivery`, `execution_requested`, `awaiting_approval`, `status_change`
- `permission_requests.status`: `requested`, `approved`, `denied`, `expired`, `not_required`
- `idempotency_keys.status`: `in_progress`, `completed`, `failed`
- `audit_log.result`: `allowed`, `denied`, `failed`
- `workspace_invitations.status`: `pending`, `accepted`, `revoked`, `expired`
- RBAC role names (`role_assignments.role_key` core, non-extension values; enumerated in `auth/src/rbac/types.ts`'s `Role` enum and `overlord.rbac.toml`): `ADMIN`, `MANAGER`, `MEMBER`, `PUBLIC`

### Open (extensions may add namespaced values)

- `workspaces.kind`, `profiles.kind`
- `execution_targets.type` (documents `local` and `virtual` as core values; a virtual target is realized by an external gateway over the Virtual Target Queue Surface), `project_resource_sources.source_kind`
- Gateway **adapter keys** (e.g. `racecar`) and virtual source/observation/failure/grant/action kinds — namespaced open-vocabulary values declared in a gateway's `rest-consumer` conformance manifest, never new closed-vocabulary values or `execution_targets.type` values
- `artifacts.type`
- `mission_events.source`, `entity_changes.entity_type`, `entity_changes.source`
- `outbox_messages.topic`, `worker_jobs.type`
- RBAC permission names
- Connector/agent identifiers

Full value lists live in the database schema contract "Controlled Vocabularies" section.

Post-delivery follow-up execution reuses existing vocabulary values: discussion-only
follow-ups append `mission_events.type = user_follow_up` while the objective remains
`complete`; explicit resume transitions the objective to `pending_delivery` and
creates a new session with `agent_sessions.delivery_state = pending_redelivery`.

---

## Machine-Readable Contract Files

The `contract/` directory contains machine-readable counterparts:

- [`contract/components.yaml`](contract/components.yaml) — Component registry with capabilities and interface declarations
- [`contract/protocol-commands.yaml`](contract/protocol-commands.yaml) — Protocol command names, required flags, and response shape versions
- [`contract/extension-points.yaml`](contract/extension-points.yaml) — Sanctioned extension points and approved capability flags
- [`contract/conformance-manifest.schema.yaml`](contract/conformance-manifest.schema.yaml) — JSON Schema (YAML) for conformance manifests
- [`contract/harness-capabilities.schema.yaml`](contract/harness-capabilities.schema.yaml) — JSON Schema (YAML) for the per-adapter agent-session capability descriptor. Referenced from the connector block of the conformance-manifest schema and from the `agentSession` vocabulary in `contract/extension-points.yaml`
- [`contract/branch-planning-vectors.json`](contract/branch-planning-vectors.json) — Golden input→output vectors pinning the duplicated per-mission branch/worktree planning algorithm (see "Shared Deterministic Algorithms")

---

## Contract Maintenance Rules

### When to update this document

| Change                                 | Contract update required?                                                                                                                 |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| New component added                    | Yes — add to component registry and `contract/components.yaml`                                                                            |
| New interaction surface                | Yes — add to interaction surfaces and `contract/components.yaml`                                                                          |
| Protocol command added/renamed         | Yes — update `contract/protocol-commands.yaml`                                                                                            |
| Protocol command flag changed          | Yes — update `contract/protocol-commands.yaml`; bump version if breaking                                                                  |
| Database table/column added            | Yes — update `09-database-schema-contract.md`                                                                                             |
| Closed vocabulary value added          | Yes — requires contract version bump                                                                                                      |
| Open vocabulary value promoted to core | Yes — add to `09-database-schema-contract.md` Controlled Vocabularies                                                                     |
| New extension point                    | Yes — add to `contract/extension-points.yaml` and this document                                                                           |
| New agent-session capability id        | Yes — add to `agentSession.capabilityIds` in `contract/extension-points.yaml` and the descriptor schema; requires a contract version bump |
| Connector capability status change     | No contract update — but a `supported` claim requires a passing fixture, and generated descriptor artifacts must be regenerated           |
| New connector shipped                  | Conformance manifest only; no contract update unless new capabilities needed                                                              |
| Extension shipped                      | Conformance manifest only; no contract update unless new extension points needed                                                          |

### Procedure for contract-modifying changes

1. Read and understand the current contract (this document + relevant `contract/*.yaml` files)
2. Draft the required contract changes
3. **Increment the contract version** in the sole authoritative `Current version` line under
   [Contract Version](#contract-version) above, and in `contract/components.yaml`. Do not add
   or update any other numeric version statement in this document.
4. Add a changelog entry to this document
5. Implement the changes in code and other docs
6. Verify any affected component's conformance manifest passes `ovld contract check`

**The implementation code must not land before the contract update.** If you are implementing code that would conflict with or extend the existing contract without updating it first, stop and update the contract first.
