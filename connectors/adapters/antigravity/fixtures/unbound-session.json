{
  "fixtureVersion": 1,
  "kind": "source-guard",
  "description": "Mandatory negative test: the Antigravity adapter registers no callback in an unbound session while its native decision and prompt-capture paths remain unimplemented.",
  "path": "connectors/adapters/antigravity/hooks.json",
  "assert": {
    "mustNotContain": [
      "PreInvocation",
      "PreToolUse",
      "pre-invocation-hook.sh",
      "pre-tool-use-hook.sh"
    ]
  }
}
