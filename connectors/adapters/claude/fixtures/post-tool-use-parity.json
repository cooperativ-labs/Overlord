{
  "fixtureVersion": 1,
  "kind": "source-guard",
  "description": "Migration parity guard. The normalized agent-session event registration is ADDITIVE: the legacy PostToolUse hook that feeds touched-file change attribution, and the legacy UserPromptSubmit hook that posts follow-up mission events, must both remain registered alongside it. Deleting either to 'clean up' would silently break delivery attribution or follow-up capture, and neither failure is visible until someone tries to deliver.",
  "path": "connectors/adapters/claude/hooks/hooks.json",
  "assert": {
    "mustContain": [
      "scripts/post-tool-use-hook.sh",
      "scripts/user-prompt-submit-hook.sh",
      "scripts/agent-session-event.sh"
    ]
  }
}
