{
  "fixtureVersion": 1,
  "kind": "source-guard",
  "description": "Cursor stop hook prefers agent-session inbox for Queued(turn-boundary) followup_message when a channel is bound.",
  "path": "connectors/adapters/cursor/hooks/overlord-stop.sh",
  "assert": {
    "mustContain": [
      "OVERLORD_SESSION_CHANNEL_ID",
      "agent-session inbox --agent cursor",
      "followup_message"
    ]
  }
}
