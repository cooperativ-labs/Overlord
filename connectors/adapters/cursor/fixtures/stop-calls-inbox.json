{
  "fixtureVersion": 1,
  "kind": "source-guard",
  "description": "Cursor stop hook reminds the agent to deliver over protocol. It does not inject session-input inbox messages.",
  "path": "connectors/adapters/cursor/hooks/overlord-stop.sh",
  "assert": {
    "mustContain": ["ovld protocol hook-event", "followup_message"],
    "mustNotContain": ["agent-session inbox"]
  }
}
