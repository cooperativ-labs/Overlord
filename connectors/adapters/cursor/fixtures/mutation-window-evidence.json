{
  "fixtureVersion": 1,
  "kind": "mutation-window",
  "description": "Cursor's recorded pre/post samples carry different call ids, so they prove completion-side evidence rather than a pair.",
  "classification": "post-only",
  "preFixture": "fixtures/normalize-pre-tool-use-shell.json",
  "postFixture": "fixtures/normalize-post-tool-use-write.json",
  "proof": {
    "pre": {
      "session": "conversation_id",
      "call": "call_id",
      "workspace": "cwd",
      "tool": "tool_name"
    },
    "post": {
      "session": "conversation_id",
      "call": "call_id",
      "workspace": "cwd",
      "tool": "tool_name",
      "directPath": "tool_input.file_path",
      "completion": {
        "path": "event",
        "equals": "postToolUse"
      }
    },
    "mismatches": [
      {
        "field": "call",
        "pre": "call_id",
        "post": "call_id"
      }
    ]
  }
}
