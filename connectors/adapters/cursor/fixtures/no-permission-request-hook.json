{
  "fixtureVersion": 1,
  "kind": "source-guard",
  "description": "Cursor has no PermissionRequest hook — its Claude-compatibility map resolves that event to null and lists it as unsupported. The generated conformance projection must therefore never re-declare the permissionHook capability or the PermissionRequest hook type for this adapter, and the installer registers the real per-tool-class decision events instead.",
  "path": "connectors/adapters/cursor/conformance-manifest.yaml",
  "assert": {
    "mustContain": ["componentKey: cursor"],
    "mustNotContain": ["permissionHook", "PermissionRequest"]
  }
}
