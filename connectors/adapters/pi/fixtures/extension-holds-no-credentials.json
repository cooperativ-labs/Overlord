{
  "fixtureVersion": 1,
  "kind": "source-guard",
  "description": "Pi's extension runs inside the agent process with its memory, credentials, and event loop. It must translate, call the CLI, and return: no HTTP client, no Overlord backend import, no token read, and every async call caught. This guard is also the adapter's unbound-session negative test — an in-process extension has no script to run, so the property is asserted on the code that ships.",
  "path": "connectors/adapters/pi/extensions/overlord.ts",
  "assert": {
    "mustContain": ["pi.exec", ".catch("],
    "mustNotContain": ["fetch(", "USER_TOKEN", "backend-client", "https://"]
  }
}
