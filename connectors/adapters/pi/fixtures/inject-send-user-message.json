{
  "fixtureVersion": 1,
  "kind": "source-guard",
  "description": "Pi no longer drains an Overlord session inbox. Latch PTY write is Phase 3.",
  "path": "connectors/adapters/pi/extensions/overlord.ts",
  "assert": {
    "mustNotContain": ["agent-session", "inbox", "sendUserMessage", "--confirm"]
  }
}
