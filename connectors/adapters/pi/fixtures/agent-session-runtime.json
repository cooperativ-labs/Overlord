{
  "fixtureVersion": 1,
  "kind": "source-guard",
  "description": "Pi Channel 1 captures follow-up through ovld protocol hook-event. Mechanical agent-session observe/decide/inject is not a live path.",
  "path": "connectors/adapters/pi/extensions/overlord.ts",
  "assert": {
    "mustContain": [
      "hook-event",
      "UserPromptSubmit",
      "pi.on('input'",
      "cacheNativeSession"
    ],
    "mustNotContain": [
      "agent-session",
      "fetch(",
      "USER_TOKEN",
      "backend-client",
      "https://"
    ]
  }
}
