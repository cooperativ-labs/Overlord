# Promote button icon: ArrowUpToLine

Future-objective Promote in `DraftObjectiveToolbar` now uses Lucide `ArrowUpToLine` instead of `ArrowUpCircle`, matching the mobile SF Symbol `arrow.up.to.line` convention from the composer primary-button ladder.
